# 批判レビューの記録

`docs/quality-gate.md` の合格判定に使った、実際に回したレビューの記録。
**何を、どの観点から、どう測って、何が出たか**を残す。出なかったことも残す —
「調べていない」と「調べたが出なかった」は違う。

## 方式

サブエージェントによる並行レビューを試みたが、この環境では起動したエージェントが
完了報告を返さなかった (最初の3体、絞り込み後の3体、診断用の最小タスク1体、
いずれも無応答)。**待ち続けても閉じないループより、閉じるループを選んだ。**

代わりに、観点ごとに独立した検証スクリプトを書き、**実プロセスとして同時に走らせた**。
各観点は別のブラウザインスタンスで動き、結果をファイルに残す。

## 観点A: アクセシビリティ / キーボード完結性

| 項目 | 結果 |
|---|---|
| 名前の無いボタン | なし (全ボタンにテキストか title) |
| Tab 到達可能な要素 | 35 |
| オーバーレイの tabindex | 0件。ただし矢印キーによる巡回を実装済みで、キーボードだけで全要素に到達できる |
| `<html lang>` | `ja` |
| ステータスバー | `rgb(139,148,158)` / 背景 `rgb(22,27,34)` / 11px |

**指摘なし。**

## 観点B: データ整合性 (同じ値で書き戻して内容が変わらないか)

21図種すべてでテンプレートをパースし、各要素のラベルを**同じ値で**書き戻して、
要素集合が変わらないことを確認。

- 20図種: OK (書き換え0件、要素集合が同一)
- kanban: 一度「★ 要素が変わった」と出たが、**検証スクリプト側の誤り**だった。
  `updateColumn(text, line, newName)` は第3引数が値なのに、スクリプトが汎用の
  `(text, line, field, value)` を仮定して `'label'` を渡していた。
  実機で列名を `Todo` → `作業待ち` に変えて意図どおり動くことを確認済み。

**実装の指摘なし。** ただし更新関数のシグネチャがモジュール間で揃っていない
(`updateColumn(text,line,value)` と `updateTaskField(text,line,field,value)`) のは
事実で、呼び出し側が間違えやすい。今回は誤検出だったが、将来の呼び出しミスの
温床にはなる。

## 観点C: 壊れた入力への堅牢性

8種の入力で pageerror が出ないことを確認。

| 入力 | status | pageerror |
|---|---|---|
| 空文字 | Error | なし |
| 空白だけ | Error | なし |
| 未知の図種 | Error | なし |
| 途中で切れた記法 | Error | なし |
| 5000文字の1ラベル | OK | なし |
| 制御文字 (NUL/BEL) | OK | なし |
| HTMLタグ入りラベル | Error | なし |
| 40段ネストの block | Error | なし |

**指摘なし。** status=Error は mermaid が拒否した表示であって、アプリは生きている。
どのケースでもプロパティパネルは描画され、エディタは操作可能なまま。

## 観点D: 性能回帰

| ノード数 | 流し込み→描画 | 1打鍵→反映 | overlay | 選択1回 | 矢印1回 |
|---|---|---|---|---|---|
| 20 | 56ms | 45ms | 21 | 83ms | 60ms |
| 60 | 90ms | 102ms | 61 | 83ms | 55ms |
| 120 | 159ms | 227ms | 121 | 68ms | 56ms |

**指摘なし。** 今回追加した処理 (オーバーレイの座標変換、選択の生存確認、
キーボード巡回) は要素数に対して線形で、120ノードでも体感を損なわない。

## 観点E: 自作コードへの敵対検証 (接続モード)

新しく書いたコードほど検証が薄いので、直前に実装した接続モードを標的に
3周のループを回した。詳細はコミット
`fix(connect): 自作の接続モードを自分で敵対検証して見つけた5件` に記録。

| 周 | 検出 |
|---|---|
| 1 | 図種切替でモードが残り別図に誤接続 / Escape で抜けられない / モード表示が無い |
| 2 | モード中に矢印キーで選択が動く / 始点を消しても線が引ける |
| 3 | 新規なし (収束) |

## 総括

実装に対する指摘は観点Eからのみ出た。A/C/D は問題なし、B は検証スクリプト側の
誤りだった。**「出なかった」を根拠にするために、何をどう測ったかを上に残している。**

---

# 第2ラウンド: 観点を増やす (2026-08-21)

6観点すべてが指摘0になった時点で「合格」を出したが、**0件は「その観点を見て
いない」でも出る**。ヘビーユーザ視点の8観点 (操作コスト / 発見可能性 / 一貫性 /
フィードバック / エラー回復 / 学習曲線 / スケール耐性 / ワークフロー適合) に対して
既存の6観点が見ていたのは実質3つだったので、見ていない側を足した。

## 追加した観点

| レビュアー | 観点 | 見るもの |
|---|---|---|
| r7-consistency | 一貫性 / 発見可能性 / エラー回復 | 21図種のプロパティ欄を横に並べ、同じ概念に同じ語・同じ操作が出ているか。削除が Undo 1回で戻るか |
| r8-scale | スケール耐性 | 100要素の図で、取りこぼし・末尾要素の選択・「押した要素が消えるか」・所要時間 |

## 見つかった欠陥

| # | 重大度 | 内容 |
|---|---|---|
| A22 | Critical | sequence の participant 削除が宣言行しか消さず、`A->>B` の参照が残るので**一覧から消えても図には残り続ける**。class / er / state を id 認識に直したとき sequence だけ取り残していた |
| A23 | Major | gantt だけプロパティ欄にタスク一覧が無く、タスクを選ぶ手段がチャート上のバーのみ。4件では困らないが100件では破綻し、一覧を持つ他20図種と揃わない |
| A24 | Minor | 一覧行の削除ボタン (✕) に title が無く、何が消えるか読み取れない。既定値が無かったので全21図種に効いていた |
| A25 | Nit | 移動操作の語が gantt だけ「上に/下に」、他は「上へ/下へ」 |

A22 は既存の単体テストでは捕まらない。パーサは宣言行を見て「B は無い」と言い、
mermaid は参照を見て「B は在る」と言う — **同じ「存在する」に二つの述語**が
使われている、このセッションで何度も出たアーキタイプ。実描画でしか差が出ないので
オラクルに `expectAbsent` / `expectPresent` を足し、旧実装で FAIL することを確認した。

## レビュアー自身の誤り (記録)

| 内容 | どう分かったか |
|---|---|
| K3 が「ボタンidの接頭辞が揃っていない」を指摘 | 接頭辞は図種ごとで設計どおり。存在しない規約を検査していた |
| K3 を「接尾辞=役割」に直しても外れ | 静的に数えると役割を持つ 51 個中 46 個が非該当。こちらも規約ではなかった。id は利用者に見えないので観点ごと落とし、エラー回復 (K6) に差し替えた |
| K2 が ↑↓ を「語のゆれ」として数えた | 記号はアイコンであって語ではない |
| R8 が「削除ボタンに辿り着けない」を4図種で報告 | 直前の手順で行の「編集」を押しており、一覧が編集画面に切り替わっていた。レビュアー側の順序の誤り |

**規約違反を数える前に、その規約が実在するかを確かめること。**

### 訂正 (2026-08-21)

上の「規約は元から無い」は**不正確**だった。ADR-022 (採択済み) が
アクションバーのボタンに `sel-<kind>-<action>` という規約を定めている。
ただしその適用範囲は `actionBarHtml` が生成するボタンに限られており、
同 ADR は「プレフィックスの `<kind>` 部分は**モジュール内で一意**であれば良い
(モジュール間は独立)」と明記している。

つまり正しくは「**範囲を限定した規約は存在し、モジュール間の独立は意図的な決定**」。
私は ADR を見ずに「規約が無い」と断定し、実装前の ADR 確認ゲートを飛ばしていた。
実行すべきは「規約が実在するかを推測で判断する」ことではなく、**ADR を先に読む**ことだった。

なお `sel-node-connect` と `block-edit-connect` の不揃いは `connectButtonHtml` 由来で
ADR-022 の範囲外。規約違反ではないが、規約の趣旨からは外れている。

## 有効性の確認 (変異注入)

| レビュアー | 壊した所 | 検出 | 復元後 |
|---|---|---|---|
| r7 K6 | 一覧削除で `pushHistory` を呼ばない | 20件 | 0件 |
| オラクル delete-cascade | participant 削除を旧実装 (行削除のみ) に戻す | 2件 | 0件 |

## 合否

`node tools/review/gate.js <repo>` → 終了コード 0。
ユニット 750 / 実描画 30ケース2本 / e2e 357 / 8観点すべて指摘0。

---

# 第3ラウンド: 残る観点 (2026-08-21)

8観点のうち、まだ見ていなかった **ワークフロー適合** を足した。

| レビュアー | 見るもの |
|---|---|
| r9-workflow | 1操作が差分をどれだけ汚すか。コメント行が消えないか、触っていない行が書き換わらないか、並び替えが行の中身を変えないか |
| r10-roundtrip | 保存した .mmd が本文と一致するか、開き直して戻るか、SVG 書き出しが中身を持つか |

想定は「mmd を Git 管理し、レビュー指摘のたびに小刻みに直す」使い方。ここで効くのは
機能の有無ではなく、GUI を1回通したときに差分がどれだけ汚れるか。動いてしまうので
これまでのレビュアーは全部素通りしていた。

## 見つかった欠陥

| # | 重大度 | 内容 |
|---|---|---|
| A26 | Nit | requirement で末尾の要素を消すと、区切りの空行だけが残りファイルが空行で終わる。テンプレートは21図種すべて末尾改行なしで揃っているので、削除だけが規約を破っていた |

## 有効性の確認 (変異注入)

| レビュアー | 壊した所 | 検出 | 復元後 |
|---|---|---|---|
| r9 W1 | 行削除がコメント行も落とす | 9件 | 0件 |
| r9 W4 | 並び替えでインデントを落とす | 1件 | 0件 |
| r10 | 保存時にコメント行を落とす | 18件 | 0件 |

## 観点の網羅状況

| # | 観点 | 見ているレビュアー |
|---|---|---|
| 1 | 操作コスト | r5 (手数の上限を数値で判定) |
| 2 | 発見可能性 | r7 K5 |
| 3 | 一貫性 | r7 K1/K2/K4 |
| 4 | フィードバック | r5 U2 |
| 5 | エラー回復 | r7 K6 (削除→Undo)、r1 |
| 6 | 学習曲線 | **未** — 自動判定に落とせていない (下記) |
| 7 | スケール耐性 | r8 (100要素) |
| 8 | ワークフロー適合 | r9 / r10 |

観点6 (学習曲線) は「初回10分で何ができるか / 熟達後どこまで速くなるか」であり、
機械が測れるのは代理指標だけになる。数値の裏付けが無い指摘は出さない方針なので、
**測れていないことを測れたことにしない**。r5 が熟達側 (最短手数) を、r7 K5 が初回側
(説明の有無) を部分的に代理している、という位置づけに留める。

---

# 第4ラウンド: 実務の名前を入れる (2026-08-21)

日本語の設計書で使う名前は素直な英数字ではない。「設計(詳細)」「配列[0]」「A#1」
「"引用"付き」— これらは mermaid の DSL では意味を持つ記号なので、そのまま本文に
置くと図が壊れるか、**入れた文字が黙って消える**。r11-specialchars で21図種に
10種類の実名を流し込み、実描画まで見た。

## 見つかった欠陥

| # | 重大度 | 内容 | 実描画での症状 |
|---|---|---|---|
| A27 | Major | mindmap: 形状記号を含むラベル | 「設計(詳細)」→ 図には**「詳細」だけ**。( が形状指定として食われ前半が捨てられる。エラーは出ない |
| A28 | Major | block: 引用符を含むラベル | parse 失敗で図が出ない |
| A29 | Minor | sequence: # を含むラベル | 「A#1」→「A」。# 以降が実体参照として食われる |
| A30 | Minor | エラー表示が読めない | 一文がペインの右端で切れる。コンテナに scale(zoom) がかかっており、文字も拡大率のまま出るため |

A27〜A29 は引用囲み / `#quot;` / `#35;` に逃がして解決した。読み戻しで実体を戻すので
往復する。記号を含まないラベルは囲まないので、差分は増えない。

## 直せないもの (mermaid 側の制限)

| 図種 | 制限 | 実測 |
|---|---|---|
| architecture-beta | ラベルは `[A-Za-z0-9_ ]` のみ | 日本語・ハイフン・括弧すべて不可。引用囲みも効かない |
| sequence | participant 別名に `;` 不可 | 生でも引用囲みでも parse 失敗 |

こちらでは直せない。ただし出るエラーは `Lexer error on line 3, column 25` で、
利用者は自分の入力のどの文字が悪いのか分からない。**直せないものを黙って壊れさせ
ない**ため、原因を日本語で名指しする診断 (`src/core/diagnose.js`) を足した。
推測はせず、規則に当てはまる入力が本文に実在するときだけ言う。

これに合わせて r11 の判定基準も変えた。問うのは「通るか」ではなく
**「原因を名指しできているか」**。診断を空にする変異を入れると11件を検出する。

## 単体テストが通っても意味が無かった例

SC-1〜SC-3 (mindmap の括弧・角括弧・波括弧) は、修正前から**通っていた**。
パーサが `[...]` の中身をそのまま返すので、モジュール単位では往復して見える。
図に出ないことは実描画でしか分からない。パーサと mermaid で「往復した」の述語が
違う、という同じアーキタイプ。

## 有効性の確認 (変異注入)

| レビュアー | 壊した所 | 検出 | 復元後 |
|---|---|---|---|
| r11 | 診断が何も言わない | 11件 | 0件 |

## ゲートの鮮度検査が働いた記録

変異を戻した直後にゲートを回したところ、10観点すべてが「ソースより古い」と判定され
不合格になった。結果ファイルの日付だけで判定を防いだ形で、この検査を入れた狙い
どおりに動いた。

---

# 第5ラウンド: 定時レビューの指摘を消化 (2026-08-21)

ヘビーユーザ視点の定時レビュー (UI-001〜UI-010) を棚卸しに載せ、推奨案で全件消化した。
UI-001 は Blocker だったのでレビュー中に即対応済み。

| 元 | 対応 |
|---|---|
| UI-002 | `?` キーでショートカット一覧。Open/Save の title に修飾キーを足して Undo/Redo と揃えた |
| UI-003 | 一覧が12行以上で絞り込み欄を自動表示。100要素で199行→11行に絞れることを実機で確認 |
| UI-004 | Ctrl+Shift+S / Export メニューで上書き先を指定。**既定の Save は従来どおり** |
| UI-005 | セクション削除の confirm を廃止し、巻き添え件数をボタンに出す方式 (block と同じ) に統一 |
| UI-006 | 保存後にステータスへ「保存: HH:MM」 |
| UI-007 | ラベル欄を打鍵ごとに反映 (150ms) + フォーカスとカーソル位置の復元 |
| UI-008 | Export の先頭に「Mermaid記法 (.mmd)」 |
| UI-009 | 削除後にステータスへ「N件削除 — Ctrl+Z で戻せます」 |
| UI-010 | 全21図種で「一覧 → 選択 → 編集」が同じ位置に出ることを r7 K7 で常設監視 |

## 実装中に自分で作った欠陥 (すべて修正済み)

| 内容 | どう分かったか |
|---|---|
| 保存トーストを `markSaved()` に入れた | markSaved は「保存した」ではなく「未保存判定の基準を更新する」関数で、起動時・ファイルを開いた時・図種切替でも呼ばれる。**保存していないのに「保存: 04:36」と表示**していた。実機スクリーンショットで発覚 |
| Save の既定をファイル選択ダイアログに変えた | ヘッドレスでダイアログが戻らず Save が完了しなくなった。1クリックで終わっていた操作をモーダルにするのは毎日使う側には改悪。上書きは明示指定時だけにした |
| ラベルの即時反映で履歴が2件積まれる | e2e (Undo の粒度) が落ちた。「1編集 = Undo 1回」はこのツールの規律。履歴に束ねモードを足し、欄を離れるときは保留中の合成 change も取り消す |
| フォーカス復元が連続入力の意図を上書き | e2e (追加後もフォームに留まる) が落ちた。復元するのは再描画でフォーカスが**失われた**ときだけに限定 |

4件のうち2件は e2e が、2件は実機の目視が捕まえた。**どちらか片方だけでは足りなかった。**

## ゲート

ユニット 787 / 実描画 30ケース2本 / e2e 357 / 12観点すべて指摘0 → 終了コード 0。

---

# 第6ラウンド: 他セッションのレビューを突き合わせ (2026-08-21)

別セッションの critic-master / critic-ux / probe-* から詳細なレビューを受け取った。
評価コミットは `e14dac7`(こちらの作業前)で、その後 master は `153ec1f` まで進んで
いる。**指摘が現在のコードで生きているかを1件ずつ実測してから**着手した。
解消済みを直したことにしない、生きているものを見逃さない、の両方が目的。

## 突き合わせの結果

| 指摘 | 現在の状態 |
|---|---|
| UI-001 Undo が逆行する | 解消済み (Ctrl+Z をアプリ履歴に一本化済み) |
| UI-002 履歴が1キーストローク単位 | 解消済み (打鍵の束ね済み) |
| UI-003 C4/Block に操作要素ゼロ | 解消済み (オーバーレイ実装済み) |
| UI-006 保存名が untitled | 解消済み |
| UI-007 追加タスクIDの衝突 | 解消済み (`nextTaskId`) |
| UI-012 ステータスが gantt 決め打ち | 解消済み |
| UI-013 ズーム引き継ぎ | 解消済み |
| UI-016 追加後にフォームが消える | 解消済み |
| UI-018 60要素でパネル4.5画面・検索なし | 解消済み (絞り込み) |
| UI-022 ショートカット一覧が無い | 解消済み (`?`) |
| probe-selection ID リネームで選択が迷子 | 解消済み (`followRename`) |
| **UI-005 block の幽霊ブロック** | **生きていた** |
| **UI-008 打鍵中に図が消える** | **生きていた** |
| **UI-011 非gantt で Ctrl+A が例外** | **生きていた** |
| **UI-017 離脱警告が無い** | **生きていた** |
| **UI-021 Ctrl+Shift+Z が Redo でない** | **生きていた** |
| **m-2 flowchart の ↑ が最頻形で無反応** | **生きていた** |
| **m-3 エッジ記法の網羅漏れ** | **生きていた (指摘より重い)** |
| **m-6 CRLF 混在** | **生きていた** |
| **probe-update kanban 同名カラム** | **生きていた** |

## 直したもの (A33〜A42)

| # | 内容 |
|---|---|
| A33 | kanban: カラムをラベル一致で探すため、同名カラムでは常に先頭側へカードが入る。行番号で一意に決めるようにし、UI の選択肢も value を行番号にした |
| A34 | block: `style` / `classDef` / `class` / `click` をトークン分解して幽霊ブロックにしていた。装飾行は一覧に出さず本文には残す |
| A35 | block: 菱形 `{"..."}` を知らず `c` と `Actuator` の2ブロックに割れていた |
| A36 | flowchart: `o--o` `x--x` `<-->` を知らず、`A o--o B` の左辺を `A o` という**存在しないノード**として登録していた |
| A37 | flowchart: 直上がエッジ行だと ↑ が無言で空振り。ノードをエッジ行にインラインで書くのが最も普通の書き方なので、実質 ↑ が常に死んでいた。エッジ行との入れ替えは構文上安全なので相手に含める。`subgraph`/`end` は越えない (属するグループが変わる) |
| A38 | 非 gantt で Ctrl+A が `parsed.tasks.map` で例外。コンソールにしか出ないので利用者には「押しても何も起きない」としか見えない。未対応ならその旨をステータスに出す |
| A39 | 構文を打っている途中は必ず一時的に不正になるのに、失敗のたびに図を赤いエラーテキストで置き換えていた。`section` と打つだけで図が消え、3ペインUIの目的が成り立たない。直前の図を残し、下端の帯で「表示しているのは直前の図」と伝える |
| A40 | 未保存のまま離脱しても警告が無い。保存がダウンロード操作しかないので習慣も付きにくい |
| A41 | `Ctrl+Shift+Z` が Undo の分岐に先に吸われて Undo になっていた。文字の大小ではなく `shiftKey` で分ける |
| A42 | CRLF 文書に挿入した行だけ復帰文字を持たず改行コードが混在。`matchEol` で元に合わせる |

## 他セッションの指摘のうち、扱いを変えたもの

- **m-3 は指摘より重かった。** 「実害は移動順が変わる程度」と書かれていたが、実際は
  `A o--o B` が `A o` という存在しないノードを一覧に出す。幽霊要素は削除の対象にも
  なるので、実害は「移動順」では済まない。
- **UI-001 / UI-002 (Undo) は既に解消済み。** ただし critic-ux の指摘した原因分析
  (ネイティブ undo が `input` を発火して履歴を汚す) は正しく、現在の実装はまさに
  その形で塞いである。

## 未対応 (今回は着手しない)

| 指摘 | 理由 |
|---|---|
| UI-009 図とエディタの双方向同期 | 機能追加。既存の欠陥ではない。価値は高いので次の反復で扱う |
| UI-014 GUI 操作が桁揃えを壊す | 書き戻しを「変更フィールドだけの置換」に変える大きな改修。R9 が差分の局所性を見ているので、悪化しないことは監視されている |
| UI-019 PNG が等倍 | 姉妹プロジェクトの前例 (scale=2) がある。ADR を起こしてから |
| probe-export maxTextSize 超過 | 未再現。2900タスク規模の確認に時間がかかるため、次の反復で検証してから判断する |
| m-1 / m-5 テストが恒真・grep ベース | テストの品質改善。r12-noop が同種の穴を機械的に見るようになったので、優先度は下げた |

---

# PL 診断 (2026-08-21) — プランと現実の乖離

批判レビューとは別に、**プランが現実を写しているか**を診断した。結果は REPLAN。

## 検出した乖離

| 内容 | 実測 |
|---|---|
| E5「一覧の絞り込みは見送り」 | **実装済み** (`ma-list-filter`)。見送り欄に残っていたのは記述誤り |
| E区分が3ラウンド以上再検証されていない | E1/E2/E4/E6 が塩漬け。判断の妥当性を一度も確かめ直していない |
| 反映経路が閉じている | `integration/all` に master 未反映が2コミット、open PR 0件 |
| ベロシティ | **データ不足**。時間軸の記録が無い。測れたのは「観点を1つ足すたびに必ず新規発見が出ている」という事実のみ |

最後の1点が重要。**「12観点すべて指摘0」は収束の証拠ではなく、観点の数の関数**である。
飽和は「新しい観点を足しても出ない」ことでしか示せない。マイルストーン M3 に置いた。

## この診断で動かしたもの

- E区分を全項目**実測し直し**、実測値と確認日を付けた (E5 は削除して B9 へ移動)
- 塩漬け4件を強制的に振り分け: E1 → M2 / E2 → 見送り継続 (根拠を実測で更新) /
  E3 → バックログ送り / E6 → ADR ドラフト起票
- D1〜D5 (第2回レビューの残り) を消化して B16〜B20 へ

## 判断を代行しなかったもの

E4 (class/er/state の move) は「実装する / 削除する / 現状維持」で作るものが変わる。
価値判断の分岐なので STOP_AND_DECIDE として質問に回した。

---

# 第7ラウンド: M3 観点の飽和確認 (2026-08-21)

PL 診断で置いたマイルストーン M3 の検証。**「12観点すべて指摘0」は収束の証拠ではなく
観点の数の関数**なので、新しい観点を2つ足して出るかどうかを見た。

**結果: 出た。飽和していない。**

| 観点 | 見るもの | 結果 |
|---|---|---|
| r13-unknown-syntax | 知らない行を「要素」として捏造していないか | 本物2件 (timeline) |
| r14-boundary | 要素が 0個 / 1個 のときに壊れないか | 本物2件 (mindmap) |

## 見つかった欠陥

| # | 内容 |
|---|---|
| A47 | timeline が `accTitle:` / `accDescr:` を「期間」として拾う。mermaid はこれを読み上げ用メタデータとして扱い図に描かないので、一覧に幽霊が出る。幽霊の ✕ を押せば読み上げ用の情報が消える |
| A48 | mindmap の `deleteNode` に**存在しない id** を渡すとルートガードのループが一度も当たらず、行番号だけで削除が進む。ルートの行を渡せば子ごと全部消える (実測: 本文が `mindmap` だけになった) |
| A49 | 同じガードが根拠より広く効いていて、子が1つも無いルートまで消せない。ガードの理由は「子が孤立して mermaid が落ちる」なので、子が無いなら拒否する理由が無い (空の mindmap を mermaid が受け付けることを実測) |

## レビュアーの前提が誤っていた記録

r13 は最初、**21件の指摘を出して全部誤りだった**。

「装飾行 (`style` / `classDef` / `accTitle` …) はどの図種でも共通に有効」という前提で
「足しても要素の集合が変わらないこと」を判定にしていた。実描画で確かめると:

- kanban は `style X fill:#f9f` を**本当にカラムとして描く** → パネルは正しい
- mindmap / timeline / block はその行を入れると **parse error** → 文書自体が不正

装飾構文は図種ごとに有効/無効が違う。判定を mermaid に接地させ直した:

1. mermaid が parse できない文書は対象外 (利用者が持ち得ない文書)
2. 描画に**その行の文字が出ている**なら、一覧に出るのが正しい
3. 描画に出ていないのに一覧に出るなら、それが捏造

これで2件だけが残り、どちらも本物だった。**規約違反を数える前に規約の実在を確かめる**
のと同じ話が、また出た (K3 の接頭辞・接尾辞、K7 の測定タイミングに次いで4回目)。

## 自分で恒真テストを書いた記録

A47 の最初のテストは `e.label || e.id` を見ていた。timeline の period に `label` は
無いので `e.id` (= `__p_0`) と比較しており、**修正前から通っていた**。
R12 (無言の空振り) で機械的に潰している欠陥を自分で書いた。実フィールド
(`period` / `events`) に直したら RED になった。

## M3 の判定

**未達。** 観点を2つ足して4件出たので、飽和は示せていない。
次の反復でさらに2観点を足し、**どちらも0件**になったときに初めて M3 を満たす。

---

# M2 完了: app.js から gantt を出す (2026-08-21)

PL 診断で3サイクル「1行も動いていない」と指摘された塩漬けタスク。振る舞い不変で
結果が見えないため一度も選ばれていなかった。

## 結果

| | 前 | 後 |
|---|---|---|
| `src/app.js` | 3073行 | **2232行** (目標 2500行以下) |
| `src/modules/gantt.js` | 941行 | 1749行 |
| 図種による場合分け | `buildOverlay` 2箇所 / `renderProps` 1箇所 | **0** |

gantt だけが app.js に残っていた理由は、クロージャ変数を直接触っていたから。実測すると:

- `buildOverlay` の外部依存は `overlayEl` の**1つだけ**。3引数契約に揃えれば移せた
- `renderProps` は757行・外部依存23件に見えたが、内訳は gantt モジュール自身の関数
  (gantt.js 内ではローカル)、ローカル変数の誤検出、`window.MA.modules.gantt` の自己参照で、
  **実質の外部依存は6つ**だった
- そのうち19箇所が**同一形の同期ブロック** (`mmdText = X; suppressSync = true;
  editorEl.value = mmdText; suppressSync = false; syncLineNumbers(); scheduleRefresh();`)
  で、他モジュールの `ctx.setMmdText` + `ctx.onUpdate` に1対1で対応した

## 自分の変換ミスを e2e が捕まえた

ループ内の代入を一時変数に置き換えるとき、**毎回 `ctx.getMmdText()` から読み直す**形に
してしまった。一括削除で最後の1件しか消えない。

```js
for (var di = 0; di < sorted.length; di++) {
  var _t6 = deleteTask(ctx.getMmdText(), sorted[di].line);  // ← 毎回もとの本文から
}
ctx.setMmdText(_t6);
```

ユニット812件・実描画30ケース・14観点はすべて通っていた。捕まえたのは e2e の
「シナリオ6: 不要なタスクを一括削除」1本だけ。**一括操作は e2e にしか出ない**という
記録として残す。同型が3箇所 (一括ステータス / 一括日付シフト / 一括削除) あり、
すべて積み上げ形に直した。

## 検証

- ユニット 812 / 実描画 30ケース2本 / e2e 357 / 14観点すべて指摘0
- 実機で: パネル描画 (入力欄10・一覧4行)、タスク追加、追加後のフォーム維持、
  ラベル変更、削除と Undo 復帰、ページエラー0件
- 移設前後で同じ測定をして差が無いことも確認済み

---

# 第8ラウンド: 観点15 と、残りの UI 指摘 (2026-08-21)

## 新観点 r15-state-carryover

「文書が入れ替わったのに、前の文書の状態が残っていないか」。

図種を切り替えると本文はひな形に置き換わる。そのとき **残ってよい状態**(ズーム、
Undo 履歴 — 切替そのものを取り消せる)と **残ってはいけない状態**(前の文書に紐づく
入力値、選択、絞り込み)がある。

| 見たもの | 結果 |
|---|---|
| 一覧の絞り込み | 行数が閾値未満になると欄ごと消えるので、古い絞り込みが残らない |
| **追加フォームの入力** | **gantt だけ残った** — 本文はひな形に置き換わっているのに、名前と日付が前の図のまま |
| Undo 履歴 | 図種切替を取り消せる(設計どおり) |
| 選択 | 切替で消える |

gantt だけ該当したのは、他の図種がフォームを毎回作り直すのに対し、gantt は追加フォームの
状態をモジュール側で保持しているため。M2 の移設で addForm を gantt.js に移したことで、
**同じ問題がモジュールの寿命に紐づくようになった**とも言える。

対処はモジュール契約に `resetTransientState()` を足し、文書が入れ替わる2箇所
(図種切替 / ファイルを開く)で呼ぶ形にした。実装していないモジュールは何もしなくてよい。

## 調べて「欠陥ではない」と分かったもの

「図種を切り替えて gantt に戻すとオーバーレイのバーが1本しか出ない」を追った結果、
**本文自体が1タスク**だった。`gantt.template()` は1タスクで、起動時のサンプル(4タスク)
とは別物。切替時は template() が読み込まれるので、バー1本は正しい。

M2 の移設前後で同じ測定をしていたので回帰でないことは分かっていたが、原因まで特定した。
**「起動時のサンプル ≠ モジュールのひな形」は app.js のコメントに書かれた意図的な設計**で、
未保存判定が起動直後に誤発火しないようにするためのもの。

## 消化した UI 指摘

| 元 | 対応 |
|---|---|
| UI-203 | 保存の入口が4つ(Save / Ctrl+S / Ctrl+Shift+S / Export の2項目)に散っていた。Save を分割ボタンにし、Export から `.mmd` 系を外した |
| UI-204 | `? ショートカット` が画面最下部の二次色で気づきにくい。初回起動時のみ5秒アクセント色にする |

## M3' の進捗

達成条件は「新観点3つ連続で0件」。

| 観点 | 新規発見 |
|---|---|
| r13 | 2件 |
| r14 | 2件 |
| **r15** | **1件** |

3つ足して合計5件。**まだ飽和していない。** 1観点あたりの発見数は 2.2 → 2.0 → 1.0 と
下がっているので、収束の方向ではあるが、条件は満たしていない。

---

# 第9ラウンド: 観点16 と、塩漬けの強制振り分け (2026-08-21)

## 新観点 r16-count-parity

「パネルが数える要素数と、mermaid が実際に描く数がずれていないか」。

これまで個別に見つけた欠陥 — sequence の participant が図に残る / block の style 幽霊 /
timeline の accTitle 幽霊 — は、**数の一致という一本の物差しで見れば同じもの**だった。
ステータスバーは「要素: N」を常時出しているので、この N が嘘なら常時嘘をつく。

**結果: 0件。M3'(新観点3つ連続で0件)のカウント1本目。**

有効性は変異注入で確認した。timeline の acc ガードを外し、ひな形に `accTitle:` を
混ぜた状態で実行すると `[accTitle] (1/7)` を検出し、ガードを戻すと0件に戻る。

### レビュアーの前提が誤っていた記録 (5回目)

最初 xychart で1件出た。系列 `__bar_0` / `__line_1` が「図に出ない」という判定。
実際は **mermaid が xychart の系列名を描かない**ので、照合できる文字が無いだけだった。
観測できないことを根拠にしてはいけない。自動採番の id を照合対象から外した。

## 塩漬けの強制振り分け

PL 診断で3サイクル滞留していた2件を、予告どおり処理した。

| 項目 | 処理 | 復活条件 |
|---|---|---|
| E4 class/er/state の move | **G区分(バックログ)へ**。C(現状維持)として扱う | A(実装)/B(削除)を選んだ時点、または `_is*Line` を直す作業が別の理由で発生した時点 |
| E3 gantt config競合 | **H区分(削除リスト)へ** | 実機で再現手順が1つでも見つかるか、遭遇報告があった時点 |

棚卸しに **G区分(バックログ)と H区分(削除リスト)を新設**した。「黙って消さない」を
構造で守るため、消したものは理由と復活条件つきで残る。

## M3' の進捗

| 観点 | 新規発見 | カウント |
|---|---|---|
| r13 | 2件 | リセット |
| r14 | 2件 | リセット |
| r15 | 1件 | リセット |
| **r16** | **0件** | **1本目** |

あと2観点連続で0件なら M3' を満たす。

---

# 第10ラウンド: 観点17 (Undo/Redo の往復整合) (2026-08-21)

これまで Undo は「1編集 = 1回で戻れるか」(r5) と「削除を戻せるか」(r7 K6) しか見て
いなかった。どちらも**1手**の検査で、複数手を戻して進める往復は誰も見ていなかった。

往復が壊れる形は静かで、しかも取り返しがつかない:

- 戻したあとに進めると別の状態になる → 作業が失われたことに後から気づく
- 途中の状態が飛ばされる → 「2回戻したのに3手前にいる」
- 進めなくなる → 戻しすぎたときに復旧できない

判定は「N手の操作を記録し、N回戻して各段が一致し、N回進めて各段が一致すること」。
実機の履歴を使うので、モジュール単体では見えない app 側の取りこぼしも出る。

**結果: 0件。M3' のカウント2本目。**

## 空振りしていないことの確認

0件は「壊れていない」と「検査が空振り」の両方で出るので、変異を2種類入れた。

| 変異 | 検出 |
|---|---|
| `redo()` を無効化 | 2件 (gantt / flowchart とも「進める」で不一致) |
| 履歴を1つおきにしか積まない | 4件 (「戻す」「進める」の両方で不一致) |
| (復元後) | 0件 |

検査自体が空振りしないよう、**操作が本当に本文を変えたか**を先に確かめる段
(U0 前提) も入れてある。変えていなければ「検査が空振りしている」と報告する。

## M3' の進捗

| 観点 | 新規発見 | カウント |
|---|---|---|
| r16 | 0件 | 1本目 |
| **r17** | **0件** | **2本目** |

あと1観点連続で0件なら M3'(観点の飽和)を満たす。

---

# 第11ラウンド: 観点18 (キーボード完結性) (2026-08-21)

「マウスに触らずに一連の作業ができるか」。ペルソナは1日100回の操作をキーボード中心で
回すので、マウスへ持ち替える回数がそのまま摩擦になる。これまでのレビュアーは
「その操作ができるか」は見てきたが、**どの入力装置で到達できるか**は誰も見ていなかった。

クリック座標を一切使わず、キーボードイベントだけで進める:
矢印キーで選択 → Tab で名前の欄へ → 打鍵して確定 → Ctrl+S で保存。

## 見つかった欠陥 (A53)

**classDiagram / erDiagram には名前を変える手段が無かった。**

Tab を30回押しても入力欄に届かないので調べたところ、選択時のパネルは
`クラス: Animal` という**読み取り専用の文字**を出すだけで、入力欄が0個だった (実測)。
`updateClassName` / `updateEntityName` に相当する関数も存在しない。

他の図種 (flowchart / block / sequence / c4 / state / requirement) は ID 欄を持ち、
変更すると参照側も追従する。class / er だけが取り残されていた。リネームは1日に何度も
通る操作なので、テキストを直接触るしかないのは GUI ツールとして成立していない。

参照の追従が要るのは削除と同じ理由で、宣言だけ変えると関係行が古い名前を指したまま
残り、mermaid は参照だけで要素を作るので**幽霊クラスが生える**。既にある名前への変更は
拒否する (mermaid は2つを黙って統合するので、利用者からは「クラスが消えた」に見える)。

## レビュアーの前提が誤っていた記録 (6回目)

直したあとも R18 は同じ指摘を出し続けた。判定が `-label$` 決め打ちで、新設した欄が
`sel-class-name` だったため。図種によって label / name / id と呼び名が違うので、
「その要素を名付ける欄なら何でもよい」に広げた。

## M3' の進捗

| 観点 | 新規発見 | カウント |
|---|---|---|
| r16 | 0件 | 1本目 |
| r17 | 0件 | 2本目 |
| **r18** | **1件** | **リセット** |

**M3' は未達に戻った。** 3本目で1件出たのでカウントは0に戻る。
1観点あたりの発見数は 2.2 → 2.0 → 1.0 → 0 → 0 → 1.0 で、**0が2回続いても3回目に出た**。
飽和していないという判断は変わらない。

---

# 第12ラウンド: W1 で最も重い取りこぼしが出た (2026-08-21)

PL 診断の W1「更新関数のシグネチャ統一」に着手したところ、**前提が3回ひっくり返った**。

## 1. 統一入口は最初から存在した

ADR ドラフトには「形が3通りあって呼び出し側が覚えられない」と書いたが、実際は
`operations.{add,update,delete,moveUp,moveDown}` という統一入口を **20/21 のモジュールが
最初から持っていた**。契約はあり、使われていなかっただけ。

持っていないのは gantt 1つ。しかも最も使う図種だった。

## 2. レビュアーが gantt を静かに素通りしていた (A54)

gantt に `operations` を足し、r12 を関数名の表から契約ベースへ書き換えたあと、
念のため gantt の統一入口を無効化する変異を入れたら **0件のままだった**。

原因: `gantt.parse` だけ `elements` を返さず `{title, dateFormat, axisFormat,
sections, tasks}` を返していた。並行レビュー機構は全体が `parse().elements` を
前提にしているので、**gantt は18観点すべてから静かに外れていた**。

「表から漏れたモジュールは検査対象から静かに外れる」と ADR ドラフトに書いた懸念が、
関数名の表ではなく**パースの返り値の形**で起きていた。最も使う図種が、最も多くの
検査から外れていた。

`elements` を返すようにしたうえで変異を入れ直すと、r12 と r14 が両方とも検出した。
`tasks` / `sections` は既存の呼び出しが全部それを見ているのでそのまま残す。

## 3. 契約に繋がっていない関数があった (A55)

r12 を契約ベースにしたら classDiagram が「どの field でも変わらない」と出た。
前ラウンドで足した `updateClassName` を `operations.update` に繋いでいなかった。
er も同じ。**関数を足しても入口に繋がなければ、契約から見れば存在しない。**

## レビュアーの前提が誤っていた記録 (7回目)

r12 を契約ベースにした直後、block / gitGraph / timeline で3件出た。統一入口の
第5引数 `opts`(要素の kind/id)を渡していなかったため。実装は正しく、検査の
呼び方が間違っていた。

## この回で分かったこと

**「18観点すべて指摘0」は、検査対象に入っている範囲でしか意味を持たない。**
gantt が外れていた期間、その0は gantt について何も言っていなかった。
観点を増やすより先に、**各観点が本当に全モジュールを見ているか**を確かめる必要がある。

---

# 第13ラウンド: 検査の網羅を検査する (2026-08-21)

前ラウンドの結論「観点を増やすより先に、各観点が本当に全モジュールを見ているかを
確かめる必要がある」を実行した。

## r19-coverage: 検査の網羅そのものを検査する

レビュアーは前提を満たさないモジュールを黙って飛ばす。

```
if (!mod || !mod.template || !mod.parse) return;
if (!before || before.length < 2) return;
if (!del) return;              // 削除関数が見つからない
if (!up || !down) return;      // 移動関数が見つからない
```

どれも正当な理由だが、**飛ばしたことが記録に残らない**。前提を並べて、どのモジュールが
どの前提で外れるかを一覧にする。

## 見つかった最大の穴: r6 が15モジュールを飛ばしていた

`moveNodeUp` / `moveTaskUp` / … という関数名の表を持っており、その名前を持たない
**15モジュールを黙って飛ばしていた**。検査していたのは6だけ。

全21モジュールが `operations.moveUp / moveDown` を持っているので、契約ベースに
書き換えた。網羅は 6 → 21 に広がった。

広げた直後に pie で変異を入れたら検出した (以前は対象外だったので何を壊しても0件だった)。

## r19 自身も同じ誤りを2回した

| 誤り | 内容 |
|---|---|
| 1回目 | 任意の前提を**関数名の表**で見ていた。この観点が問題にしている形そのもの。契約で見るよう直したら、欠落が 17件 → 4件に減った |
| 2回目 | 契約の第5引数 `opts` を渡していなかった。block は「1行に複数のブロックがあるのでどれを消すか決まらない」と**正しく拒否**しており、検査の呼び方の問題を製品の欠陥として報告しかけた |

残る1件 (gantt の moveUp) は、ひな形がセクションにタスク1つで**動かす相手がいない**
だけ。欠陥ではないので、そう区別できるよう判定を直した。

## 数字

| | 前 | 後 |
|---|---|---|
| r6 の検査対象 | 6モジュール | **21モジュール** |
| r19 が報告する欠落 | 17件 (表ベース) | 1件 (契約ベース・構造上の理由) |
| 観点数 | 18 | 19 |

**「観点を増やす」より「今ある観点の網羅を確かめる」方が、この回は効いた。**
r6 だけで検査対象が3.5倍になっている。

---

# 第14ラウンド: 表を全部消したら、UI 経路だけ直していた欠陥が2件出た (2026-08-21)

前ラウンドの続きで、残る「関数名の表」を持つレビュアー (r1 / r2 / r9 / r14) を
契約ベースに寄せた。

## 網羅の変化

| レビュアー | 前 | 後 |
|---|---|---|
| r1 破壊検査 | **14/21** (classDiagram / erDiagram / journey / requirementDiagram / sankeyBeta / timeline / xychartBeta を素通り) | 21/21 |
| r2 削除 | 19/21 (c4 / gitGraph は `deleteLine` で表に無い) | 21/21 |
| r9 ワークフロー | 19/21 | 21/21 |
| r14 境界 | 19/21 | 21/21 |
| r6 移動 (前ラウンド) | 6/21 | 21/21 |

## 見つかった欠陥

| # | 内容 |
|---|---|
| A56 | flowchart の `operations.update` が「行にエッジ記号があればエッジ」と判定していた。`A[Start] --> B{Decision}` はノードの宣言とエッジが同じ行にあるので、A のラベルを変えようとすると**エッジのラベル**が付く (`A --> \|新ラベル\| B`)。`updateNode` は id を受け取れるのに入口が渡していなかった |
| A57 | `state` と `flowchart` の `operations.delete` が単なる `deleteLine` で、A4 / A10 で直した **id 認識の削除を使っていなかった**。宣言行だけ消えて参照が残るので、mermaid は参照だけで要素を作り、一覧から消えても図には残る |

**A57 は「同じ欠陥を UI 経路だけ直して契約経路を忘れる」形。** A4 (flowchart) と
A10 (state) で UI からの削除は直したが、`operations.delete` は古いままだった。
契約経路を誰も通っていなかったので、誰も気づかなかった。

## レビュアーの前提が誤っていた記録 (8・9回目)

| 誤り | 内容 |
|---|---|
| 8回目 | r1 が `kind` を書き戻していた。`kind` はメタ情報で編集値ではないので、カラム名が "column" になる。4モジュールで偽陽性 |
| 9回目 | r2 が sankey で「押した要素が残る」と報告。sankey のノードは**流れの両端から導出される**ので、1行消しても他の行から参照されていれば残るのが正しい |

## この2ラウンドで分かったこと

**関数名の表は、検査の網羅を静かに削る。**

| | 表ベース | 契約ベース |
|---|---|---|
| r1 | 14/21 | 21/21 |
| r6 | 6/21 | 21/21 |
| r2 / r9 / r14 | 19/21 | 21/21 |

表を消して契約に寄せただけで、これまで**一度も検査されていなかった組み合わせ**から
本物の欠陥が3件 (A54 / A56 / A57) 出た。観点を増やすのではなく、既にある観点を
全モジュールに当てただけで出ている。

---

# 第15ラウンド — 網羅率を測る (X1〜X3・20観点目)

前ラウンドで「表ベースを契約ベースに直すと検査範囲が広がる」ことは分かったが、
**どのレビュアーがどれだけ見ているかは誰も測っていなかった**。
そこで共通土台 (`tools/review/lib.js`) に計測を仕込み、全レビュアーに
「検査した図種 X/21」を自動で報告させた。

## X1: 全レビュアーの網羅率 (実測)

| レビュアー | 観点 | 計測前 | 修正後 | 備考 |
|---|---|---|---|---|
| r1-destructive | 破壊しないこと | 21/21 | 21/21 | 前ラウンドで契約化済み |
| r2-delete | 削除の巻き添え | 21/21 | 21/21 | 同上 |
| r3-render | 実描画 | (計測漏れ) | 10/21 | `connect` を持つ図種のみ。仕様上の上限 |
| r4-ui | UI の一貫性 | 21/21 | 21/21 | |
| r5-user | 利用者の手順 | 6/21 | 6/21 | 実機操作。図種ごとに手順が違うため全種は非現実的 |
| r6-move | 並び替え | 21/21 | 21/21 | |
| r7-consistency | 用語と挙動の一貫性 | 21/21 | 21/21 | |
| r8-scale | スケール耐性 | 4/21 | 4/21 | 大量データを作れる図種のみ |
| r9-workflow | ワークフロー適合 | 21/21 | 21/21 | |
| r10-roundtrip | 保存と開き直し | 6/21 | 6/21 | 実機ダウンロード。代表図種 |
| **r11-specialchars** | **特殊文字** | **6/21** | **18/21** | **表ベース → 契約ベース。47件の指摘が出た** |
| r12-noop | 空振り検出 | 21/21 | 21/21 | |
| r13-unknown-syntax | 未知構文の保全 | 21/21 | 21/21 | |
| r14-boundary | 境界値 | 21/21 | 21/21 | |
| r15-state-carryover | 状態の持ち越し | 5/21 | 5/21 | 追加フォームを持つ図種のみ |
| r16-count-parity | 件数の一致 | (計測漏れ) | 21/21 | |
| r17-undo-redo | Undo/Redo の往復 | 2/21 | 2/21 | 実機3手操作。代表図種 |
| r18-keyboard-only | キーボード完結 | 4/21 | 4/21 | 実機 Tab 走査。代表図種 |
| r19-coverage | 網羅の前提条件 | 21/21 | 21/21 | |
| **r20-unicode-names** | **日本語名の非対称** | — | **19/21** | **今回追加 (20観点目)** |

計測漏れが3件 (r3 / r11 / r16) あった。`elementsOf` を経由しないレビュアーは
自動計測に乗らないので、`markExamined` を明示的に呼ぶよう直した。

## X2: 契約ベースへの変換で出た欠陥

r11 を関数名の表 (`FIELD_FNS` / `VALUE_FNS` / `ID_FNS`) から
`operations.update` に寄せた。**観点は1つも増やしていない**が 47 件の指摘が出た。

| # | 内容 | 重さ |
|---|---|---|
| A58 | `requirement` の更新が、渡された field をそのまま `field: value` として本文に書き込んでいた。パネルに無い field 名 (label / title など) が一度来ると mermaid が知らないキーが入り、**その図は以後 parse を通らなくなる** | 図が壊れる |
| A59 | `erDiagram` の parser がエンティティ名を `[A-Za-z_][A-Za-z0-9_-]*` で拾っていた。`顧客 \|\|--o{ 注文` は **mermaid が正しく描くのに一覧にも重ね合わせにも1件も出ない**。日本語で書くと GUI が図に反応しない | 日本語で使えない |
| A60 | `state` の `operations.update` が `opts.kind` ではなく行に `-->` があるかで分岐。状態は遷移行で宣言されるのが普通なので、**状態を選んでラベルを変えると矢印のラベルが書き換わっていた** | 別の要素が壊れる |
| A61 | `classDiagram` のラベル欄がクラス名 (識別子) そのものを書き換えていた。`設計(詳細)` のような実務の名前を入れると図が壊れる。mermaid の表示ラベル `class Animal["..."]` に対応した | 日本語で使えない |
| A62 | `gitGraph` / `journey` / `kanban` / `timeline` が field を見ずに `opts.kind` だけで分岐。**どんな field 名でも名前が書き換わる** (6図種24通り) | 契約違反 |
| A63 | `block` は `opts.blockId`、`requirement` は `opts.elementName` / `opts.oldName` という独自キーを要求。契約どおり `opts.id` で呼ぶと黙って空振り | 契約違反 |
| A64 | `requirement` の `element` ブロックが requirement 専用キー (`text:`) を受けていた。書くと parse が落ちる | 図が壊れる |

いずれも「UI 経路は直っているが契約経路が古い」か「表に載っていないので
一度も検査されていなかった」形。A57 に続き、同じ archetype が7件出た。

## mermaid 側の制限として名指ししたもの

こちらでは直せないものは `diagnose.js` で原因を告げる。今回の追加:

| 図種 | 制限 |
|---|---|
| gitGraph | ブランチ名に全角文字が使えない |
| requirementDiagram | 要求名・要素名に全角文字が使えない (表示は `text:` に書く) |
| pie / quadrant / packet / radar / requirement | ラベルに `"` を含められない (`&quot;` も文字として出る) |
| kanban | 列名に `( ) [ ] { }` が使えない (カード記法と衝突) |
| journey / timeline | section 名に `: ; #` が使えない |

`er` / `state` / `class` は `&quot;` がそのまま `"` として描かれるので**逃がせる**
(v11.13 実測)。逃がせるものは逃がし、逃がせないものだけ告げる。

## 描けたのに落ちている場合を黙って通さない (GUI 変更)

mermaid は必ず例外を投げるわけではない。kanban の列名に括弧を入れると
parse は通り図も出るが、**括弧の中だけが消える**。利用者から見ると
「入れたはずの文字が図に無い」だけで、原因を知る手掛かりがどこにも無かった。

描画に成功した場合も `diagnose` を引き、名指しできる原因があれば
警告帯 (琥珀色) を出すようにした。構文エラーの帯 (赤) とは色で区別する。
21図種のひな形で誤報が出ないことを e2e で固定 (`render-warning.spec.js` 4件)。
実機スクリーンショットで確認済み。

## X3: 網羅率の下限をゲートに入れた

`tools/review/coverage-floor.json` に現状の実測値を下限として記録し、
`gate.js` に `COV` 項目を追加した。下回ると FAIL する。

変異注入で空振りでないことを確認:
r11 の網羅率を 18 → 6 に戻すと `FAIL COV r11-specialchars: 6/21 (下限 18)`。

**「指摘0件」は検査した範囲の外では何も意味しない。**
観点を増やす前に、見ている範囲が縮んでいないことを機械で押さえる。

## レビュアーの前提が誤っていた記録 (10回目)

| 誤り | 内容 |
|---|---|
| 10回目 | 契約化した r11 / r20 が、**mermaid 自身が拒む本文に対して**「一覧に出ない」と報告していた。mermaid が真としないものをこちらが偽としても欠陥ではない。描画の可否を先に判定し、mermaid が受け付けた本文にだけ一覧の追随を問うよう順序を直した |

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 848 | 889 |
| e2e | 357 | 361 |
| 実描画オラクル | 30 | 30 |
| 観点 | 19 | 20 |
| 指摘 | 0 (ただし r11 は 6/21 しか見ていなかった) | 0 (r11 は 18/21) |

---

# 第16ラウンド — 実機レビュアーの範囲を広げる

前ラウンドで網羅率を測ったところ、実機を操作するレビュアーが軒並み低かった。
特に **r17 (Undo/Redo) が 2/21**。取り返しのつかない操作なのに 19 図種が未検査
だった。「実機操作は図種ごとに手順が違うから」を理由に据えていたが、
**検査の中身は図種に依存していなかった**。理由の方が間違っていた。

## r17 Undo/Redo: 2/21 → 19/21

UI 駆動の2図種は残したまま、契約 (`operations.*`) で本文を作って
エディタ経由で流し込む走査を足した。「戻して進めたら元に帰る」は
図種に依らない性質なので、契約で駆動できる。

3手 (ラベル変更 ×2 → 削除) を記録し、3回戻して各段が一致し、
3回進めて各段が一致することを全図種で確認。**指摘0件**。
gitGraph / xychart-beta は3手を作れないので未検査として記録した。

## r18 キーボード完結: 4/21 → 10/21 (欠陥1件)

一覧に4図種を直書きしていただけで、検査の中身は図種に依存していなかった。
全21図種に広げた。重ね合わせを持たない11図種 (mermaid が DSL の id を
SVG に出さない図種) は**既知の制限**なので欠陥としては出さず、
検査から外れたことを記録する。選べていた図種が選べなくなれば網羅率の下限で FAIL する。

| # | 内容 |
|---|---|
| A66 | `state` のパネルに ID 欄 (`sel-state-id`) が出ているのに、**イベントが1つも繋がっていなかった**。打鍵しても本文は変わらず、エラーも出ない |

**欄があるのに効かないのは、欄が無いより悪い。** 効いたと思って先へ進んでしまう。
他の図種 (flowchart / block / sequence / c4 / requirement / class / er) は
ID 欄を持ち参照も追従するので、state だけが取り残されていた。

`updateStateId` を追加。遷移の両端点・別名宣言の `as` 側・コンポジット宣言・
fork/join 宣言・note の宛先を追従させ、空 / 重複 / `[*]` は拒否する。
拒否したときは欄を戻すだけでなく理由を告げる (黙って戻すと「効かない欄」に見える)。

実描画オラクルに `state-id-rename` / `state-id-rename-alias` を追加し、
**Idle が1文字も残らないこと**を描画側で固定した。
実機スクリーンショットで、本文3行・図・選択・パネルすべてが追随することを確認。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 889 | 902 |
| e2e | 361 | 365 |
| 実描画オラクル | 30 | 34 |
| 検査した図種の延べ数 | 292 | 328 |

## 残っている低い網羅率と、その理由

| レビュアー | 網羅 | 理由 |
|---|---|---|
| r8-scale | 4/21 | 大量データを機械的に作れる図種が4つしかない (gantt / flowchart / class / sequence) |
| r15-state-carryover | 5/21 | 追加フォームを持つ図種のみ。持たない図種には持ち越す状態が無い |
| r5-user / r10-roundtrip | 6/21 | 実機の保存・開き直し。図種ごとの操作差が本質的にある |
| r3-render | 10/21 | `connect` を持つ図種のみ |
| r18-keyboard-only | 10/21 | 重ね合わせを持たない11図種は既知の制限 |
| r11 / r20 | 18〜19/21 | ラベルの概念が無い図種 (gitGraph / sankey / xychart) |

**これらは「まだ測れていない」であって「問題が無い」ではない。**
網羅率を上げる余地として記録に残す。下限は `coverage-floor.json` で固定した。

---

# 第17ラウンド — ヘビーユーザ視点の批判レビュー (UI-001〜UI-008)

「組込みソフトウェアアーキテクト、キーボード中心、数十〜数百枚を Git 管理、
毎日100回の操作の摩擦を重視」というペルソナで、実機を計測した。
形容詞で終わる指摘を禁止し、すべて数で示した。

## 実測して出た摩擦

| ID | 重大度 | 観点 | 内容 | 実測 |
|---|---|---|---|---|
| UI-001 | Blocker | スケール耐性 | 500エッジで図が描けなくなる | 300要素 OK / 500要素 OK / 800要素 (20,483文字) Error |
| UI-002 | Major | 操作コスト | Delete キーが Gantt でしか効かない | 20図種で削除ごとにマウス往復1回 |
| UI-003 | Major | 一貫性 | 13個のショートカットのうち3個が「Gantt のみ」 | 覚えた操作の 23% が図種によって死ぬ |
| UI-004 | Minor | 発見可能性 | 追加フォームがパネルに収まらない | 8/21図種。C4Context は追加ボタン1個が画面外 (超過 264px) |
| UI-005 | Major | フィードバック | 原因を名指しできない場合も「構文エラー」と断定 | 本文に構文誤りは1つも無い |
| UI-006 | Minor | エラー回復 | Gantt 以外では「戻せる」ことが画面に出ない | ✕ 削除後の status-info が「要素: 4 | 関連: 4」のまま |
| UI-007 | Minor | ワークフロー適合 | 保存したファイルが改行で終わらない | 末尾1行の追加が2行差分になる |
| UI-008 | Major | 学習曲線 | 追加操作にキーボードから入る手段が無い | 候補7キーすべて `document.activeElement` が body のまま |

## 直したもの

| ID | 直し方 |
|---|---|
| UI-001 | `maxEdges: 100000` を設定に追加。`maxTextSize` は上げてあったが `maxEdges` は既定の500のままだった。**800要素 → OK、1500要素 → OK** (実測)。3000要素は mermaid の配置計算が再帰上限に当たるので設定では回避できない |
| UI-002 / UI-006 | Delete キーを `operations.delete` 経由の汎用経路に置き換え、実際に消えた数を数えて告げる。全図種で「N件と関連 M件を削除 — Ctrl+Z で戻せます」 |
| UI-003 | ショートカット一覧の Delete から「Gantt のみ」を外した (実挙動と一致した) |
| UI-005 | `diagnose.js` にエッジ数超過・文字数超過・再帰上限の規則を追加。名指しできない場合の定型文を「構文エラー」から「図を描けませんでした」に変更 (原因を断定しない) |
| UI-007 | 書き出し時に末尾改行を1つ足し、読み込み時に1つ落とす。**条件を付けず常に1つ**にした (「既に改行で終わっていれば足さない」にすると利用者が打った末尾の改行が往復のたびに消える — 実際そうなっていた) |

| UI-008 | `A` で追加フォームの先頭欄へ、`E` でエディタへ移動する1打鍵を追加。欄の id は図種ごとに違う (`fc-add-node-id` / `prop-add-label` / `kb-add-col-name`) ので名前の表は持たず、「add を含む id を持つ最初の入力欄」で選ぶ。**A → 打鍵 → Enter でマウスを1度も使わずに追加できる** |

UI-004 (パネルの収まり) だけ未着手。パネルの構成を変える提案 (折り畳み見出し /
タブ分割) は GUI の作り替えなので、MOC を作って合意を取る規約になっている。
実測はしたが直し方の選択は代行しないので、バックログ (G2) に理由付きで残した。

## 削除の「1回で消える」— r2 の逃げ道

UI-002 の修正を実機で確かめていたら、**erDiagram でエンティティを選んで Delete
すると関係行だけが消え、本体のブロックが残る**ことが分かった。
r2 (削除検査) は 21/21 で「指摘0件」を報告していた。

原因は r2 の述語に付いていた2つの逃げ道:

```js
const lineGone = out.split('\n').length < t0.split('\n').length;
if (after.map(x => x.key).indexOf(el.key) !== -1 && !lineGone) { … }
//                                                  ^^^^^^^^^^ 何かの行が消えれば見逃す
const stillReferenced = ident !== null && out.indexOf(ident) >= 0;
if (after.length >= before.length && !stillReferenced) { … }
//                                   ^^^^^^^^^^^^^^^^^ 名前が本文に残っていれば見逃す
```

どちらも「導出される要素 (sankey のノード) で誤検出を出さないため」に付けたもの
だが、**どの行が消えたかを問わない**ので、間違った行を消しても通る。

述語を「**1回で消えること**」に変えた。消えない場合は消えるまで繰り返して回数を
数え、1回で消えないものを指摘にする。これで12件出た。

| # | 内容 |
|---|---|
| A67 | `erDiagram.operations.delete` が単なる `deleteLine`。エンティティは「最初に現れた行」= 関係行を持つので、押すと関係だけが消えて宣言ブロックが残る |
| A68 | `classDiagram.operations.delete` も同型。`deleteClass` (id 認識) があるのに入口が使っていなかった |
| A69 | `sankeyBeta` のノードは流れの両端から導出されるので、1本消しても他の流れが名前を挙げていれば残る。ノードを消したら端点に持つ流れをすべて落とすようにした (flowchart がエッジを落とすのと同じ約束) |

**A57 に続く同じ archetype がさらに3件。** 「UI 経路は直っているが契約経路が古い」
または「id 認識の関数はあるのに入口が繋がっていない」形は、これで通算11件になった。

## レビュアーの前提が誤っていた記録 (11・12回目)

| 誤り | 内容 |
|---|---|
| 11回目 | 新しい r2 が gitGraph の無名コミット (`id: ""`) と `checkout` / `merge` を「消えない」と報告。これらは id も name も label も無く、鍵が**添字**になる。添字は削除で振り直されるので identity として使えない (自動採番 id と同じ罠)。同定できない要素は対象から外し、外した件数を出すようにした |
| 12回目 | 同定の判定に `text` を入れ忘れ、journey のタスク (`text: '起床'`) を14要素まとめて除外していた。「その要素を人が見分ける文字」の置き場所は図種で違う (label / name / text) ので、1つでも決め打ちすると図種が黙って検査から外れる |

## 自分の変更が壊したもの (即日で検出)

| 内容 | 検出した観点 |
|---|---|
| 末尾改行を「既に改行で終わっていれば足さない」にしたら、利用者が打った末尾の改行が往復のたびに消えた | r10 往復 (6件) |
| 空 id の鍵を添字にしたら、並べ替えで集合が変わるように見えた | r6 移動 (1件) |

どちらも実装した直後のレビュアー実行で出た。**自分の直しが別の観点を壊す**のは
毎回起きるので、修正のたびに全観点を回す運用がそのまま効いている。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 902 | 931 |
| e2e | 365 | 377 |
| 実描画オラクル | 34 | 34 |
| 検査した図種の延べ数 | 328 | 328 |

## 追記: UI-008 の実装後

`A` / `E` は修飾なしの1打鍵なので、入力欄とエディタの中では文字として入る
必要がある (Delete / `?` と同じ `!inInput && !inEditor` の条件)。
実測で6図種すべて意図どおり動き、エディタでの `a` / `e` の打鍵も壊れていない。

| 図種 | `A` の飛び先 | `E` の飛び先 |
|---|---|---|
| gantt | `prop-add-label` | `editor` |
| flowchart | `fc-add-node-id` | `editor` |
| classDiagram | `cl-add-class-id` | `editor` |
| kanban | `kb-add-col-name` | `editor` |
| C4Context | `c4-add-kind` | `editor` |
| pie | `pie-add-label` | `editor` |

## 最終の数字 (第17ラウンド終了時)

| | 開始時 | 現在 |
|---|---|---|
| ユニット | 750 | 931 |
| e2e | 341 | 385 |
| 実描画オラクル | 0 | 34 |
| レビュー観点 | 4 | 20 |
| 検査した図種の延べ数 | (未計測) | 328 |
| 指摘 | — | 0 |

`node tools/review/gate.js` 合格 (G1/G2/G3/G4/G5/G7 + LOOP + COV + BACKLOG)。

---

# 第18ラウンド — 書き出したものを持ち出せるか (UI-009〜UI-012)

前回のレビューで8件中6件を直したので、同じ観点を繰り返しても新しい情報は出ない。
前回「未確認」と明示した4項目を実測した。

## UI-009 SVG の id が図ごとに一意でない (Major)

mermaid が付ける id は `mermaid-svg-<セッション内の連番>`。
**別々のセッションで書き出した2枚が同じ id を持つ**。実測: 2つの `.mmd` を
それぞれ開いて書き出すと、どちらも `mermaid-svg-2` になった。

害は2段階ある。

| 段階 | 内容 |
|---|---|
| スタイル | `#mermaid-svg-2 .node rect { … }` の形で id に紐付く (flowchart 64箇所 / gantt 109箇所) |
| マーカー | 矢印の id も同じ接頭辞 (`mermaid-svg-2_flowchart-v2-pointEnd`)。**`url(#…)` は文書内で最初に見つかった id を拾う**ので、2枚並べると2枚目の矢印が1枚目のマーカーを使う |

実測で確認できたのは「`getElementById('mermaid-svg-2')` が1枚目しか返さない」まで。
色が混ざるかは、両方が同じテーマなので再現できなかった (未確認として記録)。

書き出し時に `ma-<ファイル名>` へ付け替え、`<style>` 内の参照・`[id]` を持つ全要素・
`url(#…)` で参照する属性 (marker-start / marker-end / fill / clip-path …) を
まとめて書き換えた。旧 id の残り0、マーカー12個の重複0を実測。

**id 本体だけ直しても意味がない。** マーカーの接頭辞が残れば矢印は入れ替わったまま。

## UI-010 PNG が等倍しか出せない (Major)

実測: viewBox 788x196 → PNG 788x196 (倍率 1.00)。ズームを変えても出力寸法は
変わらない (50% / 100% / 200% いずれも同じ) ので回避策が無い。

788px 幅の画像を A4 の設計書に貼ると、拡大時も印刷時も字が読めない。
姉妹プロジェクト 01_StableBlock は同じ理由で Canvas 書き出しに scale=2 を入れている。

「PNGとして保存（2倍）」を追加。1576x392 / ファイル名 `<名前>@2x.png`。
**既定は等倍のまま**にした (既定を変えると、いま等倍を前提にしている書き出しの
寸法が黙って変わる)。Chrome の canvas 上限 65,535px を超える倍率は落とし、
落としたことを告げる (黙って小さい画像を返さない)。

## UI-011 小画面でのパネルの収まり — 前回の判定が甘かった (Minor → Major)

| 解像度 | 該当図種 | 最大の超過 |
|---|---|---|
| 1500x950 (前回の測定条件) | 8/21 | C4Context 264px |
| **1366x768 (13インチ相当)** | **15/21** | **flowchart 320px** |
| 1920x1080 | 3/21 | architecture-beta 184px |

前回 1500x950 だけで測って「Minor」と判定したが、**ノートPC の常用解像度では
倍近い図種で起きる**。測定条件が甘かったので Major に引き上げる。

未修正。パネル構成の変更 (折り畳み見出し / タブ分割) は GUI の作り替えなので
MOC の合意が要る (`docs/backlog.md` G2)。

## UI-012 Ctrl+A / Ctrl+C/V が Gantt のみ (Nit)

Delete は前ラウンドで全図種に広がったので、残る「Gantt のみ」は2項目。
未修正 — 複写は「何を複製するか」が図種で意味が変わる (gantt のタスク複製 ≠
sankey のノード複製) ため、仕様の決めが先に要る。

## 指摘ゼロの観点を作らないための走査

| 観点 | 検証したこと |
|---|---|
| 1 操作コスト | `A` → 打鍵 → `Enter` で追加が完結し、Delete も全図種で効く。追加・削除のマウス往復は0 |
| 4 フィードバック | 3000要素で「要素が多すぎて mermaid の配置計算が破綻しました」と名指しできる |
| 5 エラー回復 | Delete 後の Undo が19図種で成立。削除時に実数を告げる |
| 6 学習曲線 | ショートカット15個中「Gantt のみ」は2個 (前回3個) |
| 7 スケール耐性 | 800要素 4.4秒 / 1500要素 13.9秒 / 3000要素は不可。上限が「落ちる」から「遅い」に変わった |

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 931 | 931 |
| e2e | 385 | 390 |
| 実描画オラクル | 34 | 34 |

---

# 第19ラウンド — 測定条件そのものを疑う (Y6・21観点目)

UI-011 は「観点が足りなかった」のではなく「**測る場所が実利用と違っていた**」
ために見逃されていた。1500x950 で測って 8/21・Minor と判定したものが、
13インチのノートPC (1366x768) では **15/21**、flowchart は 320px はみ出していた。

同じ形が他にないか確かめるため、実機を使う8観点の画面サイズを 1366x768 に
揃えて走らせ直した。

## 条件を変えるだけでは足りなかった

| | 結果 |
|---|---|
| 1400x900 での20観点 | 0件 |
| **1366x768 での20観点** | **0件** |

**条件を実利用に合わせても、何も出なかった。**
「画面に収まっているか」という述語をどのレビュアーも持っていなかったからで、
UI-011 は私のその場限りの計測で見つけたものであり、**同じ回帰が起きても
誰も気付かない状態**だった。条件の見直しと述語の追加は別の作業だった。

## 21観点目 r21-reachable

見るもの:

| | 内容 |
|---|---|
| V1 | 操作に必要なもの (追加ボタン) がパネルの見える範囲にあるか |
| V2 | 収まらない場合、続きがあることが画面から分かるか / 収まっているのに合図を出していないか |
| V3 | その合図が中身に重なっていないか |
| V4 | 一番下まで送ったら合図が消えるか |

変異注入で空振りでないことを確認:

| 変異 | 結果 |
|---|---|
| 帯を出さない (`hint.hidden = true`) | **20件** 検出 (V2) |
| 常に出す (`hint.hidden = false`) | **21件** 検出 (V4) |
| 帯を中身に重ねる (`position: absolute`) | **14件** 検出 (V3) |

3つ目は最初「検出できなかった」ように見えたが、**変異が入っていなかった**
(CRLF のファイルに `\n` で置換していた)。変異が入ったことを確かめずに
「レビュアーが空振り」と結論しかけた — 変異注入そのものにも同じ罠がある。

## UI-011 の最小修正

パネルの構成を変える案 (折り畳み見出し / タブ分割) は GUI の作り替えなので
合意が要る (backlog G2 のまま)。ここでは**続きがあることを告げるだけ**にした。

実装の途中で、計測は通るのにスクリーンショットで壊れている形が出た:

| 段階 | 結果 |
|---|---|
| 帯を絶対配置で重ねる | 計測 (帯が15図種で出る) は通る。**実機では最後の行に重なって読めない** |
| 中身に下余白を足す | スクロール最下部では解決。**上部では依然として重なる** |
| 帯を通常フローに置く (必要時のみ表示) | 重なり0。収まっている6図種では高さを取らない |

「GUI の変更を実機スクリーンショット無しで PASS 判定しない」という規約が
そのまま効いた。計測だけなら1段階目で通っていた。

## UI-013 本文の編集で追加フォームの書きかけが消える (Major)

r21 とは別に、e2e が1件落ちて見つかった。最初は不安定なテストに見えたが、実測すると:

| 操作 | 結果 |
|---|---|
| 追加フォームに「WIP」→ エディタに1文字打つ | フォームが**空になる** |
| エディタに打った直後にフォームへ「ae」 | **「e」になる** (先頭が落ちる) |

パネルは毎回 `innerHTML` を作り直すので、書きかけが消える。
ただし**いつでも書き戻してよいわけではない** — 「+追加」の直後に空になるのは
正しく、そこで書き戻すと同じ要素をもう一度足しかねない。

再描画の引き金 (`refreshCause`) で分けた。エディタ由来のときだけ、
`id` に `add` を含む入力欄の値を持ち越す。図種を切り替えると `id` ごと
入れ替わるので、別の図種へ持ち越されることはない (R15 を壊していないことを
レビュアーと e2e の両方で確認)。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 931 | 931 |
| e2e | 390 | 398 |
| 観点 | 20 | 21 |
| 検査した図種の延べ数 | 328 | 349 |

---

# 第20ラウンド — 「未確認」に隠していたもの (UI-014/UI-015・22観点目)

前回まで3ラウンド続けて「クリップボード書き出しはヘッドレスでは権限が下りず
未確認」と書いていた。**Playwright は権限を与えられる。**
測れるものを「未確認」として先送りしていただけだった。

「未確認」は正直な記録の形だが、**測れるのに測っていないものを入れると、
先送りの隠れ場所になる**。今回の一覧を見直したところ2件が該当した。

| 前回「未確認」と書いたもの | 実際 |
|---|---|
| クリップボード書き出し | `context.grantPermissions` で測れる → **測ったら欠陥2件** |
| 高DPI (devicePixelRatio 2) | `deviceScaleFactor: 2` で測れる → 測った。差は無かった |
| SVG のスタイル相互適用 | 異なるテーマが要る。テーマは1つしかないので**本当に未確認** |
| 実利用の1日あたり操作回数 | 実作業ログが要る。**本当に未確認** |
| VSCode 拡張との併用 | 05 は単体 HTML。**本当に未確認** |

## UI-014 / UI-015 クリップボードのコピーが黙って失敗する (Major)

`navigator.clipboard.write(...)` を呼びっぱなしで、返る Promise を誰も
見ていなかった (`src/app.js` の旧 `exportClipboard`)。実測すると失敗が2通りあり、
**どちらも画面には何も出ない**。

| 条件 | コンソール | 画面 |
|---|---|---|
| 権限が下りない | `Write permission denied` | 無反応 |
| `ClipboardItem` が無い (Firefox / Safari / 古い版) | `ReferenceError` | 無反応 |

押しても何も起きないので、利用者はそのまま資料へ貼り付ける。すると
**前にコピーしていた何か**が入る。図が古いのではなく、**まったく別のもの**が
設計書に載る。失敗が成功と見分けられないのが一番悪い。

3通りとも画面に出るようにした:

| 条件 | 画面 |
|---|---|
| 成功 | 図をクリップボードにコピーしました (12 KB) |
| 権限が下りない | クリップボードにコピーできませんでした — Write permission denied |
| 非対応 | このブラウザは図のコピーに非対応 — Chrome / Edge なら使えます。PNG で保存してください |

未処理の例外も残らなくなった。非対応の告知は、上書き保存の
「このブラウザは上書き保存に非対応 — Chrome / Edge なら使えます」と同じ作法。

## 22観点目 r22-silent-failure

「押しても何も起きないボタン」を探す観点。既存の21観点は誰もこれを見ていなかった
(ダウンロードを伴う書き出しは r10 が見ているが、**クリップボードはファイルを
作らない**ので網に掛からなかった)。

| | 内容 |
|---|---|
| S1 | 押したときに本文か画面のどちらかが必ず動くこと |
| S2 | 未処理の例外を残さないこと (例外は利用者に届かない) |
| S3 | 何も起きない場合、その理由が画面に出ていること |

権限あり・なしの両方で走らせる。実務では両方起きる (会社の設定で
クリップボード権限が落ちていることがある)。

変異注入で確認: `exportClipboard` を修正前の形 (呼びっぱなし) に戻すと **3件**
検出した (S1 権限あり / S2 権限なし / S1 権限なし)。

## 高DPI は差が無かった (記録として残す)

`deviceScaleFactor` を 1 と 2 で比べた。パネルの超過 344px、帯の表示、
図の幅 339px、ズーム 139% — **すべて同じ**。指摘なし。
「測って差が無かった」と「測っていない」は別なので、測ったことを記録に残す。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 931 | 931 |
| e2e | 398 | 401 |
| 観点 | 21 | 22 |

---

# 第21ラウンド — 「見送り」と書いた記録を疑う

直近3ラウンドの新規欠陥は、すべて**既存の記録を疑うこと**から出た
(未確認の実測 / 測定条件 / 前回判定の訂正)。観点を足す方は当たっていない。
今回は「見送り」欄を疑った。

## 未確認だった1件を測り切った

UI-009 で「2枚並べるとスタイルが混ざる」と書いたが再現できず「未確認」に
していた。両方が同じテーマだから見分けられなかっただけで、片方を手で
書き換えれば測れる (印刷用に色を変えるのは実務で普通にやる)。

測ると**害の向きは2通り**あった。UI-009 で書いたのは片方だけだった。

| 仕組み | 勝つ側 | 起きること |
|---|---|---|
| `url(#id)` の参照 | **最初の一致** | 2枚目が1枚目のマーカーを使う (矢印の形が入れ替わる) |
| 同じセレクタの CSS | **後** | **1枚目が自分のスタイルを失う** (書き換えた色が効かなくなる) |

id を一意にした現状ではどちらも起きない。e2e で固定した。

なお、最初の測定は**書き換えが当たっていなかった**ので無効だった。
セレクタが `#id .node rect, … path{…}` の並びなので `.node rect{` を
探しても一致しない。**測定が空振りしたまま結論を出しかけた** (r21 の変異注入と
同じ罠で、これで2回目)。

## E2 の記録が誤っていた

見送り欄にはこう書いてあった。

> E2 mindmap / kanban / journey / timeline のオーバーレイ
> 4図種とも `#overlay-layer` の要素数 **0**。mermaid が DSL の id を SVG に
> 出さない制約は今も有効

2つ誤りがあった。

**誤り1: 4図種ではなく11図種だった。** r18 (キーボード完結) は 11図種で
「重ね合わせが無い」と記録していたのに、見送り欄は4図種のままだった。

**誤り2: 「mermaid が id を出さない」が全図種に当てはまらなかった。**
11図種で mermaid が SVG に出しているものを実測した:

| 図種 | SVG の id | 使えるか |
|---|---|---|
| **kanban** | `Todo` / `設計 中` (列名)、`t1` (DSL の id) | **使える。順序に依らない** |
| journey | `task0`, `task1` | 位置由来 → 使えない |
| sankey | `node-1`, `node-2` | 位置由来 → 使えない |
| mindmap | `edge_0_1` | 位置由来 → 使えない |
| timeline | `node-undefined` | 手掛かりにならない |
| gitGraph / pie / quadrant / xychart / packet / radar | id なし | 使えない |

**理由が全図種に当てはまるかを確かめずに一括りにしていた。**
10図種については理由は今も正しい。kanban だけが違った。

## kanban の重ね合わせを実装した

列は `<g class="cluster" id="列名">`、札は `<g class="node" id="DSLのid">`。
どちらも順序に依らないので直接照合できる。

実装中に出たこと:

| | 内容 |
|---|---|
| 列を押すと札が選ばれた | 列の枠は札の上に重なって見える。枠全体を当たり判定にすると札を押せない。**列は見出しの帯だけをつかみ所にする** (札は列とは別のグループにあるので重ならない) |

r18 (キーボード完結) の網羅率が 10/21 → **11/21** に上がった。
矢印キーで選べる図種が1つ増えたということで、**網羅率が実装の前進を映した**。

## A78 id 付きのカードが一覧から消えていた (Major)

重ね合わせのために mermaid の SVG id を調べていて見つかった。

kanban のカードは `[本文]` と `id[本文]` の2通りで書ける。`@{ assigned: '…' }` の
割り当てを付けるには後者が要る (mermaid の正規表記)。ところが parser は
`^\[` 始まりしか見ていなかったので、**id 付きのカードは行ごと消えていた**
(列判定の indent にも掛からない)。mermaid は描くので、図には出るのに
一覧にも重ね合わせにも出ない。

観点を増やして出たものではなく、**記録を疑った副産物**。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 931 | 938 |
| e2e | 401 | 406 |
| r18 の網羅 | 10/21 | 11/21 |

---

# 第22ラウンド — 棚卸しが現実に追いついていなかった

E2 の記録が誤っていた (第21ラウンド) ので、他の区分も同じ目で見た。

## C 区分が6ラウンド分・9項目遅れていた

棚卸しの C 区分 (検証の仕組み) は **C27 で止まっていた**。
第15〜21ラウンドで足した仕組みが1つも書かれていない:

| 抜けていたもの |
|---|
| 網羅率の自動計測 (`lib.js`) |
| 網羅率の下限ゲート (`coverage-floor.json` + `COV`) |
| r11 の契約ベース化 (6/21 → 18/21) |
| r17 / r18 の範囲拡大 |
| r2 の逃げ道を塞いだこと |
| R20 日本語名の非対称 |
| 測定条件の固定 (1366x768) |
| R21 画面に見えているか |
| R22 無言の失敗 |

さらに、**初期4観点のうち R3 と R4 は最初から記録されていなかった**。
「C2 並行レビュー機構」でまとめて済ませていた。

## ゲートは記録の陳腐化を検出できていなかった

`gate.js` の棚卸し検査は **D 区分 (残) しか見ていない**。
C 区分が9項目遅れていても、毎回 PASS していた。

観点を足しても棚卸しに書かなければ、次に読む人はその観点の存在を知らない。
「プランが現実を写す測定器」であるためには、機械で照合できる部分は
機械で照合する必要がある。

`DOC` 検査を追加した: **`tools/review/r<N>-*.js` が実在するなら、
`docs/backlog.md` に `R<N>` または `r<N>` の記録があること**。

変異注入で確認: 棚卸しから `R22` の記載を消すと
`FAIL DOC 棚卸しに記録が無い: r22-silent-failure.js`。
同時に、変異を入れる前から **r3 と r4 が本当に記録されていない**ことも出た
(変異で入れたつもりの無い指摘が出た = 検査が実物を見ている証拠でもある)。

## この形の一般化

3ラウンド続けて「記録が現実と食い違っている」が見つかっている。

| ラウンド | 誤っていた記録 | 見つけ方 |
|---|---|---|
| 19 | 測定条件 (1500x950 は実利用と違う) | 実利用の解像度で測り直した |
| 20 | 「未確認」に測れるものが入っていた | 未確認の一覧を1件ずつ検めた |
| 21 | E2「4図種」「id を出さない」 | 実測して数と理由を確かめた |
| 22 | C 区分が9項目遅れ / R3・R4 が未記録 | 実体と記録を機械で照合した |

**観点を増やす方向では1件も出ていない。** 記録を疑う方向でだけ出ている。
19〜22 の4ラウンドで、新観点による発見は0件、記録の検証による発見は7件。

## 数字

| | 前 | 後 |
|---|---|---|
| ゲートの検査項目 | 7 | 8 (`DOC` を追加) |
| 棚卸し C 区分 | 27項目 | 38項目 (C2a/C2b + C28〜C36) |

---

# 第23ラウンド — E 区分の残りを実測する (Y7)

E2 が誤っていたので、同じ欄の残り (E1 / E6 / E7) と G1 も実測した。

## E1 と E6 の記録が現実とずれていた

| # | 記録 | 実測 (2026-08-21) |
|---|---|---|
| E1 | app.js 3073 → **2232行** | **2583行**。移設は本当に済んでいるが、その後の機能追加で増えており数字が古い |
| E6 | 更新関数の形が**3通り** | `operations.update` は **4引数8図種 / 5引数13図種**の2通り |

E6 の実測から本題が出た。**4引数のモジュールは `opts` を黙って無視する。**
JavaScript は余分な引数を捨てるので、契約どおり `opts` を渡しても効かない。
そのうち要素の種別が2つ以上ある図種を調べた:

| 図種 | 要素の種別 | 危険 |
|---|---|---|
| gantt | section / task | 行が別なので line 判定で足りる |
| xychartBeta | bar / line | 同上 |
| **sequence** | participant / message | **参加者がメッセージ行で宣言されうる** |
| sankeyBeta | node / flow | ノードは流れ行から導出される |

sequence が state (A60) と同じ形だった。

## A80 宣言行を持たない参加者が一覧に出ない (Major)

`A->>B: 要求` だけ書けば mermaid は A と B を描く。実務では `participant` 行を
書かないほうがむしろ普通で、短く書けるのが利点。

ところが parser は message 行から参加者を登録していなかった。実測:

| | |
|---|---|
| mermaid が描いたアクターの図形 | **8個** |
| こちらの要素数 | **0** |
| パネルの参加者一覧 | 空 |

**sequence は最頻出の図種**で、この書き方が一番短い。図には出ているので
「一覧に出ない」ことに気付きにくい。erDiagram の日本語名 (A59) と同じ非対称。

## A81 参加者のラベルを変えるとメッセージの本文が書き換わる (Major)

`operations.update` が4引数で `opts` を受けず、行の中身で分岐していたので、
宣言行を持たない参加者を選んでラベルを変えると message 行に回っていた。

    A->>B: 要求   →   A->>B: 端末

state (A60) / flowchart (A56) と同じ形で**3例目**。
`opts.kind` で分岐し、宣言行が無ければ `participant A as ラベル` を作るようにした
(state の `updateStateLabel` と同じ作法)。ID 変更はメッセージの端点を追従させる。

## A82 参加者を消してもライフラインが図に残る (Major)

**この欠陥は以前からあったが、A80 を直して初めて見えた。**

`deleteParticipant(text, lineNum, participantId)` は前からあったのに、契約の
入口が id を渡していなかった。単なる行削除だと `participant A as Client` の
宣言だけが消え、`A->>B: Request` が残る。mermaid は参照だけで参加者を作るので
**一覧から消してもライフラインは図に残る** (しかもラベルを失って「A」に戻る)。

state / flowchart (A57) / er (A67) / class (A68) と同じ形で**5例目**、
「id 認識の関数はあるのに入口が繋がっていない」形は通算**12例目**。

r2 が捕まえられるようになったのは、暗黙参加者を parse に載せたから。
それまでは「消えたこと」を確認する相手が一覧に居なかった。
**parser を直すと、他の観点の網が初めて機能する。**

## この形の一般化 (続き)

| ラウンド | 誤っていた記録 | そこから出た欠陥 |
|---|---|---|
| 19 | 測定条件 | A75 / A76 |
| 20 | 「未確認」に測れるものが入っていた | A77 |
| 21 | E2 の対象数と理由 | A78 / A79 |
| 22 | C 区分が9項目遅れ | (記録の修正のみ) |
| 23 | E1 の数字 / E6 の形 | **A80 / A81 / A82** |

**5ラウンド続けて、新観点による発見は0件。記録の検証による発見は10件。**

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 938 | 954 |
| e2e | 406 | 406 |

---

# 第24ラウンド — E7 の記録が誤っていた (Y7 完了)

E 区分の最後の2件 (E7 / G1) を実測した。

## G1 は記録どおり

`move: false` が class / er / state (と sequence) で維持されている。訂正なし。

## E7 の記録が誤っていた

見送り欄にはこう書いてあった。

> E7 architecture の日本語ラベル / sequence 別名の `;`
> mermaid v11.13 の制約。**引用囲みも効かないことを実測済み**

実測すると:

| 入力 | 結果 |
|---|---|
| `service a(server)[設計]` | Lexer error |
| **`service a(server)["設計"]`** | **OK。「設計」を描画** |
| `participant A as a;b` | Parse error |
| `participant A as "a;b"` | Parse error (記録どおり) |

**architecture のラベルは引用符で通る。** 「引用囲みも効かない」という記録が
誤っており、直せる問題を2ラウンド「直せない制限」として扱っていた。

architecture-beta は組込みのシステム構成図そのもので、
**日本語が書けないのは実務上大きな制約**だった。

## A83 architecture のラベルが日本語を扱えなかった (Major)

書き出し3箇所 (group 追加 / service 追加 / 更新) で引用符を付け、
読み取り2箇所で外すようにした。半角英数字だけのラベルには付けない
(既存の書き出しの見た目を変えない)。

診断も「使えません」から**直し方**に変えた:

    architecture 図のラベルに半角英数字以外を入れるときは
    引用符で囲ってください (例: ["設計"])。「設計」が該当します。

実機で日本語の構成図 (制御系 / CPU コア / 共有メモリ / 外部 I/O) が
描けることを確認した。

## 副産物: `&quot;` を逃がしても出ない図種の判定

architecture は `&quot;` を文字どおり描く (er / state / class は `"` として描く)。
r11 が「`"引用"付き` が図に出ず原因も名指しできていない」と報告した。

**本文に `&quot;` が居る = こちらが逃がした印**なので、それを見つけたら
「この図種では `"` を表せません」と告げるようにした。
pie / quadrant / packet / radar / requirement / architecture が対象。

## テストの前提が変わった

`R-AR-3` (ラベル中の "db" は書き換えない) が落ちた。期待値が
`[db を持つ]` (引用符なし) で、**囲っていなかった頃の形**を見ていたため。
意図 (ラベルの文字を書き換えない) に合わせて `"db を持つ"` を期待するよう直した。

記録が誤っていると、その記録に合わせて書いたテストも誤る。

## E 区分の再検証まとめ (Y7 完了)

| # | 記録 | 実測の結果 |
|---|---|---|
| E1 | app.js 2232行 | **2583行** (数字が古い) |
| E2 | 4図種 / mermaid が id を出さない | **11図種 / kanban には当てはまらない** → A78/A79 |
| E6 | 形が3通り | **4引数8図種 / 5引数13図種** → A80/A81/A82 |
| E7 | 引用囲みも効かない | **architecture は引用符で通る** → A83 |
| G1 | move: false のまま | **記録どおり** |

**5件中4件の記録が誤っていた。** 見送り欄は「判断を記録する場所」だが、
判断の根拠になった実測値が古びても誰も気付かない構造になっていた。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 954 | 963 |
| e2e | 406 | 406 |

---

# 第25ラウンド — A 区分の抜き取り再検証 (Y8)

E 区分は5件中4件の記録が誤っていた。A 区分 (83件すべて「済」) も同じ構造の記録
なので、無作為に10件を選んで「今も直っているか」を実測した。

## 結果: 10件とも記録どおり

| # | 内容 | 結果 |
|---|---|---|
| A1 | block の削除が押した行を消す | 記録どおり |
| A2 | ID リネームが参照側に伝播する | 記録どおり |
| A15 | date-utils が null / 不正入力で壊れない | 記録どおり |
| A26 | requirement の末尾削除で空行が残らない | 記録どおり |
| A27 | mindmap の形状記号入りラベルが消えない | 記録どおり |
| A29 | sequence の # がラベルに残る | 記録どおり |
| A32 | flowchart の記号入りラベルが保たれる | 記録どおり |
| A33 | kanban の同名カラムでカードが正しい列に入る | 記録どおり |
| A34 | block が装飾行を要素にしない | 記録どおり |
| A36 | flowchart の特殊エッジで幽霊ノードが出ない | 記録どおり |

**A 区分は E 区分と違って劣化していない。** 理由ははっきりしていて、
A 区分の項目はほぼすべてテストで守られているのに対し、E 区分は
「判断を書いた文章」で、機械が触れる形になっていなかった。

**記録の形式が、記録の寿命を決めている。**

## ただし、確認の途中で別の欠陥が出た

A27 の確認で最初 field を間違え (`label` を渡したが mindmap は `text`)、
その調査中に隣の欠陥が見つかった。

### A84 mindmap の文字を変えると形状が失われる (Major)

契約入口がこう書かれていた。

    updateNodeText(text, lineNum, opts.text || value, opts.shape || value)
                                                      ^^^^^^^^^^^^^^^^^^^

`field='text'` で呼ぶと **新しい文字が形状として渡る**。`shapeToText` は知らない
形状名を既定として扱うので、文字を変えるだけで形状が消える。

    root((組み込み設計))   →   root新しい根

`root` の接頭辞が文字に食い込み、mindmap の根の指定ごと別物になっていた
(再 parse すると text が `root新しい根`)。エラーは出ないので、
図の見た目が変わったことにしか気付けない。

直したもの:
- `field='text'` では形状を渡さず、行から今の形状を読んで保つ
- `field='shape'` では文字を行から読み直して保つ
- 形状記号の前に置かれた語 (`root`) を保つ

## 「確認手順の誤り」と「本物」を切り分ける

今回、私の確認手順が2回間違っていた。

| | 誤り | どう気付いたか |
|---|---|---|
| 1 | mindmap の field を `label` と決め打ち (正しくは `text`) | NG が出たので実装を読み、field 名が違うと分かった |
| 2 | (第21ラウンド) スタイル書き換えの正規表現が当たらない | 「書き換えが当たったか」を先に出すようにした |

**手順の誤りを本物として報告しないこと**と、**手順の誤りで調査を打ち切らないこと**の
両方が要る。今回は 1 の調査の途中で A84 が出た。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 963 | 970 |
| e2e | 406 | 406 |

---

# 第26ラウンド — 散文の主張を機械に守らせる (Y9)

Y8 でこういう数字が出た。

| 区分 | 記録の形式 | 再検証の結果 |
|---|---|---|
| A (84件) | テストで守られている | 抜き取り10件すべて記録どおり |
| E (5件) | 判断を書いた文章のみ | **5件中4件が誤り** |

**記録の形式が、記録の寿命を決めている。**
E2 は現に誤っていて (「4図種」ではなく11図種、「mermaid が id を出さない」が
kanban に当てはまらない)、誤っていても誰も気付かない状態だった。

## 対象を「測れる主張」に絞る

散文をすべてテストにはできない。価値判断は正しく散文である。
測れるものだけを選んだ。

| # | 主張 | 測れるか |
|---|---|---|
| G1 | class / er / state の `move: false` が維持されている | **測れる** (ソースを見る) |
| E2 | 残る10図種は使える id を出さない | **測れる** (mermaid の出力と parse の名前を突き合わせる) |
| E7 | sequence 別名の `;` は引用符でも通らない | **測れる** (mermaid に渡す) |
| G2 | パネルの構成をどう変えるか | 測れない (価値判断・触らない) |
| G3 | 複写は図種で意味が変わる | 測れない (価値判断・触らない) |

`tools/review/record-claims.js` と、ゲートの `REC` 項目を追加した。

## 判別条件を2回書き直した

最初はパターンの列挙で書いた。

    const POSITIONAL = { journey: /^task\d+$/, 'sankey-beta': /^node-\d+$/, ... };

これで**偽陽性を2件出した** (mindmap の `node_0` と sankey の `linearGradient-6`)。
列挙は必ず漏れる。

問うべきことそのものを問う形に変えた。

> **その id は、こちらの parse が知っている名前と一致するか。**

kanban の重ね合わせが実装できたのは `<g id="設計 中">` のように
**列名そのもの**が出ていたからで、`node_0` のような添字は並べ替えると
別の要素を指す。この条件なら列挙も推測も要らない。

変異注入で確認:

| 変異 | 結果 |
|---|---|
| class.js の `move: false` を `true` に | **1件** 検出 (G1) |
| E2 の対象に kanban を混ぜる (使える id を持つ図種) | **1件** 検出 (`["Todo","InProgress","Done"]`) |

## 探査の誤りが3回目

第21ラウンド (スタイル書き換えが当たらない)、第25ラウンド (mindmap の field を
`label` と決め打ち) に続いて3回目。今回は**パターンの列挙が狭かった**。

3回とも「効かなかった」を「差が無い」と読み違えるところだった。
今回は逆に「差がある」と誤報したので、実測して偽陽性と分かった。

**列挙で書いた判別条件は疑う。** 問うべきことを直接問える形があるなら、
そちらに書き換える。

## 数字

| | 前 | 後 |
|---|---|---|
| ゲートの検査項目 | 8 | **9** (`REC` を追加) |
| 機械で守られる棚卸しの主張 | 0 | **3** (G1 / E2 / E7) |

---

# 第27ラウンド — B 区分の抜き取り再検証 (Y10)

A 区分 (84件) と E 区分 (5件) は再検証済みだが、**B 区分 (機能28件) は
一度も検証していなかった**。毎日踏む機能を10件選んで実機で確かめた。

## 結果: 機能はすべて生きている / 記録が1件古い

| # | 内容 | 結果 |
|---|---|---|
| B3 | タスクの連続入力 (追加後ラベル欄へ戻る) | 動く |
| B9 | 一覧の絞り込みが12行以上で出る | 動く |
| B14 | Export に Mermaid記法 (.mmd) | **記録が古い** — B25 で Save に移った |
| B15 | 全21図種で一覧に行が出る | 動く |
| B16 | 未保存がタイトルに出る | 動く |
| B17 | Ctrl+0 / Ctrl+9 | 動く |
| B18 | ステータスバーの `? ショートカット` | 動く |
| B20 | Escape 後のフォーカスがプレビューへ | 動く |
| B25 | Save の分割ボタン | 動く |

**B14 は機能が消えたのではなく場所が変わった。** `.mmd` は Save が
書き出している (実測: `プロジェクト計画.mmd`)。B25「保存の入口を Save に集約」で
移したときに B14 の記述を更新していなかった。

## 調査中に出た本物

### A85 一覧の絞り込みが文書を跨いで残る (Major)

前の文書で「ノード1」と絞り込んだまま別のファイルを開くと、当てはまる行が
1つも無いので **一覧が完全に空に見える** (実測: 20行あって表示0行)。

絞り込み欄は残っているが、パネルの一番上にあり、ノートPC ではスクロールしないと
見えない (UI-011)。素直に読むと「要素が無い図」に見える。

R15 (状態の持ち越し) は**追加フォームの入力欄しか見ていなかった**ので、
この状態は網に掛かっていなかった。

### A86 絞り込みの件数が、絞り込んでいるときだけ見えない (Major)

件数は出していた。ただし **placeholder に書いていた**。

    box.placeholder = q ? ('絞り込み中: ' + hit + '件') : (...)

**placeholder は値が入ると隠れる。** つまり「0件だから空なのか、要素が無いから
空なのか」を見分ける手掛かりが、**必要な場面でだけ消えていた**。

件数を欄の外に出し、0件のときは琥珀色で
「20件中 0件が一致 — 絞り込みを消すと全部出ます」と出すようにした。

## 探査の誤りが4回目と5回目

| 回 | 誤り | 気付き方 |
|---|---|---|
| 4 | 絞り込みの行数を `[data-element-id]` で数えた (正しくは `.ma-list-row`) | 22行という有り得ない数字が出た |
| 5 | 図種切替の**確認ダイアログを dismiss** していたので切替が起きていなかった | 切替後も `select=flowchart` のままだった |

5回目は危なかった。「図種を切り替えても絞り込みが消えない」と報告するところで、
実際には**切り替わっていなかった**。ダイアログを accept して測り直したら消えていた。

**探査が「効かなかった」ときは、実装ではなく探査を先に疑う。** これで5回とも
同じ形だった。

## 区分ごとの記録の寿命 (3区分そろった)

| 区分 | 記録の形式 | 再検証の結果 |
|---|---|---|
| A (84件) | テストで守られている | 10/10 記録どおり |
| **B (28件)** | **テスト + 実機 e2e** | **10/10 機能は生きている / 記録の場所が1件古い** |
| E (5件) | 判断を書いた文章のみ | 4/5 が誤り |

**機能そのものはテストが守る。「どこにあるか」は守られない。**
B14 は機能が生きていながら記録だけが古びた。機能の有無と所在は別の述語で、
テストは前者しか見ていない。

## 数字

| | 前 | 後 |
|---|---|---|
| e2e | 406 | 410 |

---

# 第28ラウンド — 検査そのものを検査する (Y11)

A・B・E の3区分は再検証したが、**C 区分 (検証の仕組み43件) は一度も検証して
いなかった**。A・B の「10/10 記録どおり」という判定は、すべて C の仕組みを
信じて出している。**前提を確かめていないのは順序が逆。**

記録には「変異で確認」とあるが、**それ自体が散文の主張**である。

## 結果: 6件中5件は有効、1件は記録が実態より広い

| # | 検査 | 変異 | 結果 |
|---|---|---|---|
| C7 | R6 並び替え | `swapLines` を無効化 | **43件検出** |
| C11 | R8 スケール耐性 | 60行超で空を返す | **1件検出** |
| C13 | R9 ワークフロー適合 | `deleteLine` がコメント行を落とす | **11件検出** |
| C15 | R11 特殊文字 | quadrant の引用符付与を外す | **8件検出** |
| C17 | R12 無言の空振り | pie の `update` を無変化に | **1件検出** |
| C20 | R15 状態の持ち越し | `resetTransientState` を呼ばない | **1件検出** |
| C25 | R19 網羅の前提条件 | pie の `elements` を空に | **1件検出** |
| **C21** | **R16 件数の一致** | pie の要素を1つ減らす | **0件** |

## C21 の記録が実態より広かった

r16 の冒頭にこう書いてあった。

> 逆 (図にあるのにパネルに無い) は、mermaid が暗黙に作る要素があるので単純比較
> できない。そちらは **r2 / r11 が個別に見ている**。

**この後半が誤り。** A59 (erDiagram の日本語エンティティ) も A80 (sequence の
宣言なし参加者) も「図にあるのにパネルに無い」形で、**r2 も r11 も捕まえておらず、
どちらも手作業で見つけた**。

棚卸しの C21 も「パネルの数 vs 実描画」と両方向を示唆していた。実態は片方向。

## 逆向きの検査を実装した

単純比較ができないのは本当なので、比較の仕方を変えた。

> **同じ図の識別子を日本語に置き換えたとき、こちらの要素数が変わらないこと。**

mermaid が両方を同じように描くなら、数が変わるのはこちらの読み取りが狭いという
こと。これが A59 / A80 の archetype そのもの。

### A87 block-beta が日本語のブロック id を読めない (Major)

逆向きの検査を入れたら出た。実測:

| | 英数字 id | 日本語 id |
|---|---|---|
| mermaid | 図形11個・文字も出る | **図形11個・文字も出る** |
| こちらの parse | 3要素 | **0要素** |
| パネルの一覧 | 3行 | **0行** |

`[A-Za-z_]` 始まりでしか拾っていなかった。erDiagram (A59) / sequence (A80) と
同じ「述語の非対称」で**3例目**。

### A88 ブロックを消すと図に幽霊が残る (Major)

A87 を直す過程で見つかった。`a --> b` を残したまま `b` を消すと、
mermaid は `b` を暗黙に作るので **図に「A C b」と出る**。
パネルには a と c しかない。A57 と同じ archetype で**13例目**。

cascade のコードは存在していたが、**私が識別子パターンを広げるときに
`\s` を潰して壊していた**。壊した状態で「元からそうだった」と誤認しかけた。

## 探査の誤りが6・7回目

| 回 | 誤り | 気付き方 |
|---|---|---|
| 6 | 変異が**構文を壊し**、モジュールが読み込まれず0件になった | 「pie は読み込めるか: false」を直接測った |
| 7 | 日本語への置き換えが**単射でなく**、`A` と `B` が同じ `識1` に潰れた | 実機で日本語 flowchart が動くことを確認して矛盾に気付いた |

7回目は「flowchart で5→1に減る」という**偽の指摘**を出すところだった。

## 副産物: モジュールの読み込み失敗が握り潰されていた

6回目の誤りの調査で分かった。`loadModules` はモジュールの読み込み失敗を
黙って捨てており、**総数も 21 → 20 に下がるので「20/20」= 満点に見えていた**。

    [r12-noop] findings=0 / 検査した図種 20/20     ← pie.js が壊れている状態

ゲートの `COV` は下限 (21) と比べるので落ちるが、**レビュアーの出力だけを
読んでいる人には何も見えない**。0件と満点が同時に出るのが一番危ない。

読み込み失敗を警告として出し、母数にも含めるようにした。

    !! モジュールを読み込めない (検査対象から外れる): pie.js: Unexpected identifier
    [r12-noop] findings=0 / 検査した図種 20/21

## 4区分そろった記録の寿命

| 区分 | 記録の形式 | 再検証の結果 |
|---|---|---|
| A (86件) | テストで守られている | 10/10 記録どおり |
| B (28件) | テスト + 実機 e2e | 機能は 10/10・**所在が1件古い** |
| **C (43件)** | **仕組みそのもの** | **6/7 有効・1件は記録が実態より広い** |
| E (5件) | 判断を書いた文章のみ | **4/5 が誤り** |

**検査は概ね有効だった。** ただし「何を見ているか」の記述は広がりやすく、
C21 は片方向しか見ていないのに両方向を示唆していた。
**検査の範囲は、検査そのものと同じくらい記録が狂いやすい。**

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 970 | 977 |
| e2e | 410 | 410 |

---

# 第29ラウンド — 新しい検査の「0件」を検める

前ラウンドで r16 に逆向きの検査を足し、A87 (block-beta の日本語 id) を見つけた。
ただし **その検査には「検査から外れた図種を黙って捨てない」規約が入っていなかった**。
他のレビュアーは全部やっているのに、新しく書いた部分だけ抜けていた。

## 0件の内訳を出したら、10/21 しか見ていなかった

    (逆向きの検査: 10 図種を検査 / 9 図種は日本語に置き換えると
     mermaid が受け付けないため対象外: architectureBeta, c4, erDiagram,
     gitGraph, quadrantChart, radarBeta, requirementDiagram, sankeyBeta, xychartBeta)

**除外された9図種に erDiagram が入っていた。** A59 が出た図種そのもので、
**この検査では A59 を見つけられなかった**ということ。

## 原因: 置き換えが広すぎた

本文中の英単語をすべて置き換えていたので、

    CUSTOMER ||--o{ ORDER : places
    識1      ||--識2{ 識3 : 識4        ← 基数記号の `o` まで巻き込む

mermaid が拒否し、そのまま「対象外」に落ちていた。

**予約語の一覧を持てば漏れる** (r11 の関数名の表と同じ形)。
置き換えるのは **そのモジュール自身が id と呼んでいるもの** だけにした。
何が識別子かはモジュールが答える。

| | 前 | 後 |
|---|---|---|
| 逆向きを検査できた図種 | 10 | **11** |
| mermaid が拒否した図種 | 9 | **3** (architecture / radar / sankey) |
| 置き換えられる id が無い図種 | (数えていない) | **7** (自動採番のみ) |

## A59 を再現して検出できることを確かめた

er の識別子パターンを ASCII 限定に戻す変異を入れた (8箇所)。

    日本語ERの要素数: 0   ← A59 が再現
    [r16-count-parity] findings=1
      - erDiagram.P2 逆向き: 識別子を日本語にすると要素が 2 → 0 に減る

**逆向きの検査が本来の archetype を捕まえられることを確認した。**
前ラウンドの時点では捕まえられていなかった。

一度目の変異注入は文字列が一致せず「入っていない」と出た。
**変異が入ったかを毎回確かめる**手順が効いた (これで確認は8回目)。

## 分かったことの一般化

**新しく書いた検査には、既存の検査が持っている規約が付いてこない。**

r16 の逆向きは、他の21観点が守っている「除外を黙って捨てない」を持たずに
生まれた。書いた本人 (私) が同じセッションでその規約を何度も適用していても、
新しいコードには自動では入らない。

規約は文章として `docs/quality-gate.md` にあるが、
**新しい検査を書くときに読み返す仕組みが無い**。

## 数字

| | 前 | 後 |
|---|---|---|
| r16 の逆向きが見る図種 | 10/21 | 11/21 (+ 除外の内訳を明示) |

---

# ラウンド30 — 逆向きの検査を全図種に広げ、「追加」に網が無いことが分かった

## 発端

r16 の逆向き (図にあるのにパネルに無い) を requirementDiagram まで広げたら
1件出た。

    requirementDiagram.P2 逆向き:
      識別子を日本語にすると要素が 2 → 1 に減る (mermaid は同じように描く)

診断 (`src/core/diagnose.js`) にはこう書いてあった。

> requirement 図の要求名・要素名には全角文字が使えません

**この記録が誤っていた。** 実測すると引用符で通る。

    requirement 受信要求 {     → Lexical error
    requirement "受信要求" {   → OK (「<<Requirement>>受信要求」を描画)

E7 (architecture, A83) とまったく同じ形。**直せる問題を mermaid の制限として
記録し、診断でその嘘を利用者に伝えていた。**

## 検査自身の誤りが2つ出た

**1. r16 が「引用符付きで通した本文」を読み直していなかった (C44)**

引用符なしで mermaid に拒否されたら引用符付きで再試行する仕組みは入れてあったが、
比較に使う要素数は**引用符なしの本文のまま**だった。requirement を直しても
指摘が消えず、直っていないように見えた。**検査の誤りは9件目。**

直したあと、直す前のモジュールに戻して1件検出することを確認した
(戻す → findings=1、直した版 → findings=0)。

**2. 述語を広げて、直したばかりの欠陥を見逃した (C46)**

後述の r23 で「こちらが渡す値が足りない」を `undefined` と空の `""` の両方で
判定したところ、**空の `""` は A91 そのものの形**だった。変異を入れても
検出できなくなっていた。述語を `undefined` だけに戻した。

> 検査側の都合で述語を広げると、見つけたい欠陥ごと消える。

## 網が掛かっていなかった場所

実描画の網は `gen-rename-cases.js` と `gen-delete-cases.js` の2本しかない。
つまり **改名と削除にしか掛かっていない**。

一番最初にやる操作である「追加」に網が無かった。そこに欠陥が溜まっていた。

追加のレビュアー R23 (`r23-add-renders.js`) を書いた。判定は mermaid に置く。
**ひな形が描けるなら、そこに1つ足したものも描けなければならない。**
足す種類はモジュール自身が一覧に出している種類 (`element.kind`) を使う
(種類の一覧を手で持つと必ず漏れる — r11 の関数名の表と同じ形)。

半角の名前で1回、日本語の名前で1回。半角で壊れるなら追加そのものが壊れている。
半角で通って日本語で壊れるなら別の話なので混ぜない。

## 出た欠陥

| # | 内容 | 重さ |
|---|---|---|
| A90 | requirement が日本語の要求名・要素名を読めない | 一覧が空になる |
| A91 | **「+ 要件追加」「+ エレメント追加」を押しただけで図が壊れる** | 追加が使えない |
| A92 | requirement のエレメント改名が契約経路で無反応 | 黙って効かない |
| A93 | gitGraph が日本語のブランチ名を裸で書き、図が壊れる | 図が壊れる |
| A94 | quadrant の点の追加だけ引用符を通していない | 図が壊れる |

A91 の実測 (mermaid v11.13):

| 書き方 | 結果 |
|---|---|
| `id: ""` / `text: ""` / `type: ""` / `docref: ""` | Parse error |
| `risk:` / `verifymethod:` (値なし) | Parse error |
| その行そのものを書かない | OK |

**単体テストは通っていた。むしろ壊れた出力の方を固定していた** —
`expect(out).toContain('id: ""')`。パーサは自分が書いた空文字をそのまま
読み戻せるので、パーサとテストだけでは永久に気づけない。

A92 は「UI 経路だけ動いて契約経路が動かない」形の**14件目**。
パネルは `updateName` を直接呼ぶので画面上は動き、`operations.update` は
kind の分岐が name より先にあるため type/docref しか知らない関数へ流れていた。

## mermaid の制限と、こちらの欠陥を分ける

日本語で壊れたとき、引用符で囲えば通るなら**こちらの欠陥**、
引用符でも通らないなら**mermaid の制限**。r23 は毎回これを実測して分ける。

| 図種 | 引用符付き | 判定 |
|---|---|---|
| requirement の要求名 / 要素名 | 通る | こちらの欠陥 (A90) |
| gitGraph の branch | 通る | こちらの欠陥 (A93) |
| quadrant の点名 | 通る | こちらの欠陥 (A94) |
| architecture の service id | 通らない | mermaid の制限 |
| radar の curve id | 通らない | mermaid の制限 |

architecture は**ラベルは引用符で通る (A83) が id は通らない**。
同じ図種の中で分かれるので、図種単位で「日本語が使えない」と記録すると必ず誤る。

## 検査の誤り (通算11件)

| # | 内容 |
|---|---|
| 9 | r16 が引用符付きの再試行後に要素数を読み直していない |
| 10 | r23 の props が図種ごとに解釈の割れる値 (`type: 'simulation'`) を渡し、gitGraph で誤検出 |
| 11 | r23 の「渡す値が足りない」述語に空の `""` を含め、A91 の形を見逃す |

10 は「値の組を2通り試し、どちらでも描けないときだけ指摘する」で解消。
端点の名前も、既存の要素名を順に試して描けたものを採る
(gitGraph の checkout はコミット id ではなくブランチ名を要る、を手で書かない)。

## 分かったことの一般化

**網は「直した操作」の周りに掛かる。誰も直していない操作には掛からない。**

改名と削除には欠陥が出るたびに網を足してきた。追加は最初に一度書いたきりで、
そこに5件が溜まっていた。**次に網が無い場所は「一度書いて以来触っていない操作」**
であって、複雑な操作ではない。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 977 | 999 |
| レビュアー | 22 | 23 |
| 追加を実描画で確認する組み合わせ | 0 | 30 |
| 指摘 (23観点) | — | 0 |

---

# ラウンド31 — 実UIを測って、検査が触れていない摩擦を3つ数えた

## 測り方

23観点のレビュアーは**モジュールの契約**を見ている。実UIの摩擦
(手数・待ち時間・表記) はそこに映らないので、1366x768 の実画面を
Playwright で操作して数を取った。

## 数えたもの

| 観点 | 実測 |
|---|---|
| 1 操作コスト | 200要素の一覧 = 399行 / 1行 43px / 全体 18685px / 見えている 657px → 末尾まで約180ホイール刻み |
| 1+7 | パネルが縦にはみ出す図種 **15/21**。architecture 520px / C4 470 / flowchart 344 / gitGraph 322 / block 317 / quadrant 316 |
| 3 一貫性 | 「名前」を指す欄が **9通り** (ラベル33 / ID15 / Name9 / Title8 / Text6 / id3 / `id (任意)` / `name` / `Name (1単語)` / タイトル / 名前)。「2つを結ぶ」ボタンが **7通り** |
| 4 フィードバック | 900要素の描画中、screencast のフレームが **6096ms のあいだ1枚も来ない** |
| 5 エラー回復 | 90打鍵ぶんは Ctrl+Z で戻せる (むしろ手前の初期テンプレートまで戻る)。合図は工具列ボタンの無効化のみ |
| 7 スケール耐性 | 10要素 1.6s / 50要素 1.6s / 200要素 2.1s / 500要素 3.8s |
| 8 ワークフロー適合 | git 差分: ラベル変更 +1/-1、id変更 +3/-3、追加 +1/-0、削除 +0/-3、並べ替え +1/-1 |

観点8 は**指摘ではなく賞賛**になった。DSL が正なので差分が最小に収まる。

## 直したもの

**A95 (UI-026)** エディタにカーソルがあるとヘルプを開く手段が一つも無かった。

    エディタで `?` → ヘルプは出ず、本文に「?」が入る   (実測)
    プレビュー / パネル / 読み込み直後で `?` → 出る

`src/app.js:2416` の条件が `!inInput && !inEditor`。`?` は本文に打てる文字
なのでこの条件自体は正しいが、**利用者が一番長くいる場所からの経路が無かった**。
F1 を追加。ヘルプ一覧に載っていなかった `F1` / `Tab` も追記。

**A96 (UI-027)** 大きい図の描画中、画面が固まったまま何も言わない。

塗ってから重い処理へ入るようにした。`await` だけではマイクロタスクしか
回らず塗り替えが起きないので、`requestAnimationFrame` の後にもう一度
キューへ戻してから `mermaid.render` を呼ぶ。実測で1秒を超え始める大きさ
(6000文字 / 300行) からにして、小さい図では出さない。

## 測り方そのものの発見

**スクリーンショットでは「固まっている瞬間」を撮れない。**

`Page.captureScreenshot` はメインスレッドが塞がっている間は返らず、
返ったときには描画が終わっている。1000要素で撮った3枚がすべて
完成後の画面だったのはこれが理由で、**危うく「進行表示が出ていない」と
報告するところだった**(検査の誤り12件目)。

塗られた瞬間を見るには、コンポジタが押し出すフレームを受け取る
(`Page.startScreencast`)。受け取った4枚はこう並んだ。

    #0  +14ms     貼る前の画面
    #1  +38ms     「描画中 …」が出ており、プレビューはまだ古い図
    (6秒間フレームなし = メインスレッドが返らない区間)
    #2  +6096ms   完成した図
    #3  +6109ms

**フレームが来ない区間そのものが、固まっていることの証拠**になっている。

## 変異注入 (13回目)

進行表示を止める変異を入れて e2e が1件落ちることを確認した。
一度目は Python の文字列が一致せず変異が入らないまま「4件とも通過」と
出た。**変異が入ったかを先に確かめる**手順で気付いた (これで9回目)。

## 数字

| | 前 | 後 |
|---|---|---|
| e2e | 410 | 418 |
| 実UIで数えた観点 | 0 | 8 |
| 未処理の UI 指摘 | 3 | 5 (UI-022/023/024/025/028、うち2件は今回実測で新規) |

---

# ラウンド32 — 欄名を3語に決め、改善案の1つが誤りだったと分かった

## 決めたこと

「名前」を指す欄が **9通り**あった (実測)。3語に決めた。**意味で分ける**ので
機械が判定できる。

    ID     他の行から参照される識別子 (消すと参照が壊れる)
    ラベル  図に出る文字で、他の行から参照されないもの
    タイトル 図全体の題

52箇所を書き換えた。内訳:

| 旧 | 新 | 件数 |
|---|---|---|
| Title | タイトル | 8 |
| Name (journey / kanban / timeline のセクション・列) | ラベル | 5 |
| Name (requirement の要求名・要素名 = 関係行から参照される) | ID | 4 |
| Text | ラベル | 6 |
| id / name / `id (任意)` / Branch name | ID | 6 |
| クラス名 / エンティティ名 / グループID / 名前 | ID / ラベル | 4 |
| Event / Value / Values | イベント / 値 | 8 |
| Source / Target (sankey の端点) | From / To | 2 |
| target / type の大小のゆれ | Target / Type | 5 |
| gitGraph のボタン文言と見出し (英語) | カタカナ | 10 |

sankey は**追加側と編集側で端点の呼び方が割れていた** (追加は Source/Target、
編集は From/To)。片方だけ直したのは今回の私で、実機の画面を撮って気付いた。

## 改善案の方が誤っていた

前ラウンドの批判レビューで UI-025 として

> 「2つを結ぶ」ボタンが7通り。「+ 接続を追加」に統一する

と書いた。**これは誤り。** 「遷移」(UML状態機械) 「関連」(クラス図)
「リレーションシップ」(ER) はいずれもドメイン上正しい用語で、
同じ語に潰すのは一貫性ではなく改悪になる。組込みのアーキテクトなら
状態遷移を「接続」と呼ばれた時点で信用しない。

実際の摩擦は文言ではなく**位置が図種ごとに動くこと**だった。
そちらは UI-023 と同じ MOC 合意待ちなので、削除リストへ理由付きで移した。

**批判レビューの指摘 (現象) は正しかったが、改善案 (処方) が誤っていた。**
指摘と処方は別に検証する必要がある。

## 直しただけでは戻る

散文の規約は守られないことが分かっている (E 区分の再検証で **5件中4件が誤り**
だった)。欄名の検査 `tools/review/terms.js` を書いてゲートに入れた。
`Name` を1つ戻す変異で1件検出することを確認した (変異注入14回目)。

最初に書いた判定式は狭すぎて、**直した3語自体が「名前を指す欄」に数えられず**
「名前を指すもの 1種」という嘘の集計を出していた。判定式を広げて 3種になった。

## まだ揃っていないものを数に出す

欄名の日英混在が残っている (日本語23種 / 英語19種)。
**直していないものを黙っていると「0件 = 揃っている」に見える**ので、
`terms.js` が毎回数えて出す。指摘にはしない — mermaid のキーワードそのもの
(`tag` / `docref`) は原語が正しく、どこまで訳すかは私の一存で決めない。
G5 として棚卸しへ。

## 数字

| | 前 | 後 |
|---|---|---|
| 「名前」を指す欄の表記 | 9通り | **3通り** |
| 欄名の異なり | 46種 | 45種 |
| 検査 | 23観点 + record-claims | **24観点** + record-claims |
| e2e | 418 | 418 (旧ラベルを固定していた3本を更新) |

---

# ラウンド33 — クリティカルパス上の1件を「合意を問える形」まで持っていった

## 診断で決めたこと

前回の PL 診断は **STOP_AND_DECIDE**。止まっている3件がいずれも
「私が決めると規約違反になる」ものだと3ラウンドかけて確定した。

    ADR-027 採番       禁止事項 (私の承認なしに採番しない)
    UI-023 パネル構成   MOC 合意が必要
    UI-022 一覧の折り畳み 同上

**MOC を作るところまでは私の仕事**なので、今回それをやった。

## やったこと

**A98 (UI-028)** Ctrl+Z / Ctrl+Y を押すたびに残り段数を出す。

    元に戻しました — 戻せる: 1 / やり直せる: 1
    これ以上戻せません — 戻せる: 0 / やり直せる: 2

合図が工具列ボタンの無効化だけだった。キーボードで押している人は
工具列を見ていない。実測では90打鍵したあと押し続けると、自分が編集を
始める前 (起動時のひな形) まで無言で戻っていた。

**A99** ステータスバーの案内が「? ショートカット」のままだった。
`?` はエディタでは効かない (本文に「?」が入る) ので、**効かない操作を
案内していた**。「F1 ショートカット」に変更。

前ラウンドで F1 を足したときに、この案内を直し忘れていた。
**入口を増やしたら案内も直す**が抜けていた形で、実機の画面を撮って気付いた。

**F6** パネル構成の MOC を作成した
(`docs/superpowers/specs/2026-08-22-panel-layout-moc.md`)。

案A 折り畳み見出し / 案B タブ分割 / 案C 一覧だけ高さ固定 / 案D 現状維持 を、
実測値と ASCII の図で並べた。推奨は案A。**実装はしていない** —
実装だけ先行させると合意前に既成事実を作ることになる。

MOC には「未確認」を明示した:

- 折り畳みの既定を「一覧=閉」にしたとき普段の使い方で邪魔になるかは**測っていない**
- 21図種すべてを共通の折り畳みに載せ替えられるかは gantt / sequence で未検証
- 案Bのタブがキーボードだけで回せるかは未検証

## 実機確認

ステータスバーを撮影して確認した。

    これ以上戻せません — 戻せる: 0 / やり直せる: 2

## 数字

| | 前 | 後 |
|---|---|---|
| e2e | 418 | 421 |
| 承認/合意待ち | 3件 (資料なし) | 3件 (**うち1件は資料あり**) |
| 未処理の UI 指摘 | 5 | 2 (UI-022 / UI-023 — 両方とも合意待ち) |

---

# ラウンド34 — 「網が無いのは触っていない操作」を2回目で当てた

## 探し方

承認待ちで手が止まったので、**自分で決めた復活条件**
(「既存観点が2ラウンド連続0件、かつ手作業で欠陥が見つかった時点」)
に従って、まず手作業で探した。

前ラウンドの一般化はこうだった。

> 網は「直した操作」の周りに掛かる。誰も直していない操作には掛からない。
> 次に網が無い場所は「一度書いて以来触っていない操作」であって、
> 複雑な操作ではない。

契約に定義されている操作は add / update / delete / moveUp / moveDown / connect。
実描画の網は **add (r23) と改名・削除 (render-oracle) の3つだけ**だった。

    並べ替え (moveUp / moveDown)  網なし  ← ここを手で探した
    接続 (connect)                網なし  ← 次の候補

## 手作業で9件

| 図種 | 何が起きるか |
|---|---|
| flowchart / erDiagram / requirementDiagram | 先頭の要素を上へ動かすと**図の宣言行と入れ替わって図が消える** |
| kanban | 札がセクション見出しを飛び越える |
| mindmap | 根が2つになる |
| packetBeta | ビット番号が飛んで mermaid に拒否される |
| timeline | 図から期間の文字が消える |

原因は全図種で同じ。

    moveUp: function(text, lineNum) {
      if (lineNum <= 1) return text;                       ← 1行目しか守っていない
      return window.MA.textUpdater.swapLines(text, lineNum, lineNum - 1);
    }

パネルの経路 (flowchart の `_moveNodeStep`) は入れ替え先が動かせる行かを
見ていたので壊れない。**「UI だけ動いて契約が壊れている」形の15例目。**

## 直し方

1行1要素とは限らない。requirement / class / er はブロックを持つので、
行だけ入れ替えると属性が別の要素にくっつく。**要素が占める範囲ごと**
入れ替える (範囲の終わりは「次の要素が始まる直前」= parse が返すのは
開始行だけなので、それ以外に手掛かりがない)。

並びが構造を持つ2図種は共通化できなかった。

- **mindmap** 字下げが親子関係そのもの。同じ親の兄弟どうしを**子孫ごと**動かす
- **packet** ビット位置が中身。入れ替えたあと幅を保って先頭から振り直す

## 検査の誤りが2つ (通算13・14件目)

**13. r24 の M3 が親の違う要素を「動くはず」と誤判定**

mindmap の兄弟は種類ではなく親で決まる。種類だけで束ねたので
FreeRTOS と Sensor を兄弟と見なし、「7個あるのに動かない」と報告した。
要素が名乗る `parentId` で束ねるようにした。

**14. r24 に M4 が無く、元の欠陥を捕まえられなかった**

M1〜M3 は**2番目の要素**しか動かさない。元の欠陥は「先頭の要素を上へ
動かす」ときに起きるので、当たらない。変異を戻したら **9件のうち2件しか
出なかった**ことから判明した。

> 直したあとに書いた検査は、直した結果に合わせて書いてしまう。
> **変異を戻して「元の欠陥が出るか」を確かめないと、この抜けは見えない。**

M4 (端の要素を外へ動かしても壊さない) を足し、しかも
**要素が1つしかない種類でも回す**ようにした (元の欠陥は1つでも起きる)。
変異を戻すと 2件 → **16件**。

## 正しい実装を欠陥と報告しかけた

r9 の W4 は「並べ替えは行の中身を変えない」を見ている。
packet の欄は `0-15: "Source Port"` のように**位置が中身**なので、
番号を振り直す正しい実装がこの規則に触れる。

行ではなく**要素の見分けがつく文字の多重集合**を比べる形に変えた。
緩めた分だけ検出力が落ちていないかは、ラベルを潰す変異で確かめた (1件検出)。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 1000 | 1063 |
| 検査 | 24観点 | **25観点** |
| 並べ替えを実描画で確認する組み合わせ | 0 | 122 (通常42 + 端80) |
| 契約経路の欠陥 (通算) | 15 | 19 |

## 次に網が無い場所

**接続 (`operations.connect`)。** 一度書いて以来触っていない。

---

# ラウンド35 — 探して出なかったので網を足さず、代わりに実UIの一番重い摩擦を直した

## 宣言どおり接続を探した — 0件

前ラウンドで「次に網が無い場所は接続 (`operations.connect`)」と書いたので、
手作業で探した。**10通り繋いで0件。**

途中で1件出たが、私の探索の誤りだった (**検査の誤り15件目**)。
requirement は `id` が要件番号 (REQ-001) で、関係の端点は `name`。
モジュールは `identityField: 'name'` と名乗っているのに、探索が勝手に
`id` を使って存在しない端点を作っていた。

改名以外のフィールド更新も探した。同じ値の書き戻し 100通り、
別の妥当な値への変更 77通り。**0件。**

こちらでも 9件出かけたが、**既存の r12 の規約**「モジュールが知らない欄は
何もしないのが正しい」に反する誤りだった (**検査の誤り16件目**)。
新しい検査に既存の規約が付いてこない、という前ラウンドと同じ形。

**欠陥が出なかったので、自分で決めた規則どおり新しい観点は足さなかった。**
規則は正しく働いた — 先に手で探させ、出なければ投資しない。

## 実UIで一番重い摩擦を測って直した (A104 / UI-031)

`scheduleRefresh` は `requestAnimationFrame` で合流させていたが、
**同じフレームの中しかまとめない**。人が打つ速さ (毎秒5文字 = 200ms 間隔)
では合流せず、1打鍵ごとに図全体を描き直していた。

10文字打つ間に UI が固まった合計 (ページ内の rAF で計測):

| 要素 | 前 | 後 |
|---|---|---|
| 10 | 0ms | 0ms |
| 50 | 797ms (10回) | 789ms (10回) |
| 200 | **4988ms (10回)** | **524ms (1回)** |
| 400 | **14718ms (10回)** | **1409ms (1回)** |

待ち時間は**前回の描き直しに実際かかった時間**から決める。固定値にすると
どちらかが壊れる — 150ms 固定では 200ms 間隔の打鍵に1回も合流せず、
軽い図では無駄に遅くなる。50要素で変わっていないのは、描き直しが 80ms で
しきい値 (150ms) を下回るため。**軽い図の即応性は落としていない。**

## 「軽い側だけ先に走らせる」が逆効果だった

状態表示が最大800ms遅れるのが気になったので、待っている間に
`refresh(true)` (読み取りと状態表示だけ) を毎回走らせてみた。

    描き直しだけ待つ              14718ms → 1460ms
    + 軽い側を毎回先に走らせる              → 22194ms  ← 悪化

**軽いはずの処理が軽くなかった。** 一覧を作り直すので、400要素だと
799行の DOM を毎回組み直して **1打鍵あたり約850ms** かかる。

これは UI-022 (一覧に仮想化が無い) と同じ根。MOC の案A (一覧を折り畳む) が
通れば、閉じている間は行を作らないので同時に解ける。G6 として記録した。

> 直感で「こちらは軽いはずだ」と思った側が重かった。
> **測らずに分けると、分けたこと自体が悪化になる。**

## 変異注入 (15回目)

待ち時間を常に0に戻す変異を入れて e2e が1件落ちることを確認した。

## 数字

| | 前 | 後 |
|---|---|---|
| e2e | 421 | 424 |
| 400要素で10文字打つ間に固まる時間 | 14718ms | 1409ms |
| 手作業で探した組み合わせ (接続・更新) | 0 | 187 |
| 検査の誤り (通算) | 14 | 16 |

---

# ラウンド36 — 「未確認」を2つ潰した。片方は検査の打ち切りが早すぎただけだった

## V1: r18 の網羅 11/21 → 21/21

前ラウンドの未確認事項に「r18 がキーボード完結性を 11/21 図種しか見ておらず、
残り10図種は未確認」と書いた。中を見たら、こう打ち切っていた。

    K1 (矢印キーで選ぶ) ができない → その図種は丸ごと検査終了

重ね合わせを持たない10図種 (timeline / mindmap / gitGraph / pie / journey /
quadrant / xychart / sankey / packet / radar) がここで落ちていた。

**K1 が使えないことと、K2〜K5 が使えないことは別の話だった。**
一覧の「編集」ボタンへ Tab で到達すれば、同じことがキーボードだけでできる。
その経路を足したら 21/21 になった。

> 「未確認」と書いた項目は、たいてい**検査が早く打ち切っている**だけで、
> 対象が検査不能なわけではない。

## 検査の誤り17件目 — 接尾辞の表

網羅を広げたら3件出た。

    mindmap.K2      Tab 30回でもラベル欄に届かない
    xychart-beta.K2 同上
    sankey-beta.K2  同上

**すべて誤報だった。** K2 は id の接尾辞 (`-label` / `-name` / `-id`) で
編集欄を見分けていたが、この3図種は `mm-edit-text` / `xy-edit-values` /
`sk-edit-from` で、どれも表に無かった。

r18 のコメントには既に

> 図種によって label / name / id と名前が違うので、`-label` 決め打ちだと
> 直したあとも検出し続ける

と書いてある。**表を広げただけで、表を持つのをやめていなかった。**
r11 で「関数名の表を持つと必ず漏れる」を学んだのに、この検査には
その規約が付いてこなかった (前ラウンドと同じ形で3回目)。

名前ではなく振る舞いで見る形に変えた —— パネルの中の文字入力欄に届き、
そこへ打鍵して本文が変わるか。

変異 (入力欄に `tabindex="-1"` を付ける) で6件検出することを確認した。

## V2: 待ち時間の上限は効いていなかった

前ラウンドで入れた「前回の描き直しにかかった時間だけ待つ」の上限 800ms が
実務で長すぎないかを測った。

打ち終わってから図が最新になるまで (1366x768):

| 要素 | 最後の打鍵から | 描き直し1回 | 内訳 |
|---|---|---|---|
| 10 | 2ms | 19ms | 待ちなし |
| 50 | 3ms | 52ms | 待ちなし |
| 200 | 584ms | 211ms | 待ち211 + 描き直し211 + 一覧162 |
| 400 | **1824ms** | 508ms | 待ち508 + 描き直し508 + **一覧約800** |

**上限800msはほとんど効いていない** (400要素でも実際の待ちは508ms)。
1.8秒のうち半分近くは**一覧の作り直し**で、これは UI-032 / G6 と同じ根。

上限を下げても効くのは600要素を超えてからで、そこでは描き直し自体が
1秒を超えるので効果は薄い。**待ち時間ではなく一覧が支配的**という結論。

## 数字

| | 前 | 後 |
|---|---|---|
| r18 の網羅 | 11/21 | **21/21** |
| 未確認事項 | 4件 | 2件 |
| 検査の誤り (通算) | 16 | 17 |

---

# ラウンド37 — 網羅率の低い4観点は、全部「表を直書きしていた」だけだった

## 仮説と結果

前ラウンドの診断でこう書いた。

> r18 は K1 が失敗した時点で K2〜K5 を丸ごと捨てていた。
> これは他の観点にも同型がある可能性がある (r3 が 10/21、r5 が 6/21、
> r8 が 4/21、r10 が 6/21、r15 が 5/21)。

**当たっていたが、原因は違った。** 打ち切りではなく、**図種の一覧を直書き**していた。

| 観点 | 何を直書きしていたか | 前 | 後 |
|---|---|---|---|
| r5 ユーザー観点 | 図種6つ | 6/21 | **21/21** |
| r10 往復整合 | 図種6つ | 6/21 | **21/21** |
| r15 状態の持ち越し | 図種5つ + **入力欄のセレクタ** | 5/21 | **21/21** |
| r8 スケール耐性 | 図種ごとの**巨大文書の生成器** | 4/21 | **19/21** |

r1 / r2 / r6 / r11 / r12 / r14 / r18 は契約ベースへ書き換え済み (C24/C26/C27/C30)。
**この4つには書き換えが届いていなかった。**

r8 は生成器を手書きしていたので、モジュール自身の `operations.add` を
繰り返して育てる形にした (r23 が同じやり方で21図種に追加を通している)。
mindmap (7要素) と sankey (5要素) は20要素まで育たないので対象外として数える。

## 変異で確認

| 観点 | 変異 | 検出 |
|---|---|---|
| r5 | Undo が2段まとめて戻る | 20件 |
| r10 | 書き出しでコメントを落とす | 63件 |
| r15 | 追加フォームを図種を跨いで持ち越す | 21件 |
| r8 | 一覧を30件で打ち切る | 36件 |

r15 の1回目の変異は**意味的に効いていなかった** (snapshot は同一呼び出し内で
完結するので、図種を跨げない)。変異が入ったことを確かめる手順で気付いた。

## 検査の誤りが4件 (通算 18〜22)

r8 を広げたら6件出たが、**5件が誤報**だった。

| # | 内容 |
|---|---|
| 19 | S4 の同一性を id の正規表現で見ていた。**自動採番の id (`__s_99` / `__p_99` / `__bar_99` / `__f_99`) は本文に現れない**ので、4図種で「消えたのは []」になる |
| 20 | 育てた gantt は100要素中96個が**セクション**。タスク一覧の行数と比べて「4件しか出ない」と誤報 |
| 21 | gitGraph の `data-element-id` は**行番号** (101)。要素の id (S99) では当たらず「削除ボタンに辿り着けない」 |
| 22 | 一覧の末尾は多くの図種で**エッジ**。要素だけ比べると「1つ消したのに何も減っていない」 |

いずれも「id とは何か」をこちらが勝手に決めていたのが原因。
**画面が付けている id を画面から取る**、**関係も数に入れる**で解消した。

## 本物が1件 (A105)

gantt のセクション行だけ `ma-list-row` と `data-element-id` を持っていなかった。
他20図種は持っている。見た目に影響しないので誰も気付かず、
**検査からも数え落ちていた**。揃えた (実機で画面と削除の動作を確認)。

## 一度直した抜けを、別の検査で作りかけた

r5 を広げたとき「重ね合わせが無い図種は `continue`」と書いた。
これは r18 が K1 の失敗で K2〜K5 を丸ごと捨てていたのと**同じ形**で、
前ラウンドで直したばかりだった。U1〜U3 だけを飛ばす形に直した。

> 直した抜けは、直した場所にしか残らない。
> **同じ手が他の場所で再発するかどうかは、書いた本人でも防げない。**

## 数字

| | 前 | 後 |
|---|---|---|
| 網羅 21/21 の観点 | 15 | **19** |
| 網羅 10/21 未満の観点 | 4 | **0** |
| 検査の誤り (通算) | 17 | 22 |

---

# ラウンド38 — 「バックログ送り」の項目が、別の作業で自動的に期限を迎えた

## Y1 の答え

前ラウンドの未確認事項「mindmap (7要素) と sankey (5要素) は20要素まで育たず、
大規模時の挙動が未測定」を調べた。原因は**記録済みの G4** だった。

    mindmap     parse は kind='node' / add は 'child' と 'sibling' しか受けない
    sankeyBeta  parse は kind='node' / add は 'flow' しか受けない

G4 にはこう書いてあった。

> 現時点で実害はない — `operations.add` を契約経由で呼ぶのは検査とテストだけで、
> app.js は各図種のパネルから直接呼んでいる
> 復活条件: 契約経由で `add` を呼ぶ経路 (複写・貼り付け・一括生成など) を作る時点

**前ラウンドで r8 を「モジュール自身の add で文書を育てる」形に書き換えたとき、
その経路を作っていた。** 書いた本人が気付かないまま復活条件を満たしていた。

> 復活条件付きでバックログに送った項目は、**別の作業が条件を満たしたときに
> 自分から知らせてはくれない**。今回は未確認事項を追いかけて偶然たどり着いた。

## 直した (A106)

既存の呼び出しは変えず、**一覧に出ている kind を別名として受ける**。

    mindmap  kind='node' → 親を指定していなければ根の子として足す
    sankey   kind='node' → 既存のノードからの流れを1本足す
             (ノードは流れの端点としてしか存在しない)

r8 の網羅が 19 → **21/21**。契約の kind は
`tests/contract-add-kind.test.js` で21図種すべて固定した。

## 検査の誤りが2件 (通算 23・24)

r8 の S4 は「消えたのは1つだけ」を求めていた。

**23件目**: sankey のノードを消すと流れも消える。これは**正しい実装**なのに
欠陥として報告しかけた。

**24件目**: では関係を一律で除こう、としたら、**関係そのものを消した4図種**
(requirement / block / C4 / architecture) が全部「巻き添え」になった。

    正: 対象が何かを決めてから、巻き添えを数える
    誤: 関係は巻き添えではない、と一律に決める

一覧の末尾は要素のことも関係のこともある。**消えたものから対象を読む**
形に直した (要素が消えていればそれが対象、関係だけなら関係が対象)。

## 数字

| | 前 | 後 |
|---|---|---|
| ユニット | 1063 | 1084 |
| 網羅 21/21 の観点 | 19 | **20** |
| 未確認事項 | 3件 | 2件 |
| 検査の誤り (通算) | 22 | 24 |
| バックログ (G区分) | 4件 | 3件 (G4 を解消) |

---

# ラウンド39 — 「誰も見張っていない」を機械に見張らせた

## 前ラウンドで見つけた穴

G4 は6ラウンド前に「復活条件付きでバックログ送り」にした項目だった。

    復活条件: 契約経由で `add` を呼ぶ経路 (複写・貼り付け・一括生成など) を作る時点

**その経路は、前ラウンドで私自身が r8 を書き換えたときに作っていた。**
気付いたのは、無関係な未確認事項 (mindmap / sankey が育たない) を
追いかけた偶然だった。

> 棚卸しに条件を書いても、**条件の監視は誰もしていない**。
> 書いた本人ですら、自分が条件を満たしたことに気付かない。

## 直し方

「復活条件」を機械で判定するのは無理な項目がある (「訳す範囲を決めた時点」)。
そこで**前提**の側を見張る — 各項目の「なぜここにあるか」= 先送りしてよい理由が、
今も成り立っているか。

| # | 前提 | 見張り方 |
|---|---|---|
| G1 | class / er / state が `move: false` のまま | 3モジュールを grep |
| G2 | パネル構成は MOC の合意が要る | 資料の有無を出す [人の判断待ち] |
| G3 | 複写が gantt だけに閉じている | 分岐を grep [人の判断待ち] |
| G5 | 欄名に英語が残っている | terms.js の実行を確認 [人の判断待ち] |
| G6 | 一覧に仮想化が無い | 上限 / virtual の有無を grep |

**人が決める項目は自動化できない。** できるのは「どちらなのかを項目ごとに
宣言させる」ところまで。宣言の無い項目を足すと FAIL する — これで
「また誰も見張らない項目ができる」のを止める。

## 変異で確認 (16回目)

| 変異 | 検出 |
|---|---|
| er を `move: true` にする | G1 の前提が崩れたと報告 |
| 一覧に `slice(0, 50)` を入れる | G6 の前提が崩れたと報告 |
| 前提の検査が無い G7 を棚卸しに足す | 「前提の検査が無い」と報告 |

## ゲートが10項目になった

    G1/G3 ユニット / G2 実描画 / G4/G5 e2e / LOOP 指摘 / COV 網羅率 /
    G7 権利 / BACKLOG 未着手 / DOC 記録の対応 / REC 主張の検証 /
    **PREMISE バックログの前提**

DOC・REC・PREMISE の3つは**成果物ではなく棚卸しを検査している**。
「プランが現実を写す測定器」であるために、機械で照合できる部分を機械に渡した。

## 数字

| | 前 | 後 |
|---|---|---|
| ゲートの項目 | 9 | **10** |
| 見張られていないバックログ項目 | 5 | **0** |
| 検査の誤り (通算) | 24 | 24 (今回は0) |

---

# ラウンド40 — 待機中に測ったら、未測定の観点から1件出た

## 宣言どおり待機した

前ラウンドの診断で「待機中はゲートを定期実行して回帰を確認するだけ。
新しい検査も観点も棚卸しの整備も足さない」と宣言した。

ゲート10項目すべて合格・回帰なし。**新しい検査は足していない。**

## 測っていなかった観点を測った

批判レビューは「8観点すべてを走査」する規約なので、
**一度も測っていなかった観点4 (壊れたときのメッセージ)** を21図種で測った。

### 原因を名指しするか — 27/27

閉じ括弧を落とす / 引用符を1つ落とす / 知らない語を足す、の3通りで壊した。
**壊れた27回すべてで日本語の説明が出た**。生の mermaid エラーが
そのまま出たケースは0件。

### 打った行が黙って消えるか — 2図種

`    ZZ不明ZZ` を1行差し込んだときの挙動:

| | 図種数 | 図種 |
|---|---|---|
| 図か一覧に出る (無視されていない) | 5 | flowchart / state / er / block / kanban |
| エラーになる (気付ける) | 14 | gantt / sequence / requirement / mindmap / gitGraph / pie / journey / quadrant / xychart / sankey / C4 / packet / architecture / radar |
| **黙って消える** | **2** | **classDiagram / timeline** |

実測で確かめた: mermaid はこの2図種で未知の行を**描けるが無視する**。
アプリは要素に数えていない (捏造していない = r13 の規約どおり正しい) が、
**利用者には何も伝わらない**。

## 直さずに記録した理由

伝えるには「パーサが要素にしなかった行」を警告することになる。
しかし `note` / `direction` / `accTitle` / css の割当てなど、
**パーサが持たないが意味のある行**を誤って「無視された」と言う危険がある。
どこまでを警告するかは価値判断なので、私の一存で決めない。

G7 として棚卸しへ。**前ラウンドで作った PREMISE のおかげで、
前提の検査を書かないと FAIL する** — 記録しただけで見張られない状態には
もうならない。仕組みが1ラウンドで効いた。

## 数字

| | 前 | 後 |
|---|---|---|
| 実測した観点 (8つのうち) | 7 | **8** |
| バックログ (G区分) | 6 | 7 (G7 追加、いずれも監視下) |
| 新しい検査 | — | **0 (宣言どおり)** |

---

# ラウンド41 — 思いついた判定式を実測したら外れた

## 前提を疑った

前ラウンドで G7 (未知の行が黙って消える) を「誤検出の危険があるので
人の判断待ち」として記録した。その前提を自分で疑ってみた。

> 一覧 (allowlist) を持たない判定式があるのではないか。
> **その行を消しても図が1ピクセルも変わらないなら、その行は効いていない。**

構成上、誤検出が起きないはずだった。実測した。

| 図種 | 差し込んだ行 | 消しても図が同じ | 判定 |
|---|---|---|---|
| classDiagram | `ZZ不明ZZ` (意味なし) | いいえ | 効いている ← **見逃し** |
| classDiagram | `direction LR` | いいえ | 効いている |
| classDiagram | `note "設計メモ"` | いいえ | 効いている |
| classDiagram | `%% コメント` | いいえ | 効いている ← **見逃し** |
| timeline | `ZZ不明ZZ` (意味なし) | はい | 効いていない |
| timeline | `accTitle: 開発の流れ` | はい | 効いていない ← **誤検出** |
| timeline | `%% コメント` | はい | 効いていない |

**両方向に外れた。**

- classDiagram は**コメント行を入れても SVG が変わる**。何も検出できない
- timeline は `accTitle` (読み上げ用の題) を「効いていない」と誤検出する。
  **視覚に出ないが意味のある行**がある以上、図の比較では分けられない

## 記録した

G7 の「なぜここにあるか」に実測を追記した。前提が実測で裏付けられた形になる。

> 思いついた判定式は、**実装する前に実測すると外れることがある**。
> 今回は「構成上、誤検出が起きないはず」という理屈が2箇所で崩れた。
> 記録しておかないと、次のラウンドで同じことを思いつく。

## 数字

| | 前 | 後 |
|---|---|---|
| 実装した機能 | — | 0 (仮説が外れたため) |
| 記録した実測 | — | 7通り |
| ゲート | 10項目合格 | 10項目合格 |

---

# ラウンド42 — 予告した期限が来たので、待つのをやめて凍結した

## やったこと

前サイクルで予告した V1〜V3 を実行した。**削除ではなく凍結** —
削除リストで管理下に置き、復活条件を残す。

| 凍結した項目 | 待った長さ | 既定の扱い |
|---|---|---|
| F5 ADR-027 の採番 | 22サイクル | ドラフトのまま残す |
| F6 パネル構成の案 + UI-022/023/032 | 11サイクル | **案D(現状維持)** |
| G3 UI-012 の複写 | 11サイクル | gantt のみのまま |

## なぜ待つのをやめたか

**同じ質問を22回繰り返すのは PL の仕事ではない。**

G1 (class/er/state の move) では「3回回答が無ければ予告どおり現状維持として
扱う」と期限を切って実行した実績があるのに、ADR-027 と MOC には期限を
切っていなかった。**自分の運用が一貫していなかった。**

私が決めないこと (規約上できない): ADR の採番 / GUI 構成の設計選択 / 複写の仕様。
私が決めること (PL の職責): それらが決まらないまま**能動的な計画に載せ続けるか**。

## 凍結が「忘れる」ことにならないように (C60)

前ラウンドで G区分 (バックログ) の前提を機械で監視するようにした。
今回、**H区分 (削除リスト) の復活条件も監視対象に入れた**。

    F5  docs/adr/README.md に ADR-027 が現れたら → 復活を報告
    F6  MOC に「採用: 案A」等が書かれたら       → 復活を報告
    全項目  復活条件が空なら FAIL (黙って消えたのと同じ)

変異3種で確認した。MOC の末尾には**再開の手順を1行で書いた** —
`採用: 案A` と書き足せば、機械が検知して削除リストから戻す。

> 凍結は「捨てる」ことではない。**戻ってくる経路を機械に持たせて初めて、
> 凍結と放置が区別できる。**

## 残っている「人の判断待ち」

| # | 内容 | 待った長さ |
|---|---|---|
| G5 | UI-030 欄名の日英混在 (訳す範囲) | 10サイクル |
| G7 | UI-036 黙って消える行 (警告の範囲) | 2サイクル |

G5 は F6 / G3 と同じ長さ待っているが、**今回の予告には含めていなかった**ので
凍結していない。予告していないものを黙って凍結するのは、
「黙って消さない」に反する。次サイクルで予告する。

## 数字

| | 前 | 後 |
|---|---|---|
| 承認待ち (F区分) | 2件 | **0件** |
| 削除リスト (H区分) | 3件 | **6件** (すべて復活条件つき・監視下) |
| 能動的な計画に載っている項目 | 3件 (すべて着手不能) | **0件** |
| ゲート | 10項目合格 | 10項目合格 |

---

# ラウンド43 — 「決定」と「その書き方の説明」が見分けられていなかった

## PR #16 を作成した

20コミット (ラウンド30〜42) が未マージのままだったので、
**承認いただける形 (PR番号)** にした。https://github.com/KawanoMomo/mermaid-assist/pull/16

**master は変更していない。** PR は「マージしてほしい」という要求であって、
main への書き込みではない。マージの可否は利用者が判断する
(PR番号を明示した一問一答が規約)。

## 予告した (G5 / G7)

前ラウンドで「G5 は予告に含めていなかったので凍結しない。次サイクルで予告する」
と書いた。予告した。

| # | 内容 | 期限 | 既定 |
|---|---|---|---|
| G5 | UI-030 欄名の日英混在 (訳す範囲) | 次サイクル | 現状維持 (英語のまま) |
| G7 | UI-036 黙って消える行 (警告の範囲) | 次々サイクル | 現状維持 (警告を出さない) |

G7 を1サイクル遅らせたのは、**記録したのが前サイクル**だから。
記録した直後に凍結の期限を切るのは、記録の意味がない。

## 検査の誤り25件目 — 説明文に反応した

前ラウンドで作った「復活条件の監視」が、いきなり誤検出した。

MOC の末尾に**再開手順**を書いた。

    選ぶときは、この資料に次の1行を書き足してください。
        採用: 案A

この**例文**を「案が選ばれた」と検知して、「復活した」と報告した。

> **指示と決定は見た目が同じ。** 区別できるのは**置かれる場所**だけ。

行頭に置かれたものだけを決定とみなす形に直した (`/^採用[:：]\s*案[A-C]/m`)。
MOC 側の説明も、字下げやコード囲みでは検知しないことを明記した。

作った翌ラウンドに自分の書いた文書で誤検出したのは、
**「機械に見張らせる」と「見張る対象を自分で汚す」が同時に起きた**形だった。

## 数字

| | 前 | 後 |
|---|---|---|
| 未マージのコミット | 20 (PR なし) | 20 (**PR #16**) |
| 期限を予告した項目 | 0 | 2 (G5 / G7) |
| 検査の誤り (通算) | 24 | 25 |
| ゲート | 10項目合格 | 10項目合格 |

---

# ラウンド44 — 打鍵数を数えたら、思っていたのと逆だった

## G5 を予告どおり凍結した

前ラウンドで「次サイクルまでに訳す範囲の指示が無ければ凍結する」と予告した。
指示が無かったので凍結し、H区分へ移した。`terms.js` の計数は残る。

能動的な計画に載っている項目は **0件** になった (すべて監視下の凍結か完了)。

## 打鍵数を数えた (観点1 / 観点6)

一度も数えていなかった「1要素を足すのに何打鍵か」を実測した。

| 経路 | 1ノード | 10ノード | 1ノードあたり |
|---|---|---|---|
| パネルの追加フォーム (キーボードのみ) | 11打鍵 | 90打鍵 | **9打鍵** |
| エディタに DSL を直接書く | 17打鍵 | 132打鍵 | 13打鍵 |

**思っていたのと逆だった。** 「DSL を直接書くほうが速い」と暗に想定していたが、
パネル経路のほうが **1.4倍速い**。`A` キーで追加フォームの先頭欄へ飛び、
Tab で送って Enter で確定できる。10回連続で試して
**フォーカスが戻らなかった回数は0**だった。

## 結ぶ側だけが要素数に比例する (G8 / UI-037)

足す側は9打鍵で一定なのに、**結ぶ側は要素数に比例する**。

| ノード数 | From の選択肢 | 末尾まで↓ | 1本結ぶまでの打鍵 |
|---|---|---|---|
| 5 | 5 | 4 | 16 |
| 20 | 20 | 19 | 46 |
| 100 | 100 | 99 | **206** |

端点の選択が絞り込みの無い `<select>` で、選択肢がノード数そのままだから。

## 測れないことを測れないと言う

`<select>` はブラウザ既定で**先頭一致**が効く。効くならこの数字は絵に描いた餅になる。
実測しようとしたが、`終了` と打っても先頭のまま動かなかった。

**アプリの欠陥として報告する前に、対照実験をした。**

    素の <select> (アプリと無関係な空ページ) で「終了」と打つ → 動かない

**この検証環境では先頭一致そのものが効かない。** アプリの問題ではなく、
測定環境の制約だった。危うく「先頭一致が効かない」と報告するところだった。

> 測れないものを根拠に直し方を決めない。
> **「測れなかった」を「欠陥」に格上げしない。**

G8 として記録し、復活条件を「実機のブラウザで先頭一致の効きを測った時点」とした。

## 数字

| | 前 | 後 |
|---|---|---|
| 能動的な計画の項目 | 0 | 0 |
| バックログ (G区分・監視下) | 2 | 2 (G5 凍結 / G8 追加) |
| 削除リスト (H区分) | 6 | 7 |
| 実測した観点 | 4 のみ | **1 / 4 / 6 / 7** |

---

# ラウンド45 — 観点8の実測を更新した。差分は増えていなかった

## なぜ測り直したか

観点8 (ワークフロー適合 = git 差分の大きさ) の実測はラウンド31で、
**14ラウンド前**だった。その間に引用符の付与・並べ替えの作り直し・
packet の番号振り直しを入れている。**差分が増えていないか**を確かめる必要があった。

## 結果: 21図種 × 4操作の差分行数

| 操作 | 中央値 | 最大 |
|---|---|---|
| 追加 | 1行 | 4行 (requirement: 属性2行+ブロック) |
| 改名 | 2行 | 4行 (er: 参照側も書き換わる) |
| 削除 | 3行 | **9行** (requirement) |
| 並べ替え | 2行 | 4行 |

**6行を超えたのは2図種だけ**で、どちらも中身を見たら最小限だった。

    packetBeta の削除 (7行)
      -96-99: "Data Offset"      +96-101: "Reserved"
      -100-105: "Reserved"       +102: "URG"
      -106: "URG"                +103: "ACK"
      -107: "ACK"
      → ビット位置が中身なので、消したら後ろを詰めるしかない

    requirementDiagram の削除 (9行)
      element ブロック4行 + 前後の空行 + 関係行1行
      → ブロックを持つ図種では、ブロックの行数がそのまま差分になる

**私の変更で差分は増えていない。** 引用符は必要なときだけ付き、
並べ替えはブロック単位で入れ替わるので行数が増えない。

## 観点8 は指摘なし

この観点はこれまで**賞賛**として扱ってきたが、14ラウンド後も同じだった。
「賞賛は測り直さないと賞賛のままだと思い込む」ので、測り直した価値はあった。

## 数字

| | |
|---|---|
| 実測した観点 (直近2サイクル以内) | 1 / 4 / 6 / 7 / **8** |
| 実測が古い観点 | 3 (terms.js が毎回数えている) / 5 (r5・r17 が毎回見ている) |
| ゲート | 10項目合格 |

---

# ラウンド46 — 規約の漏れを2つ見つけ、姉妹プロジェクトで同型の欠陥が出た

## 監視の巡回で見つけた漏れ

製品側の作業が尽きたので、CLAUDE.md の規約を自分に照らした。**2つ守れていなかった。**

| 規約 | 守れていなかった長さ |
|---|---|
| 「アーキテクチャレベルの判断をしたら、その場で `docs/adr/drafts/` にドラフトを残す」 | **16ラウンド分** |
| 「まとまった変更が完了したら ECN 化し、姉妹プロジェクトへの影響分析を提案する」 | **16ラウンド分** |

**製品の欠陥は25観点が毎回見張っているのに、規約の遵守は誰も見張っていなかった。**

## ADR ドラフト5件 (前ラウンド) と ECN 3件 (今回)

    ECN-032  契約経路の Blocker 3件と日本語識別子の扱い (A90〜A106)
    ECN-033  実描画の網を「追加」「並べ替え」に広げ、網羅率を底上げ (C44〜C58)
    ECN-034  棚卸しを機械が検査する層と、滞留への期限 (C59〜C62)

## 姉妹プロジェクトで同型の欠陥が出た

06_PlantUMLAssist に影響分析を作り、**実測した**。

    src/modules/sequence.js:1497
      moveUp: function(text, lineNum) {
        if (lineNum <= 1) return text;                    ← 05 と同一のコード
        return window.MA.textUpdater.swapLines(text, lineNum, lineNum - 1);
      }

title の無い文書で先頭の participant を上へ動かすと:

    前              後
    1: @startuml    1: actor User
    2: actor User → 2: @startuml     ← **PlantUML はこれを描けない**

**05 で直した欠陥が、姉妹プロジェクトにそのまま残っていた。**

一方 **A104 (打鍵ごとの再描画) は 06 では起きない** — `RENDER_DEBOUNCE_MS = 150`
で固定の待ちがあるため。05 は rAF だけだったので合流しなかった。
**同じ設計から出発しても、片方だけ直っていることがある。**

## PlantUML では 05 の直し方がそのままでは使えない

05 は「1行目 (図種の宣言) を守る」形で直した。PlantUML は
**`@startuml` / `@enduml` で囲む構造**なので、囲みの内側だけで動かす必要がある。

**実装はしていない。** 06 は現在の作業対象ではないので提案に留めた。

## 数字

| | |
|---|---|
| ADR ドラフト | 1 → **6** (すべて未採番) |
| ECN | 31 → **34** |
| 姉妹プロジェクトへの影響分析 | 1 → **2** |
| 06 で実測した該当 | **1件 (再現確認済み)** |

---

# ラウンド47 — 観点2を数で測ったら、読めないボタンが9個あった

## 自分に都合のよい例外を作りかけた

「検査基盤への新規投資0」と7回宣言しているのに、前ラウンドで
「規約の遵守を機械に見張らせる検査を足す」ことを考えた。
**PREMISE も同じ理屈で足している。2回目は例外ではなく方針転換になる。**

足すのをやめ、代わりに未測定の観点を測った。

## 観点5 — 回復の境界 (指摘なし)

| 操作 | Ctrl+Z |
|---|---|
| 図種を切り替える | **1回で戻せる** |
| 一覧の ✕ で要素を消す | 1回で戻せる |
| エディタで全選択して打ち消す | 1回で戻せる |

最初 5回まとめて押して「戻せない」と読んだが、**戻しすぎて起動時のひな形まで
行っていた**だけだった (検査の誤り27件目)。1回ずつ押して止める形に直した。

`confirm` はアプリ全体で**1箇所だけ** (図種切替)。
「Undo で戻せるものは確認しない」という方針が一貫していた。

## 観点2 — 数で測ったら9個見つかった (A107 / UI-038)

パネルのボタンを21図種で数えた。

| | 個数 |
|---|---|
| 文字のあるボタン (「+ ブロック追加」など) | 164 |
| 記号だけのボタン (`+` `✕` `↑` `↓`) | 114 |
| **うち説明 (title) が無いもの** | **9** |

9個はすべて `+` と `×` で、サブグラフ・状態・複合状態・クラス・名前空間・
セクション・ブロック・エンティティの追加と、timeline のイベント削除だった。

**他の追加ボタンは文字を持つので、この9個だけが読めなかった。**

`title` を付けるだけなので配置は変わらない (実機で確認)。

## 画面に出ていないものは画面からは数えられない

DOM 走査では8個しか見つからなかった。ゲートに入れるためソースを走査したら
**9個目**が出た — timeline のイベント削除ボタンで、
**イベントを持つ期間を選んだときだけ描かれる**ので、既定のひな形では出ない。

> 「画面を数える」検査は、**そのとき画面に出ているものしか数えない**。
> 条件付きで描かれるものは、ソースを数えないと漏れる。

## 数字

| | 前 | 後 |
|---|---|---|
| 説明の無い記号ボタン | 9 | **0** |
| ゲートの検査項目 (terms 内) | T1 / T2 | T1 / T2 / **T3** |
| 検査の誤り (通算) | 26 | 27 |

---

# ラウンド48 — 新プラン N2 の初回で、Git 運用に直接効く欠陥が出た

## N1: 外向き文書の鮮度 — 一致

PR #16 の説明を前ラウンドで実態に更新した。今回照合して一致 (27コミット)。

## N2: 未測定の角度を1つ測る — 改行コード

21図種 × 4操作で「CRLF の文書を編集したら改行コードが保たれるか」を測ったら
**21図種すべてで混在**した。しかし**そのまま報告しなかった。**

    実アプリで CRLF の文書を入れて textarea の値を読む
      → CRLF 0 / LF 4  = **ブラウザが LF に正規化する (HTML の仕様)**

**アプリは CRLF を保持しないので、契約に CRLF を渡す経路が UI に無い。**
21図種の結果は「起きない条件で測った数字」だった。

### より重要な経路があった

しかし**ファイルを開いて保存する**経路は別だった。

| | 直す前 | 直した後 |
|---|---|---|
| CRLF を開く → 1行足す → 保存 | **CRLF 0 / LF 6** | CRLF 5 / LF 0 |
| LF を開く → 1行足す → 保存 | CRLF 0 / LF 5 | 変わらず |

**Windows で作った .mmd を1つ触るたびに、git の差分がファイル全体になる。**
数十〜数百枚を Git 管理する使い方では、レビューで中身が読めなくなる。

`matchEol` という関数が `src/core/text-updater.js` に**書かれていたが、
21モジュール中 c4 の2箇所でしか呼ばれていなかった** — 書いて配線しなかった形。

今回は根の方 (開く / 保存) で直した。開いたファイルの改行コードを覚えて書き戻す。

## 検証の経路を間違えていた

最初の往復の測定は `editor.value` に直接代入していたので、
**「開く」経路を通っておらず**、直したのに直っていないように見えた。
`<input type=file>` に実ファイルを渡す形に直したら、前後の差がはっきり出た。

> 直したものを測るときは、**利用者が通る経路を通す**。
> 内部に直接代入する測り方は、直した場所を迂回することがある。

## 前ラウンドの診断の答え合わせ

ラウンド39で「製品側の作業在庫が尽きた」と診断した。**誤りだった。**
その後の測定で A105 / A106 / A107 / **A108** が出ている。

> 「在庫が尽きた」ではなく「測っていない場所が残っていた」。
> **新プラン N2 (未測定の角度を毎サイクル1つ測る) は初回で欠陥を出した。**

## 数字

| | 前 | 後 |
|---|---|---|
| e2e | 424 | **427** |
| 欠陥 (A区分) | 107 | **108** |
| 測定から出た欠陥 (直近9ラウンド) | 3 | **4** |
