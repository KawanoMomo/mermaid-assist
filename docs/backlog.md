# 残項目の棚卸し

「すべて完了」を言うには、何が残っているかが数えられないといけない。
このセッションで扱った項目を全部並べ、状態を書く。**未着手を完了扱いにしない。**

状態: `済` 完了 / `残` 未着手 / `見送` 意図的に着手しない (理由を書く)

## A. 発見して修正した欠陥

| # | 内容 | 状態 |
|---|---|---|
| A1 | block: 一覧の ✕ が押した行と違うブロックを削除 | 済 |
| A2 | 6モジュールで ID リネームが参照側に伝播しない | 済 |
| A3 | block: ラベル編集が前方一致する隣を破壊 / ID欄が死んだ入力 | 済 |
| A4 | flowchart: 削除が押していないノードを消す | 済 |
| A5 | 保存名が常に untitled / 未保存ガード無し / ステータスが gantt 専用 | 済 |
| A6 | 図種切替でズームが残る / フィットが横幅しか見ない | 済 |
| A7 | オーバーレイ: クリック無反応 / 矩形が同座標に重なる / ラベル照合の衝突 | 済 |
| A8 | Undo が1キーストローク単位 / Ctrl+Z の意味がフォーカスで変わる | 済 |
| A9 | キーボードで要素間を移動できない | 済 |
| A10 | class / er / state の削除が押した要素を消さない | 済 |
| A11 | 選択が消えた要素に残る / リネームで迷子になる | 済 |
| A12 | 接続モードが実装されていない (仕様にはある) | 済 |
| A13 | 接続モード: 図種切替で残る / Escape で抜けられない / 表示が無い | 済 |
| A14 | 接続モード: 矢印キーと競合 / 始点が消えても線が引ける | 済 |
| A15 | date-utils が null で 1970年、不正入力で例外 | 済 |
| A16 | gantt: duration 記法の図でドラッグが効かない | 済 |
| A17 | mindmap / packet / architecture の削除が図を壊す | 済 |
| A18 | er: 関係にラベルが必須なのに省略していた | 済 |
| A19 | gantt: 起動時 68% 縮小 / 概観でドラッグ不能 / 軸に年が出ない | 済 |
| A20 | gantt: 追加後の自動選択が連続入力を潰す | 済 |
| A21 | package.json のライセンス表記が LICENSE と不一致 | 済 |
| A22 | sequence: participant 削除が宣言行しか消さず、図には残る | 済 |
| A23 | gantt: プロパティ欄にタスク一覧が無く、100件で破綻 | 済 |
| A24 | 一覧行の ✕ に説明が無く何が消えるか読めない | 済 |
| A25 | 移動操作の語が gantt だけ「上に/下に」 | 済 |
| A26 | requirement: 末尾の要素を消すと空行が残り差分が汚れる | 済 |
| A27 | mindmap: 形状記号を含むラベルの前半が黙って消える | 済 |
| A28 | block: 引用符を含むラベルで parse 失敗 | 済 |
| A29 | sequence: # 以降が実体参照として食われる | 済 |
| A30 | エラー表示が拡大率のまま出て右端で切れる | 済 |
| A31 | flowchart/state: ラベル欄・ID欄・形状が無言で効かない (Blocker) | 済 |
| A32 | flowchart: 記号を含むラベルで parse 失敗 / 引用符が落ちる | 済 |
| A33 | kanban: 同名カラムでカードが選んだ先と違う場所に入る | 済 |
| A34 | block: style/classDef/class/click を幽霊ブロック化 | 済 |
| A35 | block: 菱形 {"..."} が2ブロックに割れる | 済 |
| A36 | flowchart: o--o / x--x / <--> で幽霊ノード (`A o`) | 済 |
| A37 | flowchart: 直上がエッジ行だと ↑ が無言で空振り | 済 |
| A38 | 非 gantt で Ctrl+A / Ctrl+C / Delete が例外または無反応 | 済 |
| A39 | 打鍵途中の構文エラーで図が丸ごと消える | 済 |
| A40 | 未保存のまま離脱しても警告が無い | 済 |
| A41 | Ctrl+Shift+Z が Undo 側に吸われ Redo にならない | 済 |
| A42 | CRLF 文書への挿入で改行コードが混在 | 済 |
| A43 | 上限超過で「OK」のまま空同然の画像を書き出す (UI-101) | 済 |
| A44 | エラー中の Export が直前の図を黙って出す (UI-103) | 済 |
| A45 | 追加フォームが全図種で Enter 確定できない (UI-102) | 済 |
| A46 | gantt の追加が日付未入力で無言に拒否される | 済 |
| A47 | timeline: accTitle / accDescr を「期間」として拾い幽霊が出る | 済 |
| A48 | mindmap: 存在しない id を渡すとルートガードを素通りして子ごと全削除 | 済 |
| A49 | mindmap: 子が無いルートまで消せない (ガードが根拠より広い) | 済 |
| A50 | 保存してもタイトルの未保存マーク `●` が消えない (UI-201) | 済 |
| A51 | 一時メッセージ中はタイトルが更新されない (早期returnの先) | 済 |
| A52 | 図種を切り替えて戻すと追加フォームの入力が残る | 済 |
| A53 | class / er の名前を GUI から変えられない (入力欄が無い) | 済 |
| A54 | gantt.parse が elements を返さず、18観点すべてから素通りしていた | 済 |
| A55 | class / er の名前変更が統一入口に繋がっていない | 済 |
| A56 | flowchart の統一入口が node と edge を取り違える | 済 |
| A57 | state / flowchart の統一入口の削除が id 認識を使っていない | 済 |
| A58 | requirement の更新が未知の field を本文に書き込み、図が parse を通らなくなる | 済 |
| A59 | erDiagram の parser が日本語エンティティ名を読めず、一覧が空になる (mermaid は描く) | 済 |
| A60 | state の統一入口が状態と遷移を取り違え、状態のラベル変更が矢印を書き換える | 済 |
| A61 | classDiagram のラベル欄がクラス名 (識別子) を書き換えていた (表示ラベルに対応) | 済 |
| A62 | gitGraph / journey / kanban / timeline が field を見ず、どんな field 名でも名前を書き換える | 済 |
| A63 | block / requirement が opts.id ではなく独自キーを要求し、契約どおりの呼出しが空振り | 済 |
| A64 | requirement の element ブロックが requirement 専用キーを受け、書くと図が壊れる | 済 |
| A65 | 描画に成功しても一部が落ちている場合、利用者に何も告げていなかった (警告帯を追加) | 済 |
| A66 | state のパネルの ID 欄にイベントが繋がっておらず、打鍵しても無反応だった | 済 |
| A67 | erDiagram の契約削除が関係行だけを消し、エンティティの宣言ブロックが残る | 済 |
| A68 | classDiagram の契約削除が id 認識の deleteClass を使っていない | 済 |
| A69 | sankeyBeta のノードを消しても、他の流れが名前を挙げていれば残る | 済 |
| A70 | maxEdges を上げておらず 500エッジで図が描けない (原因も「構文エラー」としか出ない) | 済 |
| A71 | Delete キーが Gantt でしか効かず、20図種で削除ごとにマウス往復が要る | 済 |
| A72 | 保存したファイルが改行で終わらず、Git の差分が毎回汚れる | 済 |
| A73 | 別セッションで書き出した SVG が同じ id を持ち、矢印マーカーも衝突する | 済 |
| A74 | PNG が等倍しか出せず、設計書で拡大・印刷すると字がつぶれる | 済 |
| A75 | パネルがはみ出していても、続きがあることが画面から分からない (13インチで15/21図種) | 済 |
| A76 | 本文を編集すると追加フォームの書きかけが消え、打鍵中の再描画で先頭文字も落ちる | 済 |
| A77 | クリップボードへのコピーが失敗しても画面に何も出ず、前の内容を貼り付けてしまう | 済 |
| A78 | kanban の id 付きカード (`t1[本文]`) が parser から消え、一覧にも重ね合わせにも出ない | 済 |
| A79 | kanban の重ね合わせが無く、図をクリックして選べない (見送りの理由が誤っていた) | 済 |
| A80 | sequence の宣言行を持たない参加者 (`A->>B:` だけの形) が一覧に出ない | 済 |
| A81 | sequence の参加者のラベルを変えるとメッセージの本文が書き換わる | 済 |
| A82 | sequence の参加者を消してもライフラインが図に残る (id 認識の削除が繋がっていない) | 済 |
| A83 | architecture のラベルが日本語を扱えない (引用符で通るのに「直せない制限」としていた) | 済 |
| A84 | mindmap の文字を変えると形状が失われる (契約入口が文字を形状として渡していた) | 済 |
| A85 | 一覧の絞り込みが文書を跨いで残り、別の図で一覧が空に見える | 済 |
| A86 | 絞り込みの件数を placeholder に書いており、絞り込み中だけ見えなかった | 済 |
| A87 | block-beta が日本語のブロック id を読めず、一覧も重ね合わせも空になる | 済 |
| A88 | ブロックを消すとリンクが残り、mermaid が幽霊ブロックを描く | 済 |
| A89 | モジュールの読み込み失敗が握り潰され、レビュアーが「20/20」= 満点に見えていた | 済 |
| A90 | requirementDiagram が日本語の要求名・要素名を読めず、一覧が空になる (mermaid は `requirement "受信要求"` を描く) | 済 |
| A91 | **「+ 要件追加」「+ エレメント追加」を押しただけで図が壊れる**。空の `id: ""` `text: ""` `type: ""` を出しており、mermaid が拒否する (行を省けば通る)。属性を空欄に戻す操作も同じ | 済 |
| A92 | requirement のエレメント改名が `operations.update` で無反応。kind の分岐が name より先にあり、type/docref しか知らない関数へ流れていた (パネル経路は直接呼ぶので動いていた) | 済 |
| A93 | gitGraph が日本語のブランチ名を裸で書き、図が壊れる (`branch "機能A"` なら通る)。checkout / merge の参照も引用符付きを読めなかった | 済 |
| A94 | quadrantChart の点の追加だけ引用符を通しておらず、日本語の点を1つ足すと図が壊れる。読む側も引用符を外しておらず、書き換えるたびに `""` が重なる | 済 |
| A95 | UI-026: エディタにカーソルがあるとヘルプを開く手段が一つも無い (`?` は本文に「?」が入るだけ)。テキスト中心のツールで利用者が一番長くいる場所だった → F1 をどこでも効く経路として追加 | 済 |
| A96 | UI-027: 大きい図の描画中、画面が固まったまま何も言わない (実測: 900要素で 6.1秒間フレームが1枚も来ず、ステータスは前の値のまま) → 塗ってから重い処理へ入るようにし「描画中 …」を出す。小さい図では出さない | 済 |
| A97 | UI-024/029: 「名前」を指す欄が9通りの表記 (ラベル/ID/Name/Title/Text/id/name/クラス名/エンティティ名…) → **ID / ラベル / タイトル の3語**に統一 (52箇所)。gitGraph だけ英語だったボタン文言と見出しをカタカナへ。sankey は追加側と編集側で端点の呼び方が割れていた | 済 |
| A98 | UI-028: Ctrl+Z を押しすぎると自分が編集を始める前 (起動時のひな形) まで無言で戻る。合図は工具列ボタンの無効化だけで、キーボードで押している人には見えない → 押すたびに「戻せる: N / やり直せる: M」をステータスに出す。底に着いたら「これ以上戻せません」 | 済 |
| A99 | ステータスバーの案内が「? ショートカット」のままで、**エディタでは効かない操作を案内していた** → 「F1 ショートカット」に変更 | 済 |
| A100 | **並べ替えの契約が図の宣言行を巻き込む**。`operations.moveUp` は行番号が1より大きいことしか見ておらず、先頭の要素を上へ動かすと `flowchart TD` などの宣言行と入れ替わって図が消える。er / requirement も同様。パネル経路 (flowchart の `_moveNodeStep`) は正しく、**契約経路だけが壊れていた (15例目)** | 済 |
| A101 | 並べ替えがブロックを分解する。requirement / class / er は要素が複数行を占めるので、行だけ入れ替えると属性が別の要素にくっつく → 要素が占める範囲ごと入れ替える | 済 |
| A102 | mindmap の並べ替えが親子関係を壊す (根が2つになる)。字下げが階層そのものなので、**同じ親の兄弟どうしを子孫ごと**動かす形にした | 済 |
| A103 | packet の並べ替えがビット番号を飛ばして mermaid に拒否される → 入れ替えたあと各欄の幅を保ったまま先頭から振り直す | 済 |
| A104 | UI-031: **人の速さで打つと1打鍵ごとに図全体を描き直す**。requestAnimationFrame は同じフレームの中しかまとめないため、200ms 間隔の打鍵では合流しない。実測 (10文字打つ間に UI が固まった合計): 200要素 4988ms / 400要素 **14718ms** → 前回の描き直しにかかった時間から待ち時間を決める形にし、200要素 524ms / 400要素 1409ms (約10分の1) | 済 |
| A105 | gantt のセクション行だけ一覧の規約 (`ma-list-row` / `data-element-id`) を持っておらず、**検査から数え落ちていた**。100要素の図で「一覧は4件しか出ない」と見えた。他20図種と揃えた (見た目は不変・実機で確認) | 済 |
| A106 | **G4 を解消**。`parse` が返す kind (`node`) を `operations.add` が受けず、mindmap / sankey は契約経由で足せなかった。r8 が文書を育てるために契約経由で add を呼ぶようになって表面化 (G4 の復活条件どおり)。既存の呼び出しは変えず、一覧の kind を別名として受ける。r8 の網羅が 19 → **21/21** | 済 |
| A107 | UI-038: 記号だけのボタン (`+` / `×`) 9個に説明 (title) が無く、何が起きるか読めない。実測 (21図種): 文字のあるボタン164個 / 記号だけ114個 / **うち説明が無いもの9個**。他の追加ボタンは「+ ブロック追加」のように文字を持つので、この9個だけが読めなかった → 説明を付けた (配置は不変・実機で確認)。`terms.js` の T3 でゲート化 (変異で確認) | 済 |
| A108 | UI-039: **CRLF の .mmd を開いて保存すると LF に変換され、git の差分がファイル全体になる**。`<textarea>` の値はブラウザが LF に正規化する (HTML の仕様) ので、何もしなければ必ず LF になる。実測 (開く→1行足す→保存): 直す前 CRLF 0 / LF 6、直した後 CRLF 5 / LF 0 → 開いたファイルの改行コードを覚えて書き戻す。e2e 3件で固定 (直す前に戻すと2件落ちる) | 済 |
| A109 | UI-040: 一覧を絞り込んだあと、その行へ行くのに**追加フォーム全体を通過する**。実測 (1366x768、40ノードを1行に絞る): 絞り込み欄から **Tab 14回**でようやく残った行の編集ボタンに着く。「探す→直す」は1日に何度も踏む流れ → 配置は変えず **Enter で残った最初の行へ飛ぶ**ようにした (14打鍵 → 1打鍵)。e2e 3件で固定 (変異で2件落ちる) | 済 |
| A110 | UI-042: **選んだ要素が本文の何行目か分からない**。実測 (1366x768、150ノードの図で「ノード120」= 122行目 を選択): エディタが見せているのは 1〜34行目、カーソルは151行目(末尾)、パネルに行番号は無し。DSL を直接編集する使い方では「選ぶ → その行を直す」が流れなので毎回自分で探すことになる → パネルに「N 行目へ」を出し、**押したときだけ**その行へ飛ぶ (自動スクロールはしない — 追加・削除のあとにも選択は起きるので、打っている最中に画面が飛ぶのは以前 gantt で問題になった形)。1か所 (`renderProps`) で21図種すべてに効く。e2e 3件で固定 (変異で2件落ちる) | 済 |
| A111 | A110 が **21図種中4図種で出ていなかった**。flowchart で1回確かめて全体に効くと思い込んだ形。`elements` しか見ておらず、gitGraph は選択の id が**行番号そのもの**、sankey は流れが `relations`、architecture はグループが**第3の入れ物 `groups`** にあった → 3つの入れ物と数字 id をすべて扱う形に直し、**21図種すべてを e2e で固定** (変異で検出) | 済 |
| A112 | ラウンド49 の「絞り込み欄で Enter → 残った行へ」が **gitGraph で効かない**。キーを押した瞬間に捕まえた行が、焦点を当てる時点で**文書から外れている** (実測: `attached=false`)。パネルの作り直しが図種で違い、flowchart では起きなかったので気付かなかった → **その場で捕まえず、当てる瞬間に取り直す**形にした。探索で元の実装は3回とも失敗・直した版は成功。**ただし e2e ではこの壊れ方を再現できておらず、回帰を捕まえる保証は無い** (検査の限界としてテストに明記) | 済 |
| A113 | **文書を入れ替えても、モジュールが覚えている状態が残る**。block は「最後に足した親グループ」を覚えており、**別の文書を開いても残っていた**。実務では図をまたいで命名が揃う (どの図にも `G1` がある) ので、開いた直後に追加を押すと**選んだ覚えのないグループの中に入る**。掃除する仕組みはあったが `resetTransientState` を実装しているのは gantt だけで、block は持っていなかった。さらに掃除の呼び方が「今のモジュール1つ」だったため、**図種の切替は切替*先*を掃除し、切替*元*は汚れたまま残る** (実測: block で追加 → ドロップダウンで flowchart → block のファイルを開く、で G1 が残る)。**どのモジュールが汚れているかを追うのをやめ、文書が入れ替わったら全モジュールを掃除する**形にした。変異2種で確認 | 済 |
| A114 | **A113 と同じ形が c4 にも残っていた**。block を直したとき、**残り19モジュールを数えていなかった** (1件直して掃き出さないのは既知の失敗型)。`src/modules/c4.js:16-17` は `lastAddKind` / `lastAddParent` を覚えており、`resetTransientState` を持たないので全モジュール掃除の対象外だった。**c4 は種類まで覚えるので block より重い** — 実測: 外部システム (`System_Ext`) を境界 `B1` に足したあと、同じ名前の `B1` を持つ別の図を開くと **種類も親境界もそのまま残り**、押すと `System_Ext(zz)` が `B1` の中に入った。**要素の意味が変わる**ので図の読み違いに直結する。全21モジュールを走査し、モジュール直下の可変状態は block と c4 の2つだけと確認 (他は関数ローカル)。変異で確認 | 済 |
| A115 | **c4 の契約経路が引数を1つ落としていた**。`src/modules/c4.js` の `operations.add` が `addElement` を**6引数で呼んでおり** (宣言は7引数)、`parentId` が常に undefined。UI パネル (`c4-add-btn`) は parent を渡すので画面からは正しく境界に入るが、**契約どおり呼ぶと黙ってトップレベルに出る**。「UI 経路だけ実装して契約経路を忘れる」型の**17例目**。あわせて**入れ子の字下げが固定幅**で文書の流儀を見ていなかった — 実測: 既存の子が 8 の文書に c4 は 4 (**−4**) / block は 2 (**−6**) で足していた。図は描けるが Git 差分では隣と揃わない。既存の子があればその字下げに合わせる形に変更。ユニット8本追加、変異2種で確認 | 済 |
| A116 | **マイルストーンを起点にしたガントで較正が 0 のまま**、バーもハンドルも描かれるのに掴んでも動かない。利用者からの報告「ガントの操作感が悪い → **バーのドラッグ**」を追って発見。`節目 :milestone, m1, 2026-03-01, 0d` は開始日を持つが期間が `0d` (割ると壊れる)、`実装 :t2, after m1, 10d` は期間を持つが開始日が無い。どちらも単独較正の条件を満たさず、2点較正にも足りない。**倍率は幅と期間から、原点は開始日を持つバーから、別々の相手に取れる**ように変更。マイルストーンは菱形で描かれ矩形として照合できない (実測: `barRects[0]` が null) ため、原点には `after` を解決した開始日 (`resolveSpan` が返す `starts`) を使う。**実測: pxPerDay 0 → 54.4。** 掴めるバーが `after` 指定のみになる場合は仕様どおり動かないが、理由をステータスに出すことをテストで固定した。変異で確認 | 済 |
| A117 | **エディタに入るとキーボードだけでは出られない (UI-054、Blocker)**。Tab は字下げに使う (ADR-011) ので外へ出す別のキーが要るが、実測で **Escape / Ctrl+Tab / Shift+Tab / F6 / Ctrl+Shift+Tab / F1 / Ctrl+E / Ctrl+L の8種すべてで焦点が動かなかった**。さらに `A` / `E` / 矢印 / Delete はどれも「図にフォーカスがあるとき」が条件で、その図には **`tabindex` が付いておらず Tab では入れない** (body から Tab を20回押しても `btn-open`〜`editor` で止まる)。**キーボード操作層の入口がマウス専用だった。** `focusPreview` は既にあり `tabindex="-1"` を付けて焦点を移す — **経路だけが無かった**ので、エディタでの Escape から呼ぶ (エディタの Escape は今まで無反応だったので奪う操作は無い)。移った先で何が効くかも案内する。ヘルプ表にも行き先を明記。実測: エディタ →(Escape)→ 図 →(↑↓)→ 要素選択 →(E)→ エディタ の一周が成立し、本文は不変。e2e 5本追加、変異3種で確認 | 済 |
| A118 | **保存が失敗しても黙っていた (UI-067、Major)**。棚卸し (`docs/coverage-map.md`) の未測定2位「保存に失敗したとき」を測って発見。`saveFile()` に例外処理が無く、`downloadAsFile()` が投げると **`markSaved()` にも表示にも到達せず終わる**。実測 (直す前): 保存を失敗させても表示は「要素: 3 | 関連: 2」のままで、失敗を告げるものが何も出ない。**上書き経路 (`overwriteSaved`) は try/catch を持っているのに、ダウンロード経路だけが無防備**だった。保存は「押した」ことしか分からない操作なので、黙るのが一番高くつく — 気付かないまま作業を続け、閉じたときに全部失う (UI-064) のが最悪の並び。失敗時は理由と「本文はそのまま残っています」を出し、**`markSaved()` を呼ばない** (印を残す)。e2e 4本追加、変異3種で確認 | 済 |

## B. 仕様として着手した機能

| # | 内容 | 状態 |
|---|---|---|
| B1 | gantt 項目1: 概観 / 詳細モード | 済 |
| B2 | gantt 項目2: セクションの上下入れ替え | 済 |
| B3 | gantt 項目3: タスクの連続入力 | 済 |
| B4 | gantt 項目4: 日付入力 (color-scheme) | 済 |
| B5 | gantt 項目5: マイルストーン | 済 |
| B6 | オーバーレイを 3図種 → 11図種へ | 済 |
| B7 | 接続モードを 5図種に配線 | 済 |
| B8 | ショートカット一覧 (`?` キー) + Open/Save の title に修飾キー (UI-002) | 済 |
| B9 | 一覧の絞り込み (12行以上で自動表示、件数を常に出す) (UI-003) | 済 |
| B10 | 上書き保存 (Ctrl+Shift+S / Export メニュー、既定の Save は従来どおり) (UI-004) | 済 |
| B11 | 削除の作法を件数表示に統一し confirm を廃止 (UI-005) | 済 |
| B12 | 保存・削除の一時メッセージ (UI-006 / UI-009) | 済 |
| B13 | ラベル欄を打鍵ごと反映 (150ms) + フォーカス復元 (UI-007) | 済 |
| B14 | Mermaid記法 (.mmd) の書き出し (UI-008) | 済 — **記録を訂正 (2026-08-21 再検証)**。当初 Export メニューに置いたが、B25 で **Save に集約**した。Export メニューには無く Save が `.mmd` を書き出す (実測) |
| B15 | 全21図種で「一覧 → 選択 → 編集」を揃える (UI-010) | 済 — r7 K7 で常設監視 |
| B16 | ファイル名と未保存をタイトルに常表示 (UI-104) | 済 |
| B17 | ズームのキーボード操作 Ctrl+0/9/± (UI-105) | 済 |
| B18 | ステータスバーに `? ショートカット` を常設 (UI-106) | 済 |
| B19 | 用語を日本語に統一 (`Label` → `ラベル` 17箇所) (UI-107) | 済 |
| B20 | Escape 後のフォーカスをプレビューへ (Nit-108) | 済 |
| B21 | ショートカット一覧に図種制約を明記 (UI-202) | 済 |
| B22 | 一時メッセージの文体を「何が起きたか — どうできるか」に統一 (Nit-205) | 済 |
| B23 | gantt の buildOverlay を gantt.js へ移設し、3引数契約に統一 (M2 前半) | 済 — app.js 3073 → 2995行 |
| B24 | gantt の renderProps と追加フォーム状態を gantt.js へ移設 (M2 完了) | 済 — app.js 3073 → 2232行 |
| B25 | 保存の入口を Save の分割ボタンに集約 (UI-203) | 済 |
| B26 | `? ショートカット` を初回5秒だけ強調 (UI-204) | 済 |
| B27 | classDiagram / erDiagram に名前変更を追加 (参照の追従付き) | 済 |
| B28 | gantt に統一入口 operations を追加 (20/21 → 21/21) | 済 |
| B29 | **宙に浮いた依存を指摘する (UI-047 / FEAT-002)**。`after` の指す先がどのタスクの id でもないとき、mermaid は警告を出さず任意の位置に置く。`resolveSpan` も黙って `prevEnd` に落ちるので図は描かれ、**実測で `status-parse` は "OK"**、`status-info` は「タスク: 2 | セクション: 1」のままだった。タスクの id を書き換えた直後に一番当たりやすく、**Git にコミットしてレビューに出るまで気付けない**。`danglingAfter` を追加し、ステータス行に**どのタスクのどの id か**を出す (件数だけでは数百行の図で探せない。3件以上は先頭2件+「ほかN件」)。`after` は空白区切りで複数を取れる (改名処理が split しているのと同じ) ので1つずつ確かめる。外部でも同型の不満が挙がっている ([mermaid-viewer](https://mermaid-viewer.com/en/gantt-chart-editor), 2026-08-22確認)。ユニット5本追加、変異2種で確認 | 済 |
| B30 | **版の刻印 (UI-044 / FEAT-001)**。画面に版が出ておらず、どの版を動かしているか分からない。実測: 画面テキストに `\d+\.\d+\.\d+` 形式が**1つも無く**、`[id*=version]` 要素も存在しなかった。`VERSION` (1.2.0) はファイルにあるだけで**アプリから一切参照されていなかった**。その結果、利用者から「ガントのバーが動かない」と報告を受けたとき、**どの版の話かの特定に調査の大半を費やした** (作業ツリーが origin/master より74コミット遅れていた)。ステータス行の右端に `v1.2.0` を表示 (**縦には増やさない** — 行高は 24px のまま、`F1 ショートカット` と重ならないことを実測)。単一HTMLでビルド工程が無いので値は直書きし、**VERSION との一致をテストで固定**した (人が2箇所を揃え続けるのは必ず失敗する)。変異で確認 (VERSION を 1.3.0 にして HTML を放置 → FAIL)。実機スクリーンショットで表示位置を確認済み | 済 |
| B31 | **書き出した図の線を白地で見える濃さにする (UI-041 / G9)**。利用者から「宿題は推奨で対応」と委任され、**案B(書き出し時だけ線を濃くする)を推奨・実施**。理由: 実測で問題は**線だけ** (ノードは暗い塗りなので文字は読める)、白背景は設計書貼り付けと印刷で必要、書き出し経路に閉じるので画面表示と座標に影響しない。色相を保ったまま明るさだけ落とす (crit の赤と通常の線が書き出しで区別できなくなるのを避ける)。矢印の先端は線ではなく**塗り**なので marker の中だけ塗りも見る。透過書き出しは貼る先の地色が不明なので触らない。**実測 (書き出した PNG の、線に最も多く使われた色のコントラスト比)**: flowchart 1.61→**7.11** / sequence 1.61→**5.41** / class・er・block・quadrant 1.61→**5.41** / timeline 1.50→**5.10**。**21図種中 7図種が改善、悪化 0**。残り4図種 (requirement / mindmap / architecture / radar) は**支配色が線ではなく塗り**で、暗くすると上の文字が読めなくなるため触らない。e2e 4本追加、**本物の書き出し経路 (`svgToCanvas`) を通す試験**を含め変異で確認 | 済 |
| B32 | **class の並べ替えを UI に戻した (G1)**。委任を受け、**class だけ案A(実装)、er / state は案C(現状維持)** を推奨・実施。止めていた理由は `_moveClassStep` の `_is*Line` がブロック形式のクラスを他クラスの本体に飲み込ませることだったが、**その実装はもう使っていない** — 契約経路 `operations.moveUp` は `moveElementLine` で**ブロック単位**に入れ替える。UI をそちらに差し替えて `move: true` に戻した。**実測** (`class Animal {…} class Dog {…} class Cat {…}` で Dog を上へ): 古い経路 → **変化なし(空振り)** / 契約経路 → `Dog,Animal,Cat` にブロックごと移動。実機でも `↑` が出て動き `parse: OK`。**er / state は外さない** — 要素の `line` が宣言行ではなく**関係行**を指しており (実測: er は両方 `line=2` の関係行、state は遷移行)、動かしても並びが変わらない。`properties.test.js` の不変条件を書き換え (class を一覧から外し、「クラスパネルでのみ move」「move は契約経路を呼ぶ」を新たに固定) | 済 |
| B33 | **書き出しで色を変えたことを告げる (UI-050)**。画面と同じものが出ると思っている人にとって色が変わるのは予期しない挙動で、線色を意図して設定した図ではなぜ違うのか分からない。書き出し後に「白地で見えるように線32本を濃くしました — 画面の色のまま出すには透過で書き出してください」と表示。透過書き出しでは黙る (色を変えていないため)。**この表示を作ったことで、自分のコードの欠陥が1件見つかった** — 「もともと濃い図」の xychart で19本と出たので内訳を調べたら、`stroke-width: 0px` の棒の輪郭3本を数えていた。**描かれていない線は対象外**にして 19 → 16本。**数を出さなければ気づけなかった。** e2e 3本追加、変異2種で確認 | 済 |
| B34 | **並べ替えができない図種で、理由つきの淡色ボタンを出す (UI-049)**。classDiagram では `↑↓` が出るのに erDiagram では出ない差が画面から読めなかった。内部事情 (要素の `line` が宣言行を指すか関係行を指すか) で決まっており、**無いものは探しても見つからない**ので探す時間がそのまま失われる。`opts.moveDisabledReason` を足し、er は「エンティティの行が関係行を指すため〜」、state は「状態の行が遷移行を指すため〜」を表示。実測: opacity 0.45 / cursor not-allowed。**理由を書いたモジュールだけがこの形になる** — 全パネルを一度に変えると、まだ測っていない図種まで「できない理由」を名乗ることになる (関連パネルは今までどおり非表示)。実機スクリーンショットで確認。e2e 4本追加、変異2種で確認 | 済 |
| B35 | **壊れた行を指す (企画 FEAT-011)**。構文が崩れたとき、帯は「図を描けませんでした」だけで**どの行が悪いか分からなかった** (実測: 6種類の誤りすべてで行番号なし)。**mermaid は返している** — `message: "Parse error on line 6"` と `hash.loc.first_line: 5` が例外に入っているのに捨てていた。**捨てている情報を拾って出すだけ**で、行へ飛ぶ仕組みは既存の `gotoEditorLine` を使う。実測 (直した後): 5行目の誤り → 「5 行目へ」/ 押すと5行目の `D --> E["E"` を選択しエディタに焦点。実装中に自分の欠陥を2件見つけた — (1) **範囲検査を message 経路にしか掛けておらず、2行の文書で「3 行目へ」と出た** (閉じ括弧の欠落は終端で気付かれるので mermaid は末尾より先を指す)、(2) **隠すときに中身を消しておらず、古い行番号を指すボタンが DOM に残った**。mermaid が行を返さないときは**推測で指さない**。実機スクリーンショット確認、e2e 4本追加、変異3種で確認 | 済 |
| B36 | **書き出した SVG が決定的でない範囲を README に明記 (UI-057)**。差別化軸「決定的出力・Git差分親和性」を実測したところ、**`.mmd` の保存は決定的** (2回保存してバイト一致、CRLF も保持) だが、**classDiagram の書き出し SVG はブラウザを開き直すたびに変わる**。実測: 3回とも異なり、変わるのは**メンバー文字の下線を描く `path` の制御点だけ** (端点も図の大きさ `50.140625x234` も一致するので**見た目は同じ**)。flowchart / sequence / gantt は開き直しても**バイト単位で一致**。原因は mermaid 側の描画でこちらでは制御していないので、**実装を変えず期待値を合わせる** — README に「Git に置くのは `.mmd` を推奨」と実測表つきで記載した | 済 |
| B37 | **版の鮮度を機械が数える (E13 / UI-059)**。画面に版を出した (B30) が、**それが何を識別しているか**は測っていなかった。測ったら **`v1.2.0` のまま 258 コミット / 実装変更 151 回**が積まれ、その間に**欠陥117件を直し機能36件を足していた** — 版は何も識別していなかった。利用者が74コミット遅れていたときも版は `1.2.0` のままで、**版だけでは修正が入っているか判別できず**、その調査に大半の時間を使った。直し方の候補は**測って落とした**: ビルド時の埋め込み (ビルド工程が無い) / 実行時に自分を読む (**file:// では `TypeError: Failed to fetch`** と実測) / 人が手で上げる (B30 で避けた「2箇所を人が揃える」失敗形)。残るのは**上げ忘れを機械が言う**形。上限30コミットを超えたら FAIL する検査を足し、ゲートを11項目に。**いつ上げるかは人が決める** (リリースの粒度は価値判断なので代行しない)。あわせて版を **1.2.0 → 1.3.0** に上げた。変異で確認 | 済 |
| B38 | **絞り込みへ `/` で飛べるようにした (UI-063)**。ペルソナの核心「毎日100回の操作」を**初めて端から端まで数えた**ところ、目的の要素への到達コストが経路で桁違いだった。実測 (flowchart): 20要素 18手 vs 6手 / 60要素 50手 vs 6手 / **120要素 98手 vs 6手**。絞り込みは「欄へ行く + 打つ + Enter」で**要素数によらず一定**なのに、図へ移ったときの案内は `↑↓` しか名指ししておらず**線形に伸びる方だけが目に入っていた**。1日100回なら 9200手の差。`/` キー (多くのツールで検索の入口なので覚え直しが要らない) を足し、案内文とヘルプ表にも書いた。欄は一覧12行超で出るので、出ていないときは「一覧が短いので絞り込み欄は出ていません — ↑↓ で選べます」と言う。e2e 5本追加、変異3種で確認 | 済 |
| B39 | **未保存の印を画面の中にも出す (UI-065)**。これまでタイトルバーにしか出しておらず、**タブを何枚も開く使い方ではタイトルが省略されて `●` が見えなかった**。数十〜数百枚を扱う人ほどタブが多い。ステータス行の版表示の隣に「● 未保存」を出す (目に入る必要があるので薄くしない)。実測4条件: ひな形=出ない / 直した後=出る / 保存後=消える / **保存失敗後=残る**。A118 と組で、**押したのに印が残っていることが「保存できていない」に気付く唯一の手がかり**になる | 済 |
| B40 | **測った場面と測っていない場面の一覧 (`docs/coverage-map.md`)**。ラウンド64 の診断で「製品の欠陥は尽きた」と結論したが、**翌ラウンドに未測定領域を1つ開けただけで Major が1件出た** (UI-064)。「尽きた」ではなく「**同じ場所を測り続けていた**」が正しかった。利用者が触る場面を7分類35項目に展開し、**測定済22 / 未測定8 / 測定不能5** と分類。**勘で「尽きた」と判断しないための一覧**で、網羅は主張しない (足りない場面に気付いたら足す) | 済 |

## C. 検証の仕組み

| # | 内容 | 状態 |
|---|---|---|
| C1 | render オラクル (実描画で before/after 比較) | 済 |
| C2 | 並行レビュー機構 `tools/review/` | 済 |
| C2a | 実描画のレビュアー R3 (mermaid に描かせて確認) | 済 — 初期4観点の1つ。`tools/review/r3-render.js` |
| C2b | UI の一貫性のレビュアー R4 | 済 — 初期4観点の1つ。`tools/review/r4-ui.js` |
| C3 | 合格基準 `docs/quality-gate.md` | 済 |
| C4 | レビュー実施記録 `docs/review-log.md` | 済 |
| C5 | 自動合否判定 `tools/review/gate.js` | 済 |
| C6 | この棚卸し | 済 |
| C7 | 破壊/挿入の敵対検証 R6 (旧 D1) | 済 — 変異注入で有効性を確認 (move を壊すと2件検出) |
| C8 | 端ケース: 先頭を上へ / 末尾を下へ (旧 D2) | 済 — R6 に内包 |
| C9 | ユーザー観点レビュアー R5 (旧 D3) | 済 — 変異注入で有効性を確認 (選択を壊すと13件検出) |
| C10 | 一貫性/発見可能性/エラー回復のレビュアー R7 | 済 — 変異で確認 (履歴を壊すと20件検出) |
| C11 | スケール耐性のレビュアー R8 (100要素) | 済 — A23 を検出 |
| C12 | オラクルの削除アサーション (expectAbsent/expectPresent) | 済 — 旧実装で FAIL することを確認 |
| C13 | ワークフロー適合のレビュアー R9 (差分の局所性/コメント保存) | 済 — 変異で確認 (W1:9件 / W4:1件) |
| C14 | 往復のレビュアー R10 (保存→開き直し→SVG書き出し) | 済 — 変異で確認 (18件検出) |
| C15 | 特殊文字のレビュアー R11 (21図種×実名10種) | 済 — 変異で確認 (11件検出) |
| C16 | 描画失敗の原因を名指しする診断 src/core/diagnose.js | 済 |
| C17 | 無言の空振りを探すレビュアー R12 | 済 — 変異で確認 |
| C18 | 未知構文のレビュアー R13 (判定を実描画に接地) | 済 — 変異で確認 |
| C19 | 境界のレビュアー R14 (0個 / 1個) | 済 — 変異で確認 |
| C20 | 状態の持ち越しのレビュアー R15 | 済 — 変異で確認 |
| C21 | 件数の一致のレビュアー R16 | 済 — **記録を訂正 (2026-08-21 Y11)**。当初は**片方向 (パネル→図) だけ**で、「逆は r2 / r11 が見ている」という記述も誤りだった (A59/A80 は手作業で発見)。逆向き (図→パネル) を実装し、A87 を検出 |
| C22 | Undo/Redo の往復整合のレビュアー R17 | 済 — 変異で確認 (2種類とも検出)・指摘0 |
| C23 | キーボード完結性のレビュアー R18 | 済 — 変異で確認・1件検出し修正 |
| C24 | r12 を関数名の表から契約ベースへ書き換え | 済 — 偽陽性3件を除去し、gantt の素通りを発見 |
| C25 | 検査の網羅を検査する観点 R19 | 済 — r6 の網羅が6→21モジュールに広がった |
| C26 | r6 を契約ベースへ書き換え | 済 — 15モジュールの素通りを解消 |
| C27 | r1 / r2 / r9 / r14 を契約ベースへ書き換え | 済 — r1 の網羅 14→21、r2/r9/r14 も 19→21 |
| C28 | 網羅率の自動計測 (`tools/review/lib.js` が検査した図種数を数える) | 済 — 計測して初めて r11 が 6/21 しか見ていないと分かった |
| C29 | 網羅率の下限をゲート化 (`coverage-floor.json` + `COV`) | 済 — 変異で確認 (r11 を 18→6 に戻すと FAIL) |
| C30 | r11 を契約ベースへ書き換え | 済 — 網羅 6/21 → 18/21。観点を増やさず47件の指摘 |
| C31 | r17 / r18 の範囲を実機2〜4図種から全図種へ | 済 — r17 2/21→19/21、r18 4/21→11/21 |
| C32 | r2 の逃げ道を塞ぐ (述語を「1回で消える」に) | 済 — `&& !lineGone` / `&& !stillReferenced` を除去し12件検出 |
| C33 | 日本語名の非対称のレビュアー R20 | 済 — erDiagram が日本語名を読めない件を発見 |
| C34 | 測定条件の固定 (実機レビュアーを 1366x768 に) | 済 — 8/21 と判定していたものが 15/21 だった |
| C35 | 画面に見えているかのレビュアー R21 | 済 — 変異3種で確認 (20件 / 21件 / 14件) |
| C36 | 無言の失敗のレビュアー R22 | 済 — 変異で確認 (3件)。クリップボードの無言failure を発見 |
| C37 | 棚卸しと実体の対応をゲートで照合 (`DOC`) | 済 — 変異で確認。C 区分の9項目の遅れと R3/R4 の未記録を検出 |
| C38 | E 区分の全件再検証 (Y7) | 済 — 5件中4件の記録が誤っていた (E1/E2/E6/E7) |
| C39 | A 区分の抜き取り再検証 (Y8) | 済 — 10件とも記録どおり。テストで守られている記録は劣化していない |
| C40 | 棚卸しの主張を機械で検証 (`record-claims.js` + ゲートの `REC`) | 済 — 変異2種で確認 (move: true で1件 / 使える id を持つ図種で1件)。G1 / E2 / E7 の3件を対象 |
| C41 | B 区分の抜き取り再検証 (Y10) | 済 — 10件とも機能は生きていた。記録の所在が1件古かった (B14) |
| C42 | C 区分の抜き取り再検証 (Y11) | 済 — 7件中6件は変異を検出。C21 のみ記録が実態より広く、逆向きを実装して A87 を検出 |
| C43 | モジュールの読み込み失敗を明示 (`lib.js`) | 済 — 変異で確認。壊れたモジュールがあると警告を出し、母数にも含める |
| C44 | r16 の逆向きが「引用符付きで通した本文」を読み直していなかった | 済 — 引用符付きの再試行が成功したとき、比較する要素数は**引用符なしのまま**だった。直しても指摘が消えず、直っていないように見えていた (検査の誤り9件目) |
| C45 | **追加のレビュアー R23** (`r23-add-renders.js`) | 済 — 変異2種で確認。実描画の網は改名と削除にしか掛かっておらず、**一番最初にやる「追加」に網が無かった**。そこに A91 / A93 / A94 が溜まっていた。30通り (半角/日本語 × 図種 × 種類) を実描画で確認 |
| C46 | r23 の述語を広げすぎて、直したばかりの欠陥を見逃した | 済 — 「渡す値の不足」を `undefined` だけでなく空の `""` でも判定したところ、**それは A91 そのものの形**で、変異を入れても検出できなくなった。述語を `undefined` だけに戻した |
| C47 | **欄名の検査 `terms.js`** をゲートに追加 | 済 — 変異で確認 (`Name` を1つ戻すと1件検出)。直しただけでは戻る (E 区分の再検証で散文の規約は5件中4件が誤りだった)。日英混在の数も毎回出す |
| C48 | **並べ替えのレビュアー R24** (`r24-move-renders.js`) | 済 — 変異で確認 (素の行入れ替えに戻すと16件検出)。実描画の網は追加・改名・削除にしか無く、**並べ替えに無かった**。M1 描ける / M2 数が変わらない / M3 黙って空振りしない / M4 端でも壊さない の4つ。42通り + 端80通り |
| C49 | r24 の M3 が親の違う要素を「動くはず」と誤判定 | 済 — mindmap の兄弟は種類ではなく親で決まる。要素が名乗る `parentId` で束ねるようにした (検査の誤り13件目) |
| C50 | r24 に M4 が無く、**元の欠陥を捕まえられなかった** | 済 — M1〜M3 は2番目の要素しか動かさないので、先頭を上へ動かす欠陥に当たらない。変異を戻して 9件中2件しか出ないことから判明 (検査の誤り14件目) |
| C51 | r9 の W4 が packet の正しい実装を欠陥として報告 | 済 — 「並べ替えは行の中身を変えない」は**位置が中身になっている図種**では成り立たない。行ではなく要素の見分けがつく文字の多重集合を比べる形に変えた。ラベルを潰す変異で検出することを確認 |
| C52 | **r18 の網羅を 11/21 → 21/21 へ** | 済 — 変異で確認 (入力欄に `tabindex="-1"` を付けると6件検出)。重ね合わせが無い10図種で打ち切っていたが、**K1 が使えないことと K2〜K5 が使えないことは別の話**だった。一覧の「編集」へ Tab で到達する経路を足した |
| C53 | r18 の K2 が id の接尾辞表に依存していた | 済 — `-label` / `-name` / `-id` しか見ておらず、`mm-edit-text` / `sk-edit-from` / `xy-edit-values` を「届かない」と誤報した (検査の誤り17件目)。**接尾辞の表を持つと必ず漏れる** (r11 と同じ教訓)。パネルの中の文字入力欄に届いて打鍵で本文が変わるか、という振る舞いで見る形に変えた |
| C54 | **網羅率の低い4観点を契約ベースへ** (r5 6→21 / r10 6→21 / r15 5→21 / r8 4→19) | 済 — 変異で確認 (r5:20件 / r10:63件 / r15:21件 / r8:36件)。r1/r2/r6/r11/r12/r14/r18 は書き換え済みだったのに、**この4つには書き換えが届いていなかった**。r8 は図種ごとの巨大文書の生成器を手書きしていたので、モジュール自身の `add` を繰り返して育てる形にした |
| C55 | r8 の検査の誤り4件 | 済 — S4 の同一性を id の正規表現で見ており **自動採番の id (`__s_99`) は本文に現れない**ため4図種を誤報 (19件目)。gantt はセクション96個をタスク一覧と比べて誤報 (20件目)。gitGraph の `data-element-id` は行番号で要素の id とは別物 (21件目)。一覧末尾は多くの図種でエッジなので要素だけ比べると差が出ない (22件目) |
| C56 | r5 で「K1 が失敗したら残りを飛ばす」を作りかけた | 済 — r18 で直したばかりの抜けを別の検査で再現しかけた。U1〜U3 だけを飛ばし、U4 以降は全図種で見る形に直した |
| C57 | r8 の S4 が「消えたのは1つだけ」を求めていた | 済 — sankey のノードは流れの端点としてしか存在しないので、消せば流れも消えるのが正しい。それを欠陥として報告しかけた (誤り23件目)。次に関係を一律で除いたら、関係を消した4図種が全部「巻き添え」になった (誤り24件目)。**対象が何かを決めてから巻き添えを数える**のが正しい順序だった |
| C58 | 契約の kind を全図種で検査 (`tests/contract-add-kind.test.js`) | 済 — 一覧に出ている種類で足せることを21図種で固定。radar は日本語の識別子を受け付けないので半角で試す (mermaid 側の制限。r23 が区別を実測している) |
| C59 | **バックログの前提を機械で監視** (`backlog-premises.js` + ゲートの `PREMISE`) | 済 — 変異3種で確認 (前提が崩れる / 一覧に上限が入る / 前提の検査が無い項目を足す)。G区分の各項目に「先送りしてよい理由」を宣言させ、崩れたら FAIL する。**宣言の無い項目を足すと FAIL する**ので、また誰も見張らない項目ができるのを防ぐ。人が決める項目 (訳す範囲・案の選択) は自動化できないので、待っている事実だけを毎回出す |
| C60 | **削除リスト (H区分) の復活条件も監視** | 済 — 変異3種で確認 (ADR-027 が採番された / 案が選ばれた / 復活条件が空)。凍結は「忘れる」ことではないので、**条件が満たされたら自動で気付ける**状態にした。復活条件が空の項目があると FAIL する (黙って消えたのと同じ) |
| C61 | 復活の合図が**説明文に反応**して誤検出 | 済 — MOC に書いた再開手順の例文 (`採用: 案A`) を「案が選ばれた」と誤検出した (検査の誤り25件目)。指示と決定は見た目が同じなので、**置かれる場所**で分ける (行頭のみを決定とみなす)。変異で確認 |
| C62 | **ラウンド30〜45 の判断を ADR ドラフト化** | 済 — CLAUDE.md の規約「アーキテクチャレベルの判断をしたらその場で `docs/adr/drafts/` にドラフトを残す」を**16ラウンド分守れていなかった**。5件を起票 (並べ替えのブロック単位化 / 描き直しの適応的な待ち時間 / add の kind 別名 / 欄名3語 / バックログの前提監視)。**採番はしていない** (承認が要る) |
| C63 | **ラウンド30〜45 の ECN 化と姉妹プロジェクトへの影響分析** | 済 — CLAUDE.md の規約4 も **16ラウンド分守れていなかった**。ECN-032 (契約経路の Blocker) / 033 (実描画の網の拡大) / 034 (棚卸しの検査層) を起票し README 索引を更新。06_PlantUMLAssist へ影響分析を作成し、**同型の欠陥を実測で1件検出** (`sequence.js:1497` の並べ替えが `@startuml` を1行目から追い出す) |
| C64 | 記号だけのボタンの説明をゲート化 (`terms.js` T3) | 済 — 変異で確認。**DOM 走査では見つからなかった1個 (timeline の条件付き表示のボタン) をソース走査が拾った**。画面に出ていないものは画面からは数えられない |
| C65 | **滞留サイクル数と期限を機械が数える (N4)** | 済 — 変異で確認 (期限を過去に置くと FAIL)。**人が数えていて失敗したから足した**: G7 はラウンド43 で「45まで」と予告しながら **6サイクル実行を忘れ**、直前の診断では「2サイクル以内」と**誤って書いた** (実際は11サイクル)。機械監視は忘却を防いだが**滞留の長さは誰も数えていなかった**。**これはゲートに載る3つ目の運用検査で、「検査基盤への新規投資0」という宣言は正確ではなくなった** — 次の診断で数える |
| C66 | N4 の期限判定が1サイクル甘かった | 済 — 「ラウンド N までに」なのに `now > deadline` で比べており、**期限のラウンドを丸ごと見逃していた**。実際 G8 の期限 R54 が到来しても検出されなかった。`now >= deadline` に直したら即座に検出。**N4 が防ぐはずの失敗を N4 自身がやっていた** |
| C67 | e2e が確かめたい操作を**ダイアログの既定却下で握り潰していた** | 済 — 変異で確認。破棄確認のダイアログに手当てをしないと Playwright は**既定で却下**するため、図種の切替そのものが起きず、**壊した実装でもテストが通った**。プローブ (受諾する) では漏れが出るのにテストは緑、という食い違いで気付いた。**変異が効いているのに落ちないときは、実装ではなく検査を疑う** — これで30件目 |
| C68 | 引数落ちの走査が**既知の1件すら検出できなかった** | 済 — 正規表現で呼び出しを拾ったところ、手前の `ELEMENT_KINDS.indexOf(kind)` を先に食って `addElement(...)` を飲み込んでいた。**「0件」はそのとき何の意味も持たない。** 既知の欠陥を一度戻して検出できるか確かめてから信じる、を守って発見。括弧対応で数え直したら 7→8件になった |
| C69 | 字下げの判定式が、正しい実装を「ずれ」と数えた | 済 — 「隣の行と揃うか」で測ったため、journey / timeline の `section` と kanban の列 (**親の階層に足すのが正しい**) を誤検出した。「**同じ種類の既存行**と揃うか」に直したら 3件→0件。さらに block の入れ子経路を `kind:'block'` で呼んでおり**入れ子経路を通っていなかった** (正しくは `kind:'nested'` + `props.parentId`) |
| C70 | 「gantt の追加ボタンが効かない」と読んだ | 済 — 開始日が必須で、理由を表示して正しく止めていた。入れずに押して「無反応」と読んだ |
| C71 | 「一覧の停留点が15個増えた」と読んだ | 済 — `.ma-list-row` 内しか数えず、master 側の同じボタンを数え漏らした。**パネル全体で数えると同一 (230 / タスク50まで121)** |
| C72 | 「セクション削除ボタンが master に無い」と読んだ | 済 — 同じ選択子の誤り。master にもあり、確認なしで21行消えるのは**両版共通の既存挙動** |
| C73 | 「↑ を押しても動かない」と読んだ | 済 — 掴んだのは**先頭セクションの ↑** で、元から動かない位置。同じ罠を2度踏んだ |
| C74 | 「動かせない理由が出ない」と読んだ | 済 — 読み先が `#status-message` で、実際は **`#status-info`** に出ていた |
| C75 | 「マイルストーンにバーが作られないのは一貫性の欠如」と指摘した | 済 — **撤回**。除外は意図的で、`src/modules/gantt.js:777` に実測付きの根拠が書かれていた: マイルストーンは45度回転した20×20の矩形で、`getBBox()` は**回転前**の箱を返すため x が日付と無関係。較正に入れると**他の全タスクの pxPerDay が狂う** (実測: 19.267 → 18.571、無関係なタスクが1日ずれた)。**根拠が実装に書いてあるのに読まずに指摘を出した。** これで38件目 |
| C76 | **r8 が再現しない指摘を出した (flaky)** | 済 — 変異で確認。「sankey-beta 末尾要素の削除が効かない」を1度だけ報告したが、単独で再実行すると3回とも通る。原因は削除後の**固定 900ms 待ち**で、27本を連続実行して負荷が高いときだけ間に合わなかった。**再現しない指摘は指摘そのものより質が悪い** — 落ちても信じなくなり、ゲートが働かなくなる。変化を待つ形に直し、上限 `MAX_MS_DELETE` (3000ms) を超えたときだけ報告する。検出力が落ちていないことを**UI 経路の `deleteFlow` を壊して確認** (`operations.delete` を壊しても検出されない — ボタンは契約経路を通らないため。変異の当て先を間違えると「検出できない」と誤読する) |
| C77 | 「セクション削除に手当てが無い」と指摘した (UI-045) | 済 — **格下げ**。確認ダイアログは確かに無いが、**削除後の表示は既に実装済み**だった (`src/modules/gantt.js:1420`)。実機で確認: 「セクション「S1」とタスク20件を削除 — Ctrl+Z で戻せます」。**「確認して止める」より「実行して知らせ、取り消せる」方が、毎日100回操作する利用者には正しい**ので、確認ダイアログは追加しない。指摘を Minor → **Nit / 対応不要**へ格下げ。**実装を読まずに「手当てが無い」と書いた** — C75 と同じ誤りを2回した |
| C78 | **属性で上書きして、書いた属性を読み返して「直った」と読んだ** | 済 — `setAttribute('stroke', ...)` は SVG 内の `<style>` に負けるため**描画色が変わらない**。実測: 属性で `rgb(0,0,0)` を入れても描画色は `rgb(211,211,211)` のまま、`style.setProperty(..., 'important')` なら変わった。さらに最初の測定は**自分が書いた属性を読み返していた**だけで、描画結果を測っていなかった。「18図種すべて 3.0 以上に改善」は誤り。PNG の画素で測り直したら実際の改善は 7図種 |
| C79 | 判定式が「最も明るい非白画素」を見てアンチエイリアスを拾った | 済 — 線の周囲の中間色を拾うので、どの図種も 1.0〜1.3 になり差が出なかった。**線に最も多く使われた色**で測る形に直したら 204→106/88 の変化が見えた |
| C80 | 透過書き出しの試験が `[style*=stroke]` を数えていた | 済 — mermaid 自身も inline style を持つので、元からある分を数えていた。**こちらが付けた `!important` だけ**を数える形に直した |
| C81 | **書き出しの試験が関数を直接呼んでおり、経路に繋がっているかを見ていなかった** | 済 — 呼び出し側を `if (false)` で殺しても**3本とも通った**。`svgToCanvas` という本物の経路を通す試験を足したら落ちた。A115 (UI経路と契約経路が別) / C76 (変異の当て先) と同じ骨格で、これで3回目 |
| C82 | **ライセンス不整合の指摘が、74コミット遅れた版を読んだもの** | 済 — **撤回**。`LICENSE` = MIT / `package.json` = ISC と報告したが、`origin/master` では**既に MIT に統一済み**だった。ガントの調査と同じ「古い版を読む」誤りで**3回目** |
| C83 | **「長期ガントでバーが細くなり破綻する」(UI-048) は誤り** | 済 — **撤回**。**概観モードだけを測って「破綻する」と書いた。** 詳細モードが既に実装されている (`src/app.js:1203` の `DETAIL_PX_PER_DAY = 24`、上限 `GANTT_MAX_WIDTH = 60000`)。実測: 2年の図で **pxPerDay 0.373 → 23.897**、幅 694 → 35040px。到達経路もあり、**ズームの `＋` を1回押すだけ**で切り替わる (表示も「概観」→「110%」)。外部の不満 ([mermaid-viewer](https://mermaid-viewer.com/en/gantt-chart-editor)) を**自社にも当てはまると早合点した** — 外部の指摘は必ず自社で測り直す、を守れていない。C77 / C82 と同じ「あるはずの場所を読まずに『無い』と書く」誤りで**3回目** |
| C84 | **指摘そのものを検査する層を追加 (`absence-claims.js` / ゲート ABSENCE)** | 済 — 変異で確認。ラウンド59〜60 で**あるはずの機能を読まずに「無い」と書いた誤りが3回**出た (UI-045 → `src/modules/gantt.js:1420` に既存 / UI-046 → `src/modules/gantt.js:777` に除外理由 / UI-048 → `src/app.js` の詳細モード)。**3件ともソースを検索すれば当たる場所に根拠があった。** review-log の当該ラウンドで「〜が無い」と書いた段落に、`src/...:行番号` の引用か「未確認」か「実測」を必須にした。除外は2つだけ — **訂正の文脈**と**方法論の一般化** (どちらも FAIL にすると「書くほど落ちる」逆向きの圧力になる)。**投資先が製品ではなく指摘そのもの**なので、「検査基盤への新規投資停止」の例外とした (実測: 指摘 1.0件/サイクル に対し検査誤り 7.5件/サイクル) |
| C85 | **記録の検査が、記録が古くなった瞬間を実際に捕まえた** | 済 — 変異2種で確認。class を `move: true` に戻した直後、`record-claims.js` が「棚卸し G1 は『move: false のまま』と記録しているが `move: true` がある」と FAIL した。**設計どおりに鳴った実例。** 検査側の期待を新しい事実 (class=true / er=false / state=false) に更新し、**class の move が契約経路を呼んでいること**も併せて固定した (古い `moveClassUp` に戻すとボタンは出るのに空振りするため) |
| C86 | **自分の新しいコードが幅0の線を数えていた** | 済 — 変異で確認。UI-050 の「何本濃くしたか」を出したところ、もともと濃い xychart で19本と表示された。内訳を調べたら `stroke-width: 0px` の棒の輪郭3本が含まれていた (描かれていないので見た目は変わらない)。**表示を作ったから数の正しさを問えた** — 数を出さなければ気づけなかった |
| C87 | **B34 の理由文が2つとも誤りだった (ひな形だけを測った)** | 済 — 出荷直前に自分で発見。「エンティティの行が関係行を指すため」「状態の行が遷移行を指すため」と書いたが、**ひな形が関係行の中で宣言しているだけ**だった。宣言行を持つ文書で測り直すと er も state も**正しく並びが変わる**。測り直して分かった本当の理由: er = 順序は「最初に現れた行」で決まるため関係行が先だと変わらない (文書の形で結果が変わる) / state = **図が壊れる** (`state Active {` の直後に `state Idle` が取り込まれた)。state は「順序が変わらない」ではなく「壊れる」で**重さが全く違った**。UI-048 (概観モードだけを測った) と同じ「**既定の状態だけを測る**」誤りで**4回目** |
| C88 | er の属性保持の判定が JSON のキー順まで比較していた | 済 — 順序が変わっただけで「属性の対応が変わった」と誤判定した。中身を見ると属性は正しい親に残っていた |
| C89 | **ABSENCE 検査を撤去した (効かなかった)** | 済 — 実測で判断。過去31ラウンド (R30〜R60) に遡って当てたところ、**該当56段落中47件が FAIL** (通過率 16.1%)。パターンを絞っても最良で通過率40%。**ほとんど通る状態でなければ門は機能しない** — 落ちても信じなくなるのは C76 (再現しない指摘) と同じ害。**効かなかった根本の理由**: 誤った主張は会話で出され、記録に載るのは**撤回した後**。記録を検査しても、主張ではなく散文を罰するだけだった。**自分が前ラウンドに作ったものを、実測に基づいて撤去した** |
| C90 | **構文エラーの測定で、帯を読んでいなかった** | 済 — `#status-parse` と `#status-info` だけを読み「Error の一語しか出ない」と結論した。実際は `parse-error-banner` が「図を描けませんでした — 表示しているのは直前に描けた図です」と出していた。**既定の場所だけを見る**誤りで、これで5回目 (C82 古い版 / C83 概観モード / C87 ひな形×2 / 今回)。実装を読んでいて `showParseErrorBanner` に気付き、報告前に測り直した。**行番号が無いという核心は変わらなかった**が、「一語しか出ない」は誤りだった |
| C91 | プローブの後始末漏れで chrome が28個残留 | 済 — 監視で発見。例外で落ちると `browser.close()` に到達しないため、測るたびに積み上がる (実測: 21:16〜23:27 起動分が42プロセス中28個)。終了して 42 → 14 に整理。以後プローブは `try { ... } finally { await b.close(); }` を既定にする |
| C92 | **F1 が効かないと読んだ (焦点しか見ていなかった)** | 済 — 報告前に訂正。エディタで F1 を押しても `activeElement` が `editor` のままなので「効かない」と読んだが、**ヘルプは開いていた** (`shortcut-help` が表示状態になる)。「既定の場所だけを見る」誤りの6回目。ただし**焦点が入らないこと自体は本物の問題** (UI-055) で、ヘルプの中身をキーボードで辿れない |
| C93 | 「A キーが効かない」と読みかけた | 済 — 2条件で測って解決。一周の検証で `A` を押しても焦点が動かなかったが、**要素を選択中はパネルが詳細表示になり追加フォームが無い**ため。未選択で測ると `fc-add-node-id` へ正しく移り、選択中は「この図種には追加フォームがありません」と理由を出していた。**1条件で測っていたら誤った指摘を出していた** |
| C94 | **「直せば自動的に解消する」を確かめずに書いた (UI-056)** | 済 — PL 診断で UNVERIFIABLE と自己判定し、測った。結果は**解消していた** (エディタ →Escape→ 図 →Tab 1回→ 、押すと4行目へ飛ぶ) が、**測るまでは分からなかった**。証拠なしの完了認定は自分で禁じている。回帰テストで固定した |
| C95 | **r18 が自分で入口を作っていたため、61ラウンド Blocker を見逃した** | 済 — 変異で確認。`pane.setAttribute('tabindex','-1'); pane.focus();` と書いており、コメントは「フォーカスを置くのもキーボードで」だったが**実際には検査器が製品に無い能力を自分に与えていた**。そのため「キーボードでは図に入れない」(A117) が原理的に見えない。製品が用意した経路 (エディタで Escape) だけを使う形に直し、入れなければ K0 として指摘する。**変異で A117 を殺すと21図種で検出**する (直す前は0件)。**一般化: 検査器が製品に無い能力を自分に与えると、その欠落は測れない** |
| C96 | **r8 が「効くか」と「速いか」を同じ閾値で判定していた** | 済 — 変異で確認。900ms → 3000ms に延ばしても、26観点を連続実行したときだけ「削除が効かない」を報告し続けた。実測: **単独実行なら100要素の末尾削除は 22ms、連続実行時は 3000ms 超 — 136倍の差**。機械の混み具合が正しさの判定を汚染していた。正しさは 15000ms まで待ち (本当に効かない実装なら何秒待っても変わらないので検出力は落ちない)、遅さは 8000ms で別に言う。**両方の削除経路 (`deleteParticipant` / `deleteMessage`) を壊して検出を確認** — 片方だけでは検出されず、当て先を間違えると「検出できない」と誤読する |
| C97 | **検査器の捏造を26観点で棚卸しした (C95 の横展開)** | 済 — 走査器を書き、**既知の1件 (修正前の r18) を検出できることを先に確認**してから現行に当てた。字面の走査では **27本すべて 0件**。ただし走査はソースの字面しか見ないので、別の形も調べた: (1) `click({ force: true })` が5本で使われている — Playwright の到達可能性検査を飛ばすので**画面外や覆われたボタンでも押せる**。実測: 21図種のパネルのボタンで**覆われているものは0件**なので今日は何も隠していない。(2) `window.MA.history.undo()` の直呼び — **これは実害があった (C98)** |
| C98 | **Undo/Redo を検査する r17 が、Ctrl+Z を一度も押していなかった** | 済 — 変異で確認。`window.MA.history.undo()` / `.redo()` を直接呼んでおり、**キー経路を通していなかった**。C95 (検査器が製品に無い能力を自分に与える) と同じ形で、**キーが壊れても内部関数が生きていれば通る**。利用者と同じ `Control+z` / `Control+y` に置き換えた (入力欄に焦点があると本文に打鍵が入るので図に移してから押す)。**変異でキー経路だけを殺すと3図種で検出**する (内部関数は生きたまま。修正前なら通っていた) |
| C99 | **版の鮮度検査が、直した直後に落ち続けた** | 済 — 変異で確認。最初はコミット履歴だけを見ていたので、版を 1.3.0 に上げた直後も「最終更新 2026-04-16」のままで落ちた。**直したのに落ちる検査は、直す気を削ぐ。** 作業ツリーも見るようにしたが、次は `git diff --name-only` で**差分の有無**だけを見たため、変異で 1.2.0 に戻しても差分が残り**検出できなかった**。HEAD の値と今の値を**比べる**形に直して検出するようになった。**「変わったか」を差分の有無で代用すると、戻す変異を見逃す** |
| C100 | 「120要素でも絞り込み欄が出ない」と読みかけた | 済 — 指摘にする前に閾値を読んで判明。`LIST_FILTER_MIN_ROWS = 12` に対し実測239行なので出るはずだった。原因は**測定順序** — `↓` で要素を選んでからフィルタを探しており、**パネルが詳細表示に切り替わって見えなくなっていた**。選択前に測り直したら出た |
| C101 | 「必須欄が21図種すべてで示されていない」と読みかけた | 済 — 押した結果を測ったら、**既定値で足す3件 / 理由を言う18件 / 黙る0件**だった。「入力前に示す」より「押したら教える」設計で、初回1回学べば済む。**欠陥ではない**。あわせて「記号だけの部品が半数超」も**文字だけを数えた誤り**で、説明(tooltip)を測ったら**114個すべてにあった** (`terms.js` T3 がゲート) |

## D. 残 — 着手する

(現在なし。消化したものは A / B / C へ移した)

## E. 見送り — 着手しない (理由付き・2026-08-21 実測で再検証)

PL 診断で「見送り欄が触っていない置き場になっている」と検出されたため、
全項目を実測し直した。実測値と確認日を付ける。

| # | 内容 | 実測 (2026-08-21) | 判断 |
|---|---|---|---|
| E1 | gantt の `buildOverlay` / `renderProps` を gantt.js へ移設 | **完了**。移設時 app.js 3073 → 2232行。**その後の機能追加で 2583行 (2026-08-21 再実測)** | B23 / B24 へ移動。行数は記録時点の値であり現在値ではない |
| E2 | 重ね合わせを持たない図種のオーバーレイ | **記録が誤っていた (2026-08-21 訂正)**。「4図種」ではなく **11図種**、しかも「mermaid が id を出さない」が全図種に当てはまらなかった。**kanban だけは列名と DSL の id をそのまま SVG に出しており、順序に依らない** → 実装した (A79)。残る10図種は id が `node-1` / `edge_0_1` のような位置由来で、並べ替えると別の要素を指す | 10図種は見送り継続。理由が全図種に当てはまるかを確かめずに一括りにしていた |
| E5 | 一覧の絞り込み / 検索 | `ma-list-filter` が実装済み (B9) | **記述誤り。B9 へ移動してここから削除** |
| E6 | 更新関数のシグネチャ統一 | **記録を訂正 (2026-08-21 再実測)**。`operations.update` は **4引数8図種 / 5引数13図種**の2通り。4引数のものは `opts` を黙って無視するため、要素の種別が2つ以上ある図種で誤った行を編集しうる → sequence で3件 (A80/A81/A82) を検出・修正 | ADR ドラフトを起票済み (`draft-updater-signature-unification.md`)。実施は M2 と同時 |
| E7 | architecture の日本語ラベル / sequence 別名の `;` | **記録が誤っていた (2026-08-21 訂正)**。`service a(server)["設計"]` は **通る**。「引用囲みも効かない」は誤りで、直せる問題を制限として扱っていた → 実装した (A83)。sequence 別名の `;` は引用符でも通らず記録どおり | architecture は解消。sequence の `;` は見送り継続 (診断で原因を名指し) |
| E8 | **契約の識別子 `opts.id` が21図種すべてで効かない** | 測定済み・**見送り**。ADR-012 の識別子は `opts.id` だが、行番号がずれた状態で契約経路を呼ぶと **id で正しい要素に届くのは 0/21**、うち**10図種は別の行を書き換える** (architecture / c4 / gantt / journey / kanban / pie / quadrant / radar / sequence / timeline)、5図種は何も変わらない、3図種は複数行が動く。**ただし実機で到達経路は見つからなかった** — UI の選択は行番号ではなく id を持っており (`{"type":"participant","id":"B"}`)、上に行を足した直後 (描画の間引き中 60ms) に押しても正しい要素が変わる。直さない理由: (1) 利用者に実害が無い (2) `pie` / `quadrant` / `xychart` の id は自動採番 (`__s_1` 等) で**文書中に存在しない**ため id 優先にすると逆に壊れる (3) 収穫逓減の診断直後に7モジュールを触るのは一貫しない |
| E9 | classDiagram のメンバー行が固定字下げ4 | 測定済み・**見送り**。メンバーは入れ子ではなくトップレベル行 (`Foo : +NEW int`) として足す別表現で、字下げが 4 に固定されている。実測: 2字下げの文書では **+2 ずれる** (4字下げの文書では揃う)。ひな形は全図種4字下げなのでツール自身が作ったファイルでは起きない。重さは **Nit**。21モジュールへの横展開はしない |
| E11 | 期間に応じた軸の伸縮 (企画 FEAT-004) | **不要と判明・取り下げ**。詳細モードが既に解決している (C83)。企画検討で GO-LATER にしたが、**自社の現状を測らずに競合の不満をそのまま持ち込んだ**のが誤り | (不要) |
| E12 | 図の内部 id が毎回振り直される (UI-058) | 測定済み・**対応不要**。`<line id="actor1">` → `actor3`、`<g id="root-1">` → `root-3` と変わるが、**座標は完全一致、タグ数も 120 vs 120 で同じ**。mermaid 内部の連番。改善案は「検査側で正規化する」だったが、**正規化すべき既存箇所が無い** — 描画オラクル (G2) は SVG の生バイトを比べていないので影響を受けていない。**検査を作るためだけに検査を作らない** | 決定性を守る検査を作る判断が出た時点 |
| E13 | 版にコミットハッシュが無い (UI-059) | **解決 (2026-08-22)** — B37。「要検討」のまま置かず測ったら、**版は 258 コミット分を1つの番号で表していた**。ハッシュ埋め込みは手段が無い (ビルド工程なし / file:// で自分を読めない) と実測で確認し、**上げ忘れを機械が言う**形に落とした | (解決済み) |
| E14 | 検査器の `click({ force: true })` | 測定済み・**現状維持**。5本で使われており、Playwright の到達可能性検査を飛ばす。実測: 21図種のパネルのボタンで**覆われているものは0件**(「画面外」はスクロールで到達できるので欠陥ではない)。**今日は何も隠していない**が、覆いが生まれても気付けない状態ではある。外すとスクロール絡みで不安定になる可能性があり、**壊れていないものを触らない** | パネルに重なりを持つ UI を入れる判断が出た時点、または「押せるはずのボタンが押せない」報告が出た時点 |
| E15 | 別々の図が同じタイトルだと保存名がぶつかる (UI-066) | 測定済み・**判断待ち**。実測: タイトルが同じ2つの図が、どちらも `プロジェクト計画.mmd` になる。ファイルを開いていれば**開いた名前が優先される**ので、既存の図を直す運用では起きない。起きるのは**新規作成した図を保存するとき**だけ。名前の付け方 (日付を足す / 連番 / 保存時に確認) は**運用の好みが分かれる価値判断**なので代行しない | 名前の付け方の指示が出た時点、または同名で上書きした事例の報告が出た時点 |
| E10 | マイルストーンをオーバーレイで選べない | 測定済み・**見送り**。`getBBox()` が回転前の箱を返すため、較正に入れると他タスクの日付が狂う (C75 に実測)。**正しく扱うなら回転後の中心 (`getCTM`/`getBoundingClientRect`) を使い、かつ較正からは明示的に除外する**必要がある。較正はドラッグの根幹なので、重さ Nit の機能のために触る場所ではない。実害は「マイルストーンだけ選択・ドラッグできない」 | マイルストーンを動かしたいという要望が出た時点。または較正を別の理由で作り直す作業が発生した時点 |

## G. バックログ — やらないが捨ててもいない

| # | 内容 | なぜここにあるか | 復活条件 |
|---|---|---|---|
| G1 | class / er / state の move (旧 E4) | **決着 (2026-08-22 / ラウンド59)。** 委任を受け、**class = 案A(実装) / er・state = 案C(現状維持)** を推奨・実施 (B32)。35サイクル滞留していたが、**一律に決めるのが誤りだった** — 測ったら class だけ条件が揃っていた | (決着済み) |
| G2 | (H区分へ移動) UI-023 パネル構成の作り替え | **2026-08-22 に凍結。** F6 の案の選択が11サイクル無かったため、UI-022 / UI-032 とともに削除リストへ。実測値 (1366x768 で 15/21図種・最大520px) は H区分に残る | (不要) |
| G3 | (H区分へ移動) UI-012 の複写 | **2026-08-22 に凍結。** 11サイクル仕様の決めが無かったため削除リストへ | (不要) |
| G4 | (解消) `parse` の kind と `add` の kind のずれ | **A106 で解消。** 復活条件「契約経由で add を呼ぶ経路を作る時点」を r8 が満たしたため | (不要) |
| G5 | (H区分へ移動) UI-030 欄名の日英混在 | **2026-08-22 に凍結。** 予告どおり次サイクルまでに訳す範囲の指示が無かったため削除リストへ。`terms.js` の計数は残る | (不要) |
| G6 | (H区分へ移動) UI-032 一覧の作り直しが重い | **2026-08-22 に凍結。** UI-022 と同じ根で、案の選択に依存するため一緒に削除リストへ。実測値 (400要素で1打鍵あたり約850ms・打ち終わりから1824ms のうち約800ms) は H区分に残る | (不要) |
| G7 | (H区分へ移動) UI-036 黙って消える行 | **2026-08-22 に凍結。** ラウンド43 で「ラウンド45までに指示が無ければ凍結」と予告しながら、**6サイクル実行を忘れていた** (N4 が検出)。予告どおり削除リストへ | (不要) |
| G8 | (H区分へ移動) UI-037 エッジの端点の `<select>` | **2026-08-22 に凍結。** 予告した期限 (ラウンド54) までに実機での測定も置き換えの判断も無かったため削除リストへ | (不要) |
| G9 | UI-041: **書き出した図の線 (矢印・関連線) が白地でほとんど見えない**。PNG は `#ffffff` で塗る (`src/app.js:1409`) が、図は暗いテーマで描かれているので線が明るいまま。実測 (白に対するコントラスト比、3.0未満は見分けにくい): architecture **1.00 (完全に見えない)** / xychart 1.13 / flowchart・state・class・er・requirement・sequence・block **1.20** / gitGraph 1.96 / gantt 2.09 / mindmap 2.94 — **12図種**。実際に書き出した PNG を見て確認した (ノードは暗い塗りなので文字は読める。**薄いのは線だけ**) | 直し方が3通りあり、**どれも見た目の選択**。**予告 (R56): ラウンド59 までに方針の指示が無ければ H区分へ凍結する。** 既定は現状維持 (白で塗ったまま)。実測値と書き出した PNG は残るので、決まればそこから再開できる。選択肢: (A) 書き出しに画面と同じ暗い背景を敷く (白い文書に貼ると暗い塊になる) / (B) 書き出しのときだけ線を濃くする (画面と成果物が違って見える) / (C) 「書き出しのテーマ」を選べるようにする (UI が増える → MOC の判断が要る)。私の一存で決めない | 書き出しの見た目の方針が決まった時点 (棚卸しのこの行に「採用: A」等と行頭に書けば再開)。または白地で線が見えないという報告が出た時点 | **凍結 (2026-08-22 / ラウンド59)**: 予告した期限 R59 が到来し、9サイクル案の選択が無かった。機械 (`backlog-premises.js`) が期限超過を検出。**既定を案C(現状維持)として H区分へ移す。**12図種のコントラスト比の実測値と3案は下の削除リストに残るので、案を選べばそこから再開できる

## H. 削除リスト — 黙って消さない

| 削った項目 | 理由 | 復活条件 |
|---|---|---|
| G9 UI-041: 書き出した図の線が白地で見えない | **凍結を解除して実施 (2026-08-22)。** 利用者から「宿題は推奨で対応」と委任を受け、**案B を推奨・実施** (B31)。凍結時の実測値 (12図種がコントラスト比 3.0未満) は B31 の実施後の測定で置き換わった | (実施済み・復活条件は不要) |
| E3 gantt M-7 (非同期 refresh の config 競合) | PL 診断3サイクル滞留。実測で再現せず、再現手順も特定できていない。再現しない不具合に予防的変更を入れると、効果を検証できないまま複雑さだけが残る。2サイクル前に「回答が無ければ削除」と予告済み | 実機で再現手順が1つでも見つかるか、遭遇報告があった時点 |
| E5 一覧の絞り込み (旧) | 実装済みだった (B9)。見送り欄に残っていたのは記述誤り | (不要) |
| UI-025 接続ボタンの文言統一 | **批判レビューの改善案の方が誤りだった。** 「+ 遷移追加」(UML状態機械) 「+ 関連追加」(クラス図) 「+ リレーションシップ追加」(ER) はいずれもドメイン上正しい用語で、「+ 接続を追加」に潰すのは一貫性ではなく改悪。実際の摩擦は文言ではなく**位置が図種ごとに動くこと**で、そちらは UI-023 (パネル構成) と同じ MOC 合意待ち | 位置を固定する MOC の合意が取れた時点 |
| F5 | ADR-027 の採番 (`draft-updater-signature-unification.md`) | **済 (2026-08-22)** — 利用者から「宿題は推奨で対応」と委任を受け、**ADR-027 として採番**。22サイクル待って凍結していた項目。README 一覧と categories.md も同時更新 |
| F6 パネル構成の案の選択 + UI-022 / UI-023 / UI-032 | **11サイクル案の選択が無かった。** 既定を**案D(現状維持)**として扱う。3件の実測値は残す — 一覧の走査 18685px / 180ホイール刻み、パネルのはみ出し 15/21図種 最大520px、一覧の作り直し 400要素で1打鍵あたり約850ms。**MOC (`docs/superpowers/specs/2026-08-22-panel-layout-moc.md`) は残るので、案A〜Cのいずれかを選べばそこから再開できる** | 案A〜Cのいずれかが選ばれた時点。または「1画面に収まらない図種でしか作業しない」運用が実際に問題になった報告が出た時点 |
| G3 UI-012: 複写 (Ctrl+C/V) が Gantt のみ | **11サイクル仕様の決めが無かった。** 複写は「何を複製するか」が図種で意味が変わる (gantt のタスク複製 ≠ sankey のノード複製) ので、私の一存で広げない。Delete は契約経由で全図種に広げ済み | 複写の対象が図種ごとに決まった時点。または一覧から外して別枠にする判断が出た時点 |
| G5 UI-030: 欄名の日英混在 | **11サイクル訳す範囲の指示が無かった。** 予告どおり凍結。既定は**現状維持 (英語のまま)**。日本語23種 / 英語19種 (Tech, Description, From, To, Target, Score, Actors, Meta, Icon, Q1〜Q4, Min, Max, Period, Section, X, Y)。`tag` / `docref` は mermaid のキーワードそのものなので原語が正しい。**`terms.js` が毎回数え続けるので、決まればそこから再開できる** | 訳す範囲が決まった時点 (棚卸しのこの行に「訳す: <語の一覧>」と書けば再開) |
| G7 UI-036: 黙って消える行 (classDiagram / timeline) | **11サイクル警告の範囲の指示が無かった。** ラウンド43 で「ラウンド45まで」と予告しながら **6サイクル実行を忘れていた** — N4 (滞留サイクル数を数える) の初回で検出した。既定は**現状維持 (警告を出さない)**。アプリは捏造していない (r13 の規約どおり正しい) ので実害は「打った行が効かないことに気付けない」だけ。**「消しても図が変わらないなら効いていない」という判定式は実測で外れている** (classDiagram はコメント行でも SVG が変わり、timeline は accTitle を誤検出) | 警告する行の範囲が決まった時点 (棚卸しのこの行に「警告: <範囲>」と行頭に書けば再開)。または mermaid がこの2図種でもエラーを返すようになった時点 |
| G8 UI-037: エッジの端点を選ぶ `<select>` に絞り込みが無い | **10サイクル判断が無かった。** 予告どおり凍結。重大度は **Nit〜Minor** (実測で訂正済み): 先頭一致が日本語でも効き、**同じ文字を繰り返すと同じ接頭辞の中を巡回する** (「終」を4回で 終了処理19 → 39 → 59 → 79 → 99) ので、目的の要素には5打鍵・1本結ぶのに約17打鍵。**コストは要素数ではなく「同じ接頭辞を持つ要素の数」に比例する**。当初「206打鍵」と書いたのは↓しか使わない最悪値だった | ラベルの接頭辞が揃う運用 (「処理1」〜) で実際に困った報告が出た時点。または `<select>` を絞り込み付きの部品に替える判断が出た時点 |

## F. 承認待ち — 完了

| # | 内容 | 結果 |
|---|---|---|
| F1 | master へのマージ | 済 — PR 番号を明示して承認を得、PR #13 をマージ (master 06e23eb)。内容を含む PR #6〜#12 は差分0件を確認してクローズ |
| F3 | master へのマージ (第2回) | 済 — PR #15 を利用者がマージ (master `5d9d2ff`、25コミット)。未反映0件を確認 |
| F4 | ECN 化 | 済 — ECN-029 / 030 / 031 を作成し README 一覧に登録 |
| F2 | ADR ドラフトの採番 | 済 — 承認を得て ADR-023〜026 を採番。README 一覧に抜けていた ADR-020〜022 も同時に登録し、categories.md も更新 |
| F5 | ADR-027 の採番 (`draft-updater-signature-unification.md`) | **済 (2026-08-22)** — 利用者から「宿題は推奨で対応」と委任を受け、**ADR-027 として採番**。22サイクル待って凍結していた項目。README 一覧と categories.md も同時更新 |
| F6 | パネル構成の MOC (`docs/superpowers/specs/2026-08-22-panel-layout-moc.md`) | **凍結 (2026-08-22)** — 11サイクル案の選択が無かった。前サイクルで予告したとおり **既定を案D(現状維持)として扱い**、UI-022/023/032 とともに H区分へ移す。**MOC は残るので、案を選べばそこから再開できる。** 実装はしていない |
| F7 | master へのマージ (第3回) | **PR #16 を作成した (2026-08-22)。** ラウンド30〜42 の20コミット。Blocker 3件を含む欠陥17件の修正と検査の網羅の底上げ。**master は変更していない** — マージの可否は利用者が判断する (PR番号を明示した一問一答が規約) |
| F8 | ADR ドラフト6件の採番 | **済 (2026-08-22)** — 委任を受け **ADR-027〜032** として採番。027 更新関数のシグネチャ統一 / 028 並べ替えのブロック意味論 / 029 kind の別名 / 030 描き直しの待ち時間 / 031 欄名の3語 / 032 先送りの前提監視。**索引の鮮度**: README 一覧に6行追加、categories.md に**性能・プロセスの2カテゴリを新設**。ADR-031 のカテゴリ表記が索引の語彙に無かったので UI/UX に統一 (索引に無い語を作らない)。照合: 索引22件 = 実体22件、drafts は空 |

## UI-069 — 一覧で切れた名前を読む手段が無い (Minor / 日次 / 観点3 一貫性・観点7 スケール耐性)

**再現手順**
1. `mermaid-assist.html` を 1366x768 で開く
2. エディタに `flowchart TD` / `N0["ComM_ChannelStateManager_MainFunction"]` /
   `N1["ComM_ChannelStateManager_Init"]` / `N0 --> N1` を入れる
3. 右の一覧を見る

**測ったこと**: 名前欄の幅は **123px**、名前の描画に必要な幅は **273px**。
`text-overflow:ellipsis` で切れ、**`ComM_ChannelStat` までしか読めない**。
`title` 属性が無いのでホバーしても全体が出ない。

**なぜ困るか**: 組込みの BSW 名は先頭が共通で末尾だけ違う
(`ComM_ChannelStateManager_Init` / `_MainFunction` / `_DeInit`)。
見える範囲が全部同じなので、**一覧上で見分けられない**。図の側を目で追うか、
エディタ本文を読みに行くかの二択になる。

**製品の中で不揃いだった**: `src/modules/gantt.js:1158,1181` は自前の行に
`title=` を付けていた。41か所が通る共有関数 `src/ui/properties.js:185` には
無かった。gantt だけ読めて他の20図種は読めない状態だった。

**直した**: `src/ui/properties.js:185` に `title` を足した。
flowchart / class / state で末尾まで出ることを実測。

---

## UI-070 — Fit を押すと見える範囲が減る (Minor / 日次 / 観点3 一貫性)

**再現手順**
1. `mermaid-assist.html` を 1366x768 で開く
2. 60要素の縦フローチャート (`M0 --> M1 --> ... --> M59`) を入れる
3. ツールバーの `Fit` を押す

**測ったこと**: Fit 前は図の **8%** が見えていた。Fit 後は倍率が **239%** に
上がり、見える割合が **3%** に減った。`Fit` は「幅に合わせる」実装
(`src/app.js:zoomToFit`) で、縦長の図では幅いっぱいまで**拡大**する。

**比較**: draw.io の `Ctrl+Shift+H`、Visio の「ウィンドウに合わせる」は
どちらも図の全体が収まる方向に働く。`Fit` と書かれたボタンを押して
見える範囲が減るのは、どの読み方をしても擁護できない。

**直した**: `src/app.js` の `zoomToFit()` で
`fitZoom = min(widthFit, max(heightFit, 1))` とした。
**縦横の両方に収めると倍率が 0.09 まで落ちて文字が読めなくなる**ため、
どちらが良いかという好みの問題には踏み込まず、**害だけを取り除いた**:
拡大した結果はみ出すなら抑えるが、100% を下回ってまで縮めない。

- 60要素: Fit 後 6% → 11% (悪化しない)
- 4要素 : Fit 後 100% → 100%、倍率1.7で幅を活かす (役割は残る)

**途中の誤り**: 最初は `if (heightFit < 1)` とだけ書き、**拡大した後の高さを
見ていなかった**。そのため4要素の小さい図で Fit が 100% → 58% に悪化した。
同じ欠陥の別の顔で、テストの3件目がこれを押さえている。

## UI-073 — キーボードで図を動かせない (Minor / 日次 / 観点1 操作コスト)

**再現手順**: ①40要素の縦フローチャートを入れる ②エディタで `Escape` を押す
③`PageDown` を押す

**測ったこと**: `Escape` で `#preview-pane` に焦点が移るが、
`PageDown` / `ArrowDown` / `End` / `Space` が**どれも効かず** `scrollTop` は 0 のまま。
**スクロールするのは内側の `#preview-container`** で、焦点は外枠に当たっていた。

**なぜ困るか**: キーボード中心のペルソナが、図の下の方を見るのにマウスへ
持ち替えるしかない。A117 で通したキーボード経路がここで切れていた。

**直した**: `focusPreview()` の焦点をスクロールする要素そのものに変更
(`src/app.js`)。`PageDown` で 595px、`End` で 3481px 動くことを実測。
`ArrowDown` は要素選択 (M0→M1→M2) に割り当て済みで、スクロールしないのが正しい。

---

## UI-074 — 選んだ要素が画面の外だと、光っても見えない (Minor / 毎回 / 観点4 フィードバック)

**再現手順**: ①40要素の縦フローチャートを入れる ②一覧で「工程38」の編集を押す

**測ったこと**: 図は光る (overlay の `stroke` が `none` → `#7ee787`) が、
その要素は**枠の上端から 3974px、枠の高さは 681px、`scrollTop` は 0 のまま**。
光っても見えない。そこからホイールで探すことになり、手数は要素数に比例する。

**直した**: `scrollSelectedIntoView()` を追加 (`src/ui/overlay-geom.js`)。
工程20 → 1791、工程38 → 3481 まで動き、いずれも画面内に入ることを実測。
**もう見えている要素では動かさない** (見えているものを選ぶたびに図が跳ねる)。

**呼ぶ場所が全て**:
- `buildNodeOverlay` から呼ぶと**21図種中5つでしか効かない** (flowchart は自前で
  オーバレイを作る)。最初これで flowchart が動かなかった
- 描画経路 (`app.js:413`) から呼ぶと**打鍵のたびに位置を奪う**
- 正解は `rebuildOverlay()` — 選択変更の onChange からしか呼ばれず、全図種が通る

**途中の誤り**: 「同じ選択なら動かさない」ガードを入れたが、**変異を入れても
挙動が変わらず死んだコードだった** (打鍵経路がそもそも通らない)。
効かないコードに「位置を守っている」と書くのは嘘になるので外した。

## UI-075 — 保存先のファイル名が画面内に出ない (Minor / 毎回 / 観点3 一貫性)

**再現手順**: ①`設計-A.mmd` を開く ②ステータス行を見る

**測ったこと**: `document.title` は `"設計-A.mmd — MermaidAssist"` だが、
**ステータス欄にも本文にも `.mmd` が出ない**。タブを何枚も開くとタイトルが
省略され、`Ctrl+S` を押す前に保存先を確かめる手段が「タブをホバーする」しか無い。

**UI-065 (未保存の印) と同じ型の欠落**。あのとき印は画面へ出したのに、
**どのファイルへ書くのかはタイトルバーに残したまま**だった。

**直した**: `#status-file` を追加。4条件で実測 — 起動直後 `プロジェクト計画.mmd` /
図を書いた後 `flowchart-20260823.mmd` / A を開いた後 `設計-A.mmd` /
図種を変えた後 `sequenceDiagram-20260823.mmd`。すべて追随する。

---

## UI-076 — 透過PNGが通常PNGと同じ名前で出る (Minor / 週次 / 観点3 一貫性)

**再現手順**: ①図を書く ②Export ▾ →「PNGとして保存」③Export ▾ →「PNG(透過背景)」

**測ったこと**: 両方とも `flowchart-20260823.png`。2倍版は `@2x` が付くのに
**透過だけ規則から漏れていた**。保存先に同名 + ` (1)` が並び、開くまで
どちらが透過か分からない。中身は確かに違う:

| 書き出し | 左上の画素 |
|---|---|
| 通常 | `[255,255,255,255]` (白・不透明) |
| 透過 | `[0,0,0,0]` (透明) |

**直した**: `-transparent` を付ける。`flowchart-20260823-transparent.png`。
通常版には余計な接尾辞を付けない (毎日使う経路の名前を汚さない)。

**ついでに確認できたこと**: 2倍は本当に2倍 (140x174 → 280x348)。
書き出し4種とも2回で同じサイズ (決定的)。

## UI-077 — 接続モード中に、どれが起点か図で分からない (Minor / 日次 / 観点4 フィードバック)

**再現手順**: ①要素を一覧で選ぶ ②「ここから線を引く」を押す ③図を見る

**測ったこと (DOM 差分で決着)**: 選んだ直後は overlay に `stroke="#7ee787"` が
**1個**、接続モードに入ると **0個**。印が消える。
ステータス行は「接続モード: C から線を引きます — 相手をクリック
(Escape で中止)」と正しく言うが、**視線は図にある**。
起点を確かめるにはステータス行へ視線を移す1手が要った。

**直した**: 接続元を `#f0883e` (橙) で光らせる。**選択の緑とは別の色**に
した — 同じ色だと「選ばれている」のか「ここから線を引く」のかが区別できない。

**判定は hitRect の中でする**: hitRect を直接呼ぶモジュールが4つある
(flowchart / block / c4 / kanban)。呼び出し側に渡させると 21図種のうち
一部だけ光る不揃いを作る。

**途中で作った欠陥**: 実装したら**中止しても印が残った** —
オーバレイを描き直す人がいなかった。`cancelConnectionMode` の呼び出しは
app.js に3か所あり、そこへ個別に再描画を足すと**1つ忘れる**。
`selection.js` と同じ形の `init(callback)` を connection-mode に足し、
**1か所で配線**した。

接続ボタンを持つ3図種 (flowchart / block-beta / stateDiagram) で
起点が光ることを実測。変異4件で対応する検査が落ちる。

## UI-081 — 同じ意味の警告が25通りの言い回しで書かれている (Minor / 日次 / 観点3 一貫性)

**再現手順**: ①`flowchart` で ID 欄を空のまま「+ 追加」→「IDは必須です」
②`kanban` で同じことをする →「Name 必須」③`c4` で →「From/To は必須」

**測ったこと**: 「必須」を告げる alert が **44箇所・25通りの言い回し**で
書かれていた。同じ意味の重複:

| 意味 | 言い回し |
|---|---|
| ID が要る | `IDは必須です` (8) / `ID は必須です` (1) — **空白の有無** |
| From/To が要る | `From/To 必須` / `From/To は必須` / `From / To を選択してください` — **3通り** |
| 名前が要る | `Name は必須です` (4) / `Name 必須` (1) |
| 全部要る | `全項目必須` (4) / `全項目が必要です` (1) |
| ID と Label が要る | `ID と Label 必須` (2) / `ID と Label は必須` (1) |

**なぜ困るか**: 毎日100回この文言を読む人にとって、**同じ条件が違う言い方を
するのは「別のことが起きた」と読める**。図種を跨いで作業するほど効く。

**直した**: 全44箇所を **最頻の形 `<欄名> は必須です`** に統一した。
**頻度で決めており、好みで選んでいない。**
「〜を先に追加してください」(前提が足りない) は別の意味なので触っていない。

**検査の穴も同時に埋めた**: `terms.js` は欄名の一貫性を数えていたのに、
**利用者が実際に読む alert の文言は範囲外**だった (`alert` の出現 0箇所)。
T4 として検査を足し、`X は必須です` の形から外れたら指摘する。
変異 (空白を1つ消す) で落ちることを確認済み。

---

## UI-079 — **撤回**

「本文が空のとき追加を押しても何も起きず、何も言わない」は**誤りだった**。
原因は本文ではなく **ID欄が空であること**で、しかも
`alert('IDは必須です')` が出ていた。
私の測定コードが `p.on('dialog', d => d.accept())` で **alert を潰していた**。
詳細は `docs/review-log.md` と `tools/review/MEASURING.md` の規約10。

## UI-066 — 新規の図の名前が、ひな形のタイトル由来だった (Minor / 週次 / 観点3 一貫性)

**決定**: 2026-08-23 / Kawano Momo — **図種+日付に統一**

**測ったこと (直す前)**:

| 場面 | 名前 |
|---|---|
| 起動直後 | `プロジェクト計画.mmd` (gantt のひな形のタイトル) |
| 図種を切り替えた後 | `sequenceDiagram-20260823.mmd` |

**同じ「新規の図」なのに規則が2通りあった。** かつ同じひな形から作った図は
全部同じ名前になり、保存先に `プロジェクト計画 (1).mmd` が並んで、
**開くまでどれがどれか分からなかった**。

**直した**: `downloadBaseName` からタイトルを外し、常に `<図種>-<日付>` にする。
**開いたファイル名は残す** — そちらは保存先の取り違えを防ぐために要る
(`設計-A.mmd` を開いたら `設計-A.mmd` に書き戻す)。

実機で確認: 起動直後 `gantt-20260823.mmd` / flowchart `flowchart-20260823.mmd` /
A を開いた後 `設計-A.mmd` / 実際の保存名も同じ。

**使われなくなった `title` 引数は残さず落とした。** 死んだ引数は嘘になる。
既存ユニット FN-2 は旧仕様 (タイトルを使う) を記録していたので、
**仕様変更として更新した** (壊れたのではなく、決定で変わった)。
FN-5/FN-6 もシグネチャに合わせた。

e2e 5本追加、変異3件 (タイトルを使う / ファイル名を無視 / 日付を付けない) で確認。

---

## 再監査 — 過去の「黙って失敗する」判定 (UI-079 の撤回を受けて)

21図種で「必須欄を空のまま追加」を **alert の文言を記録して**測り直した:

| 結果 | 件数 |
|---|---|
| 既定値で足す | 2 (gantt / gitGraph) |
| 理由を言う | 19 |
| **黙る** | **0** |

**UI-079 以外に誤報は見つからなかった。**
A118 (保存が失敗しても黙っていた) も正しい指摘だった —
`src/app.js` に `alert(` は **0箇所**で、修正前は `try/catch` も無く、
`downloadAsFile()` が投げれば本当に何も出なかった。

UI-081 の統一も実機で確認: 出た文言は7種で、**すべて「X は必須です」の形**。

## UI-078 — 接続の拒否が黙って起き、モードも解除されていた (Minor / 稀 / 観点4 フィードバック)

**決定**: 2026-08-23 — **全図種で自己ループを許す**(当初の「図種で分ける」は
私の誤った説明が前提だったため、実測を報告して再判断を仰いだ)

**根本原因**: `connection-mode.js` の `notifyTarget` が
**コールバックより先に** `cancelConnectionMode()` を呼んでいた。
`properties.js` の拒否条件3つ (`!target` / `!target.id` / `target.id === src.id`)
は**どれも黙って戻る**ので、押した人から見ると
「何も起きず、接続モードも消えた」状態になり、やり直しに3クリック要った。

**直した**:

1. **自己ループを許す** — 8図種すべてで mermaid が描けることを実測。
   テキストで書けるものを GUI が拒む理由がない
2. `notifyTarget` は**コールバックの戻り値を見てから**解除する。
   `false` なら受理していないのでモードを続ける
   (戻り値を返さない既存の呼び出しは受理として扱い、従来どおり解除)
3. 始点が本文から消えている場合は
   **「線の始点が本文から消えています — 選び直してください (Escape で中止)」**
   と告げてモードを続ける (実測で到達を確認)

**死んだ文言は置かなかった**: `!target.id` の分岐は、`notifyTarget` を呼ぶ
唯一の場所 (`app.js:2446`) が `seqKind && seqId` のときしか呼ばないので
**到達しない**。番人としては残すが、**到達しないコードに「理由を告げる」と
書くと嘘が資産になる**ので文言は持たせていない。

**副産物として確認したこと**: 自己ループを引くと mermaid が空ラベルの
内部ノードを2つ作る (`["Start","End","",""]`)。**手書きの `A --> A` でも同じ**
なので mermaid の描画由来であり、この変更由来ではない。

e2e 5本追加、変異4件 (自己ループを拒む / 拒否でも解除する / 告げる配線を切る /
受理してもモードを続ける) で確認。

## FEAT-001 — エディタで用語を置き換えられるようにした (2026-08-23 決定)

**測ったこと (直す前)**: レビュー指摘で `_Init` → `_Initialize` に統一するとき、
5要素の改名に **15操作** (編集ボタン5回 + 入力5回 + 確定5回)。
エディタは素の `<textarea>` で **Ctrl+H は無反応**だった。

**着手前に操作単位へ分解した** (18件 → GO 5件 / NO-GO 8件 / NEED-INFO 5件)。
作るものと作らないものが実装中にぶれないようにするため。

| ID | 操作 | 実測 |
|---|---|---|
| FEAT-1001 | Ctrl+H で置換の欄を開く | 開くと `replace-find` に焦点が入る |
| FEAT-1002 | 探す語を入れて件数を知る | `_Init` → 「4 件」/ 無い語 → 「見つかりません」 |
| FEAT-1003 | すべて置換する | 4件を置換、状態 OK |
| FEAT-1004 | Ctrl+Z 1回で戻す | 本文が完全に戻る |
| FEAT-1005 | Escape で閉じる | 閉じて焦点がエディタへ戻る |

**15操作 → 2操作**(探す語と置き換える語を入れて1回押す)。

**入れなかったもの (NO-GO 8件)**:
- **正規表現** — 誤った式で本文全体を壊す事故は Ctrl+Z 1回では気付けない
- 1件ずつ確認 / 大小区別 / 単語単位 / 選択範囲 / 図で光らせる / 差分表示 /
  検索語の履歴 / 複数ファイル横断 — いずれも規模 M 以上で、
  **当初の目的 (15操作→2操作) には要らない**

**守ったこと**:
- `pushHistory` を**1回だけ**呼ぶ (`pushHistoryCoalesced` は打鍵の束ね用で、
  混ざると戻る単位がぶれる)。**置換と Ctrl+Z を同じ変更に入れた** —
  戻せない一括置換が一度も動く状態を作らないため
- 空の探す語 / 見つからない語 は**理由を告げて本文を触らない**
- 狭い画面 (800x600) で欄が溢れない (`flex: 1 1 0; min-width: 0`)

e2e 8本追加、変異7件 (履歴を積まない / 見つからないとき黙る / 空のとき黙る /
焦点を入れない / Escape で閉じない / 幅を固定する / 件数を出さない) で確認。

## UI-080 — どこにも繋がっていない要素に気付けない (Minor / 日次 / 観点8)

**再現手順**: ①`flowchart TD` / `A["要件"] --> B["設計"]` / `Z["孤立"]` を書く ②図を見る

**測ったこと (直す前)**: 描けて、何も言わない (「要素: 3 | 関連: 1」と数が出るだけ)。
消し忘れた要素がレビューに出る。回避策は図を目で追って数えることで、
手数は要素数に比例する。

**直した**: `どこにも繋がっていない: 孤立` をステータス行に出す。
gantt の `danglingAfter` と同じ形で **件数ではなく名前**を出し、
多いときは先頭2件 + 「ほかN件」。

### 規則を実測で決めた (図種ごとの対応表を作らない)

21図種を測って分かったこと:

| 分類 | 図種 | 扱い |
|---|---|---|
| **関係を持たない** | gantt / timeline / mindmap / gitGraph / pie / journey / quadrantChart / xychart-beta / packet-beta / kanban / radar-beta (11図種) | **何も言わない**。ひな形の全要素が孤立に見えるため |
| 関係を持つ | sequenceDiagram / flowchart / stateDiagram / classDiagram / erDiagram / requirementDiagram / block-beta / sankey-beta / C4Context / architecture-beta (10図種) | 孤立を数える |

**関係の端点が要素の `id` と一致しない図種がある**:

- **requirementDiagram**: 関係は `name` (`sample_req`) を使い、`id` は
  内側のフィールド (`REQ-001`)。**素朴に id で照合すると全件が孤立に見える**
- **stateDiagram**: `[*]` は開始/終了の擬似状態で要素ではない

→ **要素が持つ「いずれかの文字列」が端点に現れるか**で判定する。
**対応表を持つと図種が増えるたびにずれる** (FEAT-903 で同じ罠を予告済み)。

**21図種のひな形で誤検知 0 件**を実測してから規則を決めた。

e2e 11本追加、変異5件 (出さない / 関係なしでも数える / id だけで照合 /
全部並べる / 名前を出さない) で確認。

### 途中の測定誤り

最初 `e.id` だけで照合し、**requirementDiagram のひな形で1件孤立**と読んだ。
**製品の欠陥ではなく私の測定誤り** — 関係が使う識別子と `e.id` が別物だった。
`MEASURING.md` の「否定形は二重に確かめる」に従って中身を見たら分かった。

## UI-082 — 足した要素の表記が、その文書の流儀に合わない (Minor / 週次 / 観点3・視点2)

**視点2 (図を受け取る側) の測定で検出。**

**測ったこと**: 既存が `A["要件"]` の文書に足すと `NEW[新規]` になり、
**同じ文書に表記が2通り**並ぶ。図の見た目は変わらないが、
**Git 差分で追加行だけ書式が違い**、受け取った人が「なぜここだけ」と読む。

**A115 (入れ子の字下げ) と同じ型。** あのとき「既存の子があればその字下げに
合わせる」と直したのに、**引用符には同じ判断を適用していなかった**(部分適用)。

**直した**: `documentPrefersQuotes(text)` で文書の流儀を読み、
`buildShape` に渡す。実測5条件:

| 文書 | 引用符あり/なし | 足すと |
|---|---|---|
| 全部 引用符あり | 2 / 0 | `NEW["新規"]` |
| 全部 引用符なし | 0 / 2 | `NEW[新規]` |
| 混在 (あり2/なし1) | 2 / 1 | `NEW["新規"]` |
| 混在 (あり1/なし2) | 1 / 2 | `NEW[新規]` |
| **ひな形** | **0 / 4** | **`NEW[新規]`** |

**多数決で決めた。** ひな形は引用符なし (0対4) なので、
**「1つでもあれば付ける」にするとひな形の流儀を壊す**。

**必要性の判定は上書きしない** — `設計(詳細)` のように引用符が要るラベルは、
文書の流儀によらず必ず付く。

### mindmap には入れない (理由を実測で示す)

引用符を条件付きで付けるモジュールは **flowchart と mindmap の2つだけ**。
**mindmap には適用しない**:

`mindmap.js:110-113` に記録がある — **default 形状には囲む場所が無く**、
引用符を付けると `[...]` (四角) になる。つまり mindmap では
**引用符が形状と結びついており**、流儀に合わせると**描かれる形が変わる**。
flowchart の「書式だけ」とは別物。実測: mindmap のひな形は引用符 0 個。

理由を `mindmap.js` にコメントとして残した。**黙って対象外にしない。**

e2e 6本追加、変異4件 (流儀を見ない / 1つでもあれば付ける /
必要性の判定を上書きする / 常に付けない) で確認。
